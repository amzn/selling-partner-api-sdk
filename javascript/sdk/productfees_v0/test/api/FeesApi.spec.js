/**
 * Selling Partner API for Product Fees
 * The Selling Partner API for Product Fees lets you programmatically retrieve estimated fees for a product. You can then account for those fees in your pricing.
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD.
    define(['expect.js', 'sinon', process.cwd()+'/src/index'], factory);
  } else if (typeof module === 'object' && module.exports) {
    // CommonJS-like environments that support module.exports, like Node.
    factory(require('expect.js'), require('sinon'), require(process.cwd()+'/src/index'));
  } else {
    // Browser globals (root is window)
    factory(root.expect, root.sinon, root.SellingPartnerApiForProductFees);
  }
}(this, function(expect, sinon, SellingPartnerApiForProductFees) {
  'use strict';

  var instance;
  var sandbox;
  const testEndpoint = 'https://localhost:3000';
  const testAccessToken = "testAccessToken";

  // Helper function to generate random test data
  function generateMockData(dataType, isArray = false) {
    if (!dataType) return {};

    // Handle array types
    if (isArray) {
      return [generateMockData(dataType), generateMockData(dataType)];
    }

    switch(dataType) {
      case 'String':
        return 'mock-' + Math.random().toString(36).substring(2, 10);
      case 'Number':
        return Math.floor(Math.random() * 1000);
      case 'Boolean':
        return Math.random() > 0.5;
      case 'Date':
        return new Date().toISOString();
      default:
        try {
          const ModelClass = SellingPartnerApiForProductFees[dataType];
          if (ModelClass) {
            const instance = Object.create(ModelClass.prototype);
            return instance;
          }
        } catch (e) {
          console.error("Error creating instance of", dataType);
          return {};
        }
        return {};
    }
  }
  

// Generate mock requests and responses for each operation
const mockgetMyFeesEstimateForASINData = {
  request: {
    'asin': generateMockData('String'),
    'body': generateMockData('GetMyFeesEstimateRequest')
  },
  response: {
    data: generateMockData('GetMyFeesEstimateResponse'),
    statusCode: 200,
    headers: {}
  }
};
const mockgetMyFeesEstimateForSKUData = {
  request: {
    'sellerSKU': generateMockData('String'),
    'body': generateMockData('GetMyFeesEstimateRequest')
  },
  response: {
    data: generateMockData('GetMyFeesEstimateResponse'),
    statusCode: 200,
    headers: {}
  }
};
const mockgetMyFeesEstimatesData = {
  request: {
    'body': generateMockData('[FeesEstimateByIdRequest]', true)
  },
  response: {
    data: generateMockData('FeesEstimateResult'),
    statusCode: 200,
    headers: {}
  }
};

  beforeEach(function() {
    sandbox = sinon.createSandbox();
    var apiClientInstance = new SellingPartnerApiForProductFees.ApiClient(testEndpoint);
    apiClientInstance.applyXAmzAccessTokenToRequest(testAccessToken);
    sandbox.stub(apiClientInstance, 'callApi');
    instance = new SellingPartnerApiForProductFees.FeesApi(apiClientInstance);
  });

  afterEach(function() {
    sandbox.restore();
  });

  describe('FeesApi', function() {
    describe('getMyFeesEstimateForASIN', function() {
      
      it('should successfully call getMyFeesEstimateForASIN', function(done) {
        instance.apiClient.callApi.resolves(mockgetMyFeesEstimateForASINData.response);

        const params = [
          mockgetMyFeesEstimateForASINData.request['asin'],
          mockgetMyFeesEstimateForASINData.request['body']
        ];
        instance.getMyFeesEstimateForASIN(...params)
          .then(function(data) {
            expect(data instanceof SellingPartnerApiForProductFees.GetMyFeesEstimateResponse).to.be.true;
            done();
          })
          .catch(done);
      });

      it('should successfully call getMyFeesEstimateForASINWithHttpInfo', function(done) {
        instance.apiClient.callApi.resolves(mockgetMyFeesEstimateForASINData.response);

        const params = [
          mockgetMyFeesEstimateForASINData.request['asin'],
          mockgetMyFeesEstimateForASINData.request['body']
        ];
        instance.getMyFeesEstimateForASINWithHttpInfo(...params)
          .then(function(response) {
            expect(response).to.have.property('statusCode');
            expect(response.statusCode).to.equal(mockgetMyFeesEstimateForASINData.response.statusCode)
            expect(response).to.have.property('headers');
            expect(response).to.have.property('data');
            done();
          })
          .catch(done);
      });

      it('should handle API errors', function(done) {
        var errorResponse = {
          errors: new Error('Expected error to be thrown'),
          statusCode: 400,
          headers: {}
        };
        instance.apiClient.callApi.rejects(errorResponse);

        const params = [
          mockgetMyFeesEstimateForASINData.request['asin'],
          mockgetMyFeesEstimateForASINData.request['body']
        ];
        instance.getMyFeesEstimateForASIN(...params)
          .then(function() {
            done(new Error('Expected error to be thrown'));
          })
          .catch(function(error) {
            expect(error).to.exist;
            expect(error.statusCode).to.equal(400)
            done();
          });
      });
    });
    describe('getMyFeesEstimateForSKU', function() {
      
      it('should successfully call getMyFeesEstimateForSKU', function(done) {
        instance.apiClient.callApi.resolves(mockgetMyFeesEstimateForSKUData.response);

        const params = [
          mockgetMyFeesEstimateForSKUData.request['sellerSKU'],
          mockgetMyFeesEstimateForSKUData.request['body']
        ];
        instance.getMyFeesEstimateForSKU(...params)
          .then(function(data) {
            expect(data instanceof SellingPartnerApiForProductFees.GetMyFeesEstimateResponse).to.be.true;
            done();
          })
          .catch(done);
      });

      it('should successfully call getMyFeesEstimateForSKUWithHttpInfo', function(done) {
        instance.apiClient.callApi.resolves(mockgetMyFeesEstimateForSKUData.response);

        const params = [
          mockgetMyFeesEstimateForSKUData.request['sellerSKU'],
          mockgetMyFeesEstimateForSKUData.request['body']
        ];
        instance.getMyFeesEstimateForSKUWithHttpInfo(...params)
          .then(function(response) {
            expect(response).to.have.property('statusCode');
            expect(response.statusCode).to.equal(mockgetMyFeesEstimateForSKUData.response.statusCode)
            expect(response).to.have.property('headers');
            expect(response).to.have.property('data');
            done();
          })
          .catch(done);
      });

      it('should handle API errors', function(done) {
        var errorResponse = {
          errors: new Error('Expected error to be thrown'),
          statusCode: 400,
          headers: {}
        };
        instance.apiClient.callApi.rejects(errorResponse);

        const params = [
          mockgetMyFeesEstimateForSKUData.request['sellerSKU'],
          mockgetMyFeesEstimateForSKUData.request['body']
        ];
        instance.getMyFeesEstimateForSKU(...params)
          .then(function() {
            done(new Error('Expected error to be thrown'));
          })
          .catch(function(error) {
            expect(error).to.exist;
            expect(error.statusCode).to.equal(400)
            done();
          });
      });
    });
    describe('getMyFeesEstimates', function() {
      
      it('should successfully call getMyFeesEstimates', function(done) {
        instance.apiClient.callApi.resolves(mockgetMyFeesEstimatesData.response);

        const params = [
          mockgetMyFeesEstimatesData.request['body']
        ];
        instance.getMyFeesEstimates(...params)
          .then(function(data) {
            expect(data instanceof SellingPartnerApiForProductFees.FeesEstimateResult).to.be.true;
            done();
          })
          .catch(done);
      });

      it('should successfully call getMyFeesEstimatesWithHttpInfo', function(done) {
        instance.apiClient.callApi.resolves(mockgetMyFeesEstimatesData.response);

        const params = [
          mockgetMyFeesEstimatesData.request['body']
        ];
        instance.getMyFeesEstimatesWithHttpInfo(...params)
          .then(function(response) {
            expect(response).to.have.property('statusCode');
            expect(response.statusCode).to.equal(mockgetMyFeesEstimatesData.response.statusCode)
            expect(response).to.have.property('headers');
            expect(response).to.have.property('data');
            done();
          })
          .catch(done);
      });

      it('should handle API errors', function(done) {
        var errorResponse = {
          errors: new Error('Expected error to be thrown'),
          statusCode: 400,
          headers: {}
        };
        instance.apiClient.callApi.rejects(errorResponse);

        const params = [
          mockgetMyFeesEstimatesData.request['body']
        ];
        instance.getMyFeesEstimates(...params)
          .then(function() {
            done(new Error('Expected error to be thrown'));
          })
          .catch(function(error) {
            expect(error).to.exist;
            expect(error.statusCode).to.equal(400)
            done();
          });
      });
    });

    describe('constructor', function() {
      it('should use default ApiClient when none provided', function() {
        var defaultInstance = new SellingPartnerApiForProductFees.FeesApi();
        expect(defaultInstance.apiClient).to.equal(SellingPartnerApiForProductFees.ApiClient.instance);
      });

      it('should use provided ApiClient', function() {
        var customClient = new SellingPartnerApiForProductFees.ApiClient();
        var customInstance = new SellingPartnerApiForProductFees.FeesApi(customClient);
        expect(customInstance.apiClient).to.equal(customClient);
      });
    });
  });
}));
