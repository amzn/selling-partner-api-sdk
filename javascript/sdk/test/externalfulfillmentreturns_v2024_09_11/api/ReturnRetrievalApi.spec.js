/**
 * The Selling Partner API for Amazon External Fulfillment Return Item Processing
 * You can use the Amazon External Fulfillment Return Item Processing API to retrieve, track, and process return items through Amazon's External Fulfillment returns management system.
 *
 * The version of the OpenAPI document: 2024-09-11
 * Contact: marketplaceapitest@amazon.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import sinon from 'sinon';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'externalfulfillmentreturns_v2024_09_11', 'index.js');
const TheSellingPartnerApiForAmazonExternalFulfillmentReturnItemProcessing = await import(modulePath);

let instance;
let sandbox;
const testEndpoint = 'https://localhost:3000';
const testAccessToken = "testAccessToken";

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = TheSellingPartnerApiForAmazonExternalFulfillmentReturnItemProcessing[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}

// Generate mock requests and responses for each operation
const mockgetReturnData = {
  request: {
    'returnId': generateMockData('String')
  },
  response: {
    data: generateMockData('Return'),
    statusCode: 200,
    headers: {}
  }
};
const mocklistReturnsData = {
  request: {
  },
  response: {
    data: generateMockData('ReturnsResponse'),
    statusCode: 200,
    headers: {}
  }
};

describe('ReturnRetrievalApi', () => {
  beforeEach(() => {
    sandbox = sinon.createSandbox();
    const apiClientInstance = new TheSellingPartnerApiForAmazonExternalFulfillmentReturnItemProcessing.ApiClient(testEndpoint);
    apiClientInstance.applyXAmzAccessTokenToRequest(testAccessToken);
    sandbox.stub(apiClientInstance, 'callApi');
    instance = new TheSellingPartnerApiForAmazonExternalFulfillmentReturnItemProcessing.ReturnRetrievalApi(apiClientInstance);
  });

  afterEach(() => {
    sandbox.restore();
  });

  describe('getReturn', () => {
    it('should successfully call getReturn', async () => {
      instance.apiClient.callApi.resolves(mockgetReturnData.response);

      const params = [
        mockgetReturnData.request['returnId']
      ];
      const data = await instance.getReturn(...params);

      expect(data instanceof TheSellingPartnerApiForAmazonExternalFulfillmentReturnItemProcessing.Return).to.be.true;
      expect(data).to.equal(mockgetReturnData.response.data);
    });

    it('should successfully call getReturnWithHttpInfo', async () => {
      instance.apiClient.callApi.resolves(mockgetReturnData.response);

      const params = [
        mockgetReturnData.request['returnId']
      ];
      const response = await instance.getReturnWithHttpInfo(...params);

      expect(response).to.have.property('statusCode');
      expect(response.statusCode).to.equal(mockgetReturnData.response.statusCode)
      expect(response).to.have.property('headers');
      expect(response).to.have.property('data');
      expect(response.data).to.equal(mockgetReturnData.response.data)
    });

    it('should handle API errors', async () => {
      const errorResponse = {
        errors: new Error('Expected error to be thrown'),
        statusCode: 400,
        headers: {}
      };
      instance.apiClient.callApi.rejects(errorResponse);

      try {
        const params = [
          mockgetReturnData.request['returnId']
        ];
        await instance.getReturn(...params);
        throw new Error('Expected error to be thrown');
      } catch (error) {
        expect(error).to.exist;
        expect(error.statusCode).to.equal(400);
      }
    });
  });
  describe('listReturns', () => {
    it('should successfully call listReturns', async () => {
      instance.apiClient.callApi.resolves(mocklistReturnsData.response);

      const params = [
      ];
      const data = await instance.listReturns(...params);

      expect(data instanceof TheSellingPartnerApiForAmazonExternalFulfillmentReturnItemProcessing.ReturnsResponse).to.be.true;
      expect(data).to.equal(mocklistReturnsData.response.data);
    });

    it('should successfully call listReturnsWithHttpInfo', async () => {
      instance.apiClient.callApi.resolves(mocklistReturnsData.response);

      const params = [
      ];
      const response = await instance.listReturnsWithHttpInfo(...params);

      expect(response).to.have.property('statusCode');
      expect(response.statusCode).to.equal(mocklistReturnsData.response.statusCode)
      expect(response).to.have.property('headers');
      expect(response).to.have.property('data');
      expect(response.data).to.equal(mocklistReturnsData.response.data)
    });

    it('should handle API errors', async () => {
      const errorResponse = {
        errors: new Error('Expected error to be thrown'),
        statusCode: 400,
        headers: {}
      };
      instance.apiClient.callApi.rejects(errorResponse);

      try {
        const params = [
        ];
        await instance.listReturns(...params);
        throw new Error('Expected error to be thrown');
      } catch (error) {
        expect(error).to.exist;
        expect(error.statusCode).to.equal(400);
      }
    });
  });

  describe('constructor', () => {
    it('should use default ApiClient when none provided', () => {
      const defaultInstance = new TheSellingPartnerApiForAmazonExternalFulfillmentReturnItemProcessing.ReturnRetrievalApi();
      expect(defaultInstance.apiClient).to.equal(TheSellingPartnerApiForAmazonExternalFulfillmentReturnItemProcessing.ApiClient.instance);
    });

    it('should use provided ApiClient', () => {
      const customClient = new TheSellingPartnerApiForAmazonExternalFulfillmentReturnItemProcessing.ApiClient();
      const customInstance = new TheSellingPartnerApiForAmazonExternalFulfillmentReturnItemProcessing.ReturnRetrievalApi(customClient);
      expect(customInstance.apiClient).to.equal(customClient);
    });
  });
});
