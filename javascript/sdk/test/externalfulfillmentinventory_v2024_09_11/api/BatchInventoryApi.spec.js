/**
 * The Selling Partner API for External Fulfillment Inventory Management
 * You can use the Amazon External Fulfillment Inventory API to manage inventory operations in Amazon's External Fulfillment network, including batch inventory updates and retrievals.
 *
 * The version of the OpenAPI document: 2024-09-11
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import expect from 'expect.js';
import sinon from 'sinon';
import { join } from 'path';

const modulePath = join(process.cwd(), 'src', 'externalfulfillmentinventory_v2024_09_11', 'index.js');
const TheSellingPartnerApiForExternalFulfillmentInventoryManagement = await import(modulePath);

let instance;
let sandbox;
const testEndpoint = 'https://localhost:3000';
const testAccessToken = "testAccessToken";

// Helper function to generate random test data
function generateMockData(dataType, isArray = false) {
  if (!dataType) return {};

  // Handle array types
  if (isArray) {
    return [generateMockData(dataType), generateMockData(dataType)];
  }

  switch(dataType) {
    case 'String':
      return 'mock-' + Math.random().toString(36).substring(2, 10);
    case 'Number':
      return Math.floor(Math.random() * 1000);
    case 'Boolean':
      return Math.random() > 0.5;
    case 'Date':
      return new Date().toISOString();
    default:
      try {
        const ModelClass = TheSellingPartnerApiForExternalFulfillmentInventoryManagement[dataType];
        if (ModelClass) {
          const instance = Object.create(ModelClass.prototype);
          return instance;
        }
      } catch (e) {
        console.error("Error creating instance of", dataType);
        return {};
      }
      return {};
  }
}

// Generate mock requests and responses for each operation
const mockbatchInventoryData = {
  request: {
    'body': generateMockData('BatchInventoryRequest')
  },
  response: {
    data: generateMockData('BatchInventoryResponse'),
    statusCode: 207,
    headers: {}
  }
};

describe('BatchInventoryApi', () => {
  beforeEach(() => {
    sandbox = sinon.createSandbox();
    const apiClientInstance = new TheSellingPartnerApiForExternalFulfillmentInventoryManagement.ApiClient(testEndpoint);
    apiClientInstance.applyXAmzAccessTokenToRequest(testAccessToken);
    sandbox.stub(apiClientInstance, 'callApi');
    instance = new TheSellingPartnerApiForExternalFulfillmentInventoryManagement.BatchInventoryApi(apiClientInstance);
  });

  afterEach(() => {
    sandbox.restore();
  });

  describe('batchInventory', () => {
    it('should successfully call batchInventory', async () => {
      instance.apiClient.callApi.resolves(mockbatchInventoryData.response);

      const params = [
        mockbatchInventoryData.request['body']
      ];
      const data = await instance.batchInventory(...params);

      expect(data instanceof TheSellingPartnerApiForExternalFulfillmentInventoryManagement.BatchInventoryResponse).to.be.true;
      expect(data).to.equal(mockbatchInventoryData.response.data);
    });

    it('should successfully call batchInventoryWithHttpInfo', async () => {
      instance.apiClient.callApi.resolves(mockbatchInventoryData.response);

      const params = [
        mockbatchInventoryData.request['body']
      ];
      const response = await instance.batchInventoryWithHttpInfo(...params);

      expect(response).to.have.property('statusCode');
      expect(response.statusCode).to.equal(mockbatchInventoryData.response.statusCode)
      expect(response).to.have.property('headers');
      expect(response).to.have.property('data');
      expect(response.data).to.equal(mockbatchInventoryData.response.data)
    });

    it('should handle API errors', async () => {
      const errorResponse = {
        errors: new Error('Expected error to be thrown'),
        statusCode: 400,
        headers: {}
      };
      instance.apiClient.callApi.rejects(errorResponse);

      try {
        const params = [
          mockbatchInventoryData.request['body']
        ];
        await instance.batchInventory(...params);
        throw new Error('Expected error to be thrown');
      } catch (error) {
        expect(error).to.exist;
        expect(error.statusCode).to.equal(400);
      }
    });
  });

  describe('constructor', () => {
    it('should use default ApiClient when none provided', () => {
      const defaultInstance = new TheSellingPartnerApiForExternalFulfillmentInventoryManagement.BatchInventoryApi();
      expect(defaultInstance.apiClient).to.equal(TheSellingPartnerApiForExternalFulfillmentInventoryManagement.ApiClient.instance);
    });

    it('should use provided ApiClient', () => {
      const customClient = new TheSellingPartnerApiForExternalFulfillmentInventoryManagement.ApiClient();
      const customInstance = new TheSellingPartnerApiForExternalFulfillmentInventoryManagement.BatchInventoryApi(customClient);
      expect(customInstance.apiClient).to.equal(customClient);
    });
  });
});
