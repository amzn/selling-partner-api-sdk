/**
 * The Selling Partner API for tracking services provided by Amazon.
 * The Tracking Selling Partner API allows users to track shipments to and from Amazon-owned storage warehouses, fulfillment centers, and external warehouses.
 *
 * The version of the OpenAPI document: 2026-01-30
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import { ApiClient } from '../ApiClient.js'
import { TrackingEstimate } from './TrackingEstimate.js'
import { TrackingIdentifier } from './TrackingIdentifier.js'
import { TrackingMilestone } from './TrackingMilestone.js'

/**
 * The TrackingDetail model module.
 * @module tracking_v2026_01_30/model/TrackingDetail
 * @version 2026-01-30
 */
export class TrackingDetail {
  /**
   * Constructs a new <code>TrackingDetail</code>.
   * Tracking information for the shipment identifier.
   * @alias module:tracking_v2026_01_30/model/TrackingDetail
   * @class
   * @param identifier {TrackingIdentifier}
   * @param milestoneHistory {TrackingMilestone[]} A chronological list of all milestone events for the shipment, from most recent to oldest.
   */
  constructor (identifier, milestoneHistory) {
    this.identifier = identifier
    this.milestoneHistory = milestoneHistory
  }

  /**
   * Constructs a <code>TrackingDetail</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {TrackingDetail} obj Optional instance to populate.
   * @return {TrackingDetail} The populated <code>TrackingDetail</code> instance.
   */
  static constructFromObject (data, obj) {
    if (data) {
      switch (typeof data) {
        case 'string':
          obj = String(data)
          break
        case 'number':
          obj = Number(data)
          break
        case 'boolean':
          obj = Boolean(data)
          break
      }
      obj = obj || new TrackingDetail()
      if (data.hasOwnProperty('identifier')) { obj.identifier = TrackingIdentifier.constructFromObject(data.identifier) }
      if (data.hasOwnProperty('trackingUrl')) { obj.trackingUrl = ApiClient.convertToType(data.trackingUrl, 'String') }
      if (data.hasOwnProperty('trackingEstimates')) { obj.trackingEstimates = ApiClient.convertToType(data.trackingEstimates, [TrackingEstimate]) }
      if (data.hasOwnProperty('latestMilestone')) { obj.latestMilestone = TrackingMilestone.constructFromObject(data.latestMilestone) }
      if (data.hasOwnProperty('milestoneHistory')) { obj.milestoneHistory = ApiClient.convertToType(data.milestoneHistory, [TrackingMilestone]) }
    }
    return obj
  }
}

/**
 * @member {TrackingIdentifier} identifier
 * @type {TrackingIdentifier}
 */
TrackingDetail.prototype.identifier = undefined

/**
 * The URL to view the shipment's tracking information.
 * @member {String} trackingUrl
 * @type {String}
 */
TrackingDetail.prototype.trackingUrl = undefined

/**
 * A list of time-based estimated intervals for the shipment, such as estimated delivery date.
 * @member {TrackingEstimate[]} trackingEstimates
 * @type {TrackingEstimate[]}
 */
TrackingDetail.prototype.trackingEstimates = undefined

/**
 * @member {TrackingMilestone} latestMilestone
 * @type {TrackingMilestone}
 */
TrackingDetail.prototype.latestMilestone = undefined

/**
 * A chronological list of all milestone events for the shipment, from most recent to oldest.
 * @member {TrackingMilestone[]} milestoneHistory
 * @type {TrackingMilestone[]}
 */
TrackingDetail.prototype.milestoneHistory = undefined
