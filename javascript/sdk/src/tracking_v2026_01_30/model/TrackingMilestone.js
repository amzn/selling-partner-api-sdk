/**
 * The Selling Partner API for tracking services provided by Amazon.
 * The Tracking Selling Partner API allows users to track shipments to and from Amazon-owned storage warehouses, fulfillment centers, and external warehouses.
 *
 * The version of the OpenAPI document: 2026-01-30
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import { ApiClient } from '../ApiClient.js'
import { TrackingMilestoneLocation } from './TrackingMilestoneLocation.js'
import { TrackingMilestoneStatus } from './TrackingMilestoneStatus.js'

/**
 * The TrackingMilestone model module.
 * @module tracking_v2026_01_30/model/TrackingMilestone
 * @version 2026-01-30
 */
export class TrackingMilestone {
  /**
   * Constructs a new <code>TrackingMilestone</code>.
   * A single milestone event in a shipment&#39;s journey.
   * @alias module:tracking_v2026_01_30/model/TrackingMilestone
   * @class
   * @param status {TrackingMilestoneStatus}
   * @param occurredAt {String} A timestamp in UTC RFC 3339 date-time format.
   */
  constructor (status, occurredAt) {
    this.status = status
    this.occurredAt = occurredAt
  }

  /**
   * Constructs a <code>TrackingMilestone</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {TrackingMilestone} obj Optional instance to populate.
   * @return {TrackingMilestone} The populated <code>TrackingMilestone</code> instance.
   */
  static constructFromObject (data, obj) {
    if (data) {
      switch (typeof data) {
        case 'string':
          obj = String(data)
          break
        case 'number':
          obj = Number(data)
          break
        case 'boolean':
          obj = Boolean(data)
          break
      }
      obj = obj || new TrackingMilestone()
      if (data.hasOwnProperty('status')) { obj.status = TrackingMilestoneStatus.constructFromObject(data.status) }
      if (data.hasOwnProperty('location')) { obj.location = TrackingMilestoneLocation.constructFromObject(data.location) }
      if (data.hasOwnProperty('occurredAt')) { obj.occurredAt = ApiClient.convertToType(data.occurredAt, 'String') }
    }
    return obj
  }
}

/**
 * @member {TrackingMilestoneStatus} status
 * @type {TrackingMilestoneStatus}
 */
TrackingMilestone.prototype.status = undefined

/**
 * @member {TrackingMilestoneLocation} location
 * @type {TrackingMilestoneLocation}
 */
TrackingMilestone.prototype.location = undefined

/**
 * A timestamp in UTC RFC 3339 date-time format.
 * @member {String} occurredAt
 * @type {String}
 */
TrackingMilestone.prototype.occurredAt = undefined
