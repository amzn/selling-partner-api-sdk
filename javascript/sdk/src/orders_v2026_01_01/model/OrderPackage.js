/**
 * The Selling Partner API for Orders
 * The Selling Partner API for Orders returns orders information. This API supports the following types of orders:  - FBM (Fulfilled by Merchant) - FBA (Fulfilled by Amazon) - read only - Amazon Fresh  This API does not display order data that is more than two years old, except in the JP, AU, and SG marketplaces, for which data from 2016 and after is available.
 *
 * The version of the OpenAPI document: 2026-01-01
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import { ApiClient } from '../ApiClient.js'
import { MerchantAddress } from './MerchantAddress.js'
import { PackageItem } from './PackageItem.js'
import { PackageStatus } from './PackageStatus.js'

/**
 * The OrderPackage model module.
 * @module orders_v2026_01_01/model/OrderPackage
 * @version 2026-01-01
 */
export class OrderPackage {
  /**
   * Constructs a new <code>OrderPackage</code>.
   * Information about a physical shipping package, including tracking details. **Note:** Only available for merchant-fulfilled (FBM) orders.
   * @alias module:orders_v2026_01_01/model/OrderPackage
   * @class
   * @param packageReferenceId {String} A unique identifier for this package within the context of the order.
   */
  constructor (packageReferenceId) {
    this.packageReferenceId = packageReferenceId
  }

  /**
   * Constructs a <code>OrderPackage</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {OrderPackage} obj Optional instance to populate.
   * @return {OrderPackage} The populated <code>OrderPackage</code> instance.
   */
  static constructFromObject (data, obj) {
    if (data) {
      switch (typeof data) {
        case 'string':
          obj = String(data)
          break
        case 'number':
          obj = Number(data)
          break
        case 'boolean':
          obj = Boolean(data)
          break
      }
      obj = obj || new OrderPackage()
      if (data.hasOwnProperty('packageReferenceId')) { obj.packageReferenceId = ApiClient.convertToType(data.packageReferenceId, 'String') }
      if (data.hasOwnProperty('createdTime')) { obj.createdTime = ApiClient.convertToType(data.createdTime, 'Date') }
      if (data.hasOwnProperty('packageStatus')) { obj.packageStatus = PackageStatus.constructFromObject(data.packageStatus) }
      if (data.hasOwnProperty('carrier')) { obj.carrier = ApiClient.convertToType(data.carrier, 'String') }
      if (data.hasOwnProperty('shipTime')) { obj.shipTime = ApiClient.convertToType(data.shipTime, 'Date') }
      if (data.hasOwnProperty('shippingService')) { obj.shippingService = ApiClient.convertToType(data.shippingService, 'String') }
      if (data.hasOwnProperty('trackingNumber')) { obj.trackingNumber = ApiClient.convertToType(data.trackingNumber, 'String') }
      if (data.hasOwnProperty('shipFromAddress')) { obj.shipFromAddress = MerchantAddress.constructFromObject(data.shipFromAddress) }
      if (data.hasOwnProperty('packageItems')) { obj.packageItems = ApiClient.convertToType(data.packageItems, [PackageItem]) }
    }
    return obj
  }
}

/**
 * A unique identifier for this package within the context of the order.
 * @member {String} packageReferenceId
 * @type {String}
 */
OrderPackage.prototype.packageReferenceId = undefined

/**
 * The exact time when this shipping package was created and prepared for shipment. In [ISO 8601](https://developer-docs.amazon.com/sp-api/docs/iso-8601) format.
 * @member {Date} createdTime
 * @type {Date}
 */
OrderPackage.prototype.createdTime = undefined

/**
 * @member {PackageStatus} packageStatus
 * @type {PackageStatus}
 */
OrderPackage.prototype.packageStatus = undefined

/**
 * The carrier responsible for transporting this package to the customer.
 * @member {String} carrier
 * @type {String}
 */
OrderPackage.prototype.carrier = undefined

/**
 * The exact time when this package was handed over to the carrier and began its journey to the customer. In [ISO 8601](https://developer-docs.amazon.com/sp-api/docs/iso-8601) format.
 * @member {Date} shipTime
 * @type {Date}
 */
OrderPackage.prototype.shipTime = undefined

/**
 * The specific shipping method or service used for delivering this package.
 * @member {String} shippingService
 * @type {String}
 */
OrderPackage.prototype.shippingService = undefined

/**
 * The carrier-provided tracking number that customers can use to monitor the package's delivery progress.
 * @member {String} trackingNumber
 * @type {String}
 */
OrderPackage.prototype.trackingNumber = undefined

/**
 * @member {MerchantAddress} shipFromAddress
 * @type {MerchantAddress}
 */
OrderPackage.prototype.shipFromAddress = undefined

/**
 * A list of all order items included in this specific package.
 * @member {PackageItem[]} packageItems
 * @type {PackageItem[]}
 */
OrderPackage.prototype.packageItems = undefined
