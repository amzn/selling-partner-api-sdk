/**
 * The Selling Partner API for Finances
 * The Selling Partner API for Finances provides financial information relevant to a seller's business. You can obtain financial events for a given order or date range without having to wait until a statement period closes.
 *
 * The version of the OpenAPI document: 2024-06-19
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import { ApiClient } from '../ApiClient.js'
import { Currency } from './Currency.js'
import { PartnerMetadata } from './PartnerMetadata.js'

/**
 * The Balance model module.
 * @module finances_2024_06_19/model/Balance
 * @version 2024-06-19
 */
export class Balance {
  /**
   * Constructs a new <code>Balance</code>.
   * All information relevant to a balance.
   * @alias module:finances_2024_06_19/model/Balance
   * @class
   */
  constructor () {
  }

  /**
   * Constructs a <code>Balance</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {Balance} obj Optional instance to populate.
   * @return {Balance} The populated <code>Balance</code> instance.
   */
  static constructFromObject (data, obj) {
    if (data) {
      switch (typeof data) {
        case 'string':
          obj = String(data)
          break
        case 'number':
          obj = Number(data)
          break
        case 'boolean':
          obj = Boolean(data)
          break
      }
      obj = obj || new Balance()
      if (data.hasOwnProperty('partnerMetadata')) { obj.partnerMetadata = PartnerMetadata.constructFromObject(data.partnerMetadata) }
      if (data.hasOwnProperty('balanceType')) { obj.balanceType = ApiClient.convertToType(data.balanceType, 'String') }
      if (data.hasOwnProperty('amount')) { obj.amount = Currency.constructFromObject(data.amount) }
      if (data.hasOwnProperty('balances')) { obj.balances = ApiClient.convertToType(data.balances, [Balance]) }
      if (data.hasOwnProperty('asOfDate')) { obj.asOfDate = ApiClient.convertToType(data.asOfDate, 'Date') }
      if (data.hasOwnProperty('lastUpdatedTime')) { obj.lastUpdatedTime = ApiClient.convertToType(data.lastUpdatedTime, 'Date') }
    }
    return obj
  }
}

/**
 * @member {PartnerMetadata} partnerMetadata
 * @type {PartnerMetadata}
 */
Balance.prototype.partnerMetadata = undefined

/**
 * Allowed values for the <code>balanceType</code> property.
 * @enum {String}
 * @readonly
 */
Balance.BalanceTypeEnum = {

  /**
     * value: "AVAILABLE"
     * @const
     */
  AVAILABLE: 'AVAILABLE',

  /**
     * value: "RESERVED"
     * @const
     */
  RESERVED: 'RESERVED',

  /**
     * value: "ACCOUNT_LEVEL_RESERVE"
     * @const
     */
  ACCOUNT_LEVEL_RESERVE: 'ACCOUNT_LEVEL_RESERVE',

  /**
     * value: "DEFERRED"
     * @const
     */
  DEFERRED: 'DEFERRED',

  /**
     * value: "TOTAL"
     * @const
     */
  TOTAL: 'TOTAL'
}
/**
 * The type of balance.
 * @member {String} balanceType
 * @type {String}
 */
Balance.prototype.balanceType = undefined

/**
 * @member {Currency} amount
 * @type {Currency}
 */
Balance.prototype.amount = undefined

/**
 * A list of balances that comprise this balance.
 * @member {Balance[]} balances
 * @type {Balance[]}
 */
Balance.prototype.balances = undefined

/**
 * A date in [ISO 8601](https://developer-docs.amazon.com/sp-api/docs/iso-8601) date-time format.
 * @member {Date} asOfDate
 * @type {Date}
 */
Balance.prototype.asOfDate = undefined

/**
 * The date and time when the balance was last updated, in [ISO 8601](https://developer-docs.amazon.com/sp-api/docs/iso-8601) date-time format. The value is in the marketplace's local time zone. This field is present for point-in-time balance queries (when no `asOfDate` query parameter is provided). Null for ending balance queries.
 * @member {Date} lastUpdatedTime
 * @type {Date}
 */
Balance.prototype.lastUpdatedTime = undefined
