/**
 * The Selling Partner API for Fulfillment Outbound
 * The Selling Partner API for Fulfillment Outbound lets you create applications that help a seller fulfill Multi-Channel Fulfillment orders using their inventory in Amazon's fulfillment network. You can get information on both potential and existing fulfillment orders.
 *
 * The version of the OpenAPI document: 2026-07-04
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import { ApiClient } from '../ApiClient.js'
import { CreateOrderLineItem } from './CreateOrderLineItem.js'
import { OrderDestination } from './OrderDestination.js'
import { OrderFulfillmentConfiguration } from './OrderFulfillmentConfiguration.js'
import { OrderOrigin } from './OrderOrigin.js'
import { PaymentInformation } from './PaymentInformation.js'

/**
 * The CreateOrderRequest model module.
 * @module fulfillmentoutbound_v2026_07_04/model/CreateOrderRequest
 * @version 2026-07-04
 */
export class CreateOrderRequest {
  /**
   * Constructs a new <code>CreateOrderRequest</code>.
   * The request body schema for &#x60;createOrder&#x60;.
   * @alias module:fulfillmentoutbound_v2026_07_04/model/CreateOrderRequest
   * @class
   * @param orderId {String} A fulfillment order identifier that the seller creates to track their fulfillment order. The `orderId` must be unique for each fulfillment order that a seller creates. If the seller's system already creates unique order identifiers, then these might be good values for them to use.
   * @param destination {OrderDestination}
   * @param lineItems {CreateOrderLineItem[]} A list of items to include in the fulfillment order, including quantity.
   */
  constructor (orderId, destination, lineItems) {
    this.orderId = orderId
    this.destination = destination
    this.lineItems = lineItems
  }

  /**
   * Constructs a <code>CreateOrderRequest</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {CreateOrderRequest} obj Optional instance to populate.
   * @return {CreateOrderRequest} The populated <code>CreateOrderRequest</code> instance.
   */
  static constructFromObject (data, obj) {
    if (data) {
      switch (typeof data) {
        case 'string':
          obj = String(data)
          break
        case 'number':
          obj = Number(data)
          break
        case 'boolean':
          obj = Boolean(data)
          break
      }
      obj = obj || new CreateOrderRequest()
      if (data.hasOwnProperty('orderId')) { obj.orderId = ApiClient.convertToType(data.orderId, 'String') }
      if (data.hasOwnProperty('channel')) { obj.channel = ApiClient.convertToType(data.channel, 'String') }
      if (data.hasOwnProperty('fulfillmentConfiguration')) { obj.fulfillmentConfiguration = OrderFulfillmentConfiguration.constructFromObject(data.fulfillmentConfiguration) }
      if (data.hasOwnProperty('origin')) { obj.origin = OrderOrigin.constructFromObject(data.origin) }
      if (data.hasOwnProperty('destination')) { obj.destination = OrderDestination.constructFromObject(data.destination) }
      if (data.hasOwnProperty('lineItems')) { obj.lineItems = ApiClient.convertToType(data.lineItems, [CreateOrderLineItem]) }
      if (data.hasOwnProperty('paymentInformation')) { obj.paymentInformation = PaymentInformation.constructFromObject(data.paymentInformation) }
    }
    return obj
  }
}

/**
 * A fulfillment order identifier that the seller creates to track their fulfillment order. The `orderId` must be unique for each fulfillment order that a seller creates. If the seller's system already creates unique order identifiers, then these might be good values for them to use.
 * @member {String} orderId
 * @type {String}
 */
CreateOrderRequest.prototype.orderId = undefined

/**
 * The sales channel of the fulfillment order.
 * @member {String} channel
 * @type {String}
 */
CreateOrderRequest.prototype.channel = undefined

/**
 * @member {OrderFulfillmentConfiguration} fulfillmentConfiguration
 * @type {OrderFulfillmentConfiguration}
 */
CreateOrderRequest.prototype.fulfillmentConfiguration = undefined

/**
 * @member {OrderOrigin} origin
 * @type {OrderOrigin}
 */
CreateOrderRequest.prototype.origin = undefined

/**
 * @member {OrderDestination} destination
 * @type {OrderDestination}
 */
CreateOrderRequest.prototype.destination = undefined

/**
 * A list of items to include in the fulfillment order, including quantity.
 * @member {CreateOrderLineItem[]} lineItems
 * @type {CreateOrderLineItem[]}
 */
CreateOrderRequest.prototype.lineItems = undefined

/**
 * @member {PaymentInformation} paymentInformation
 * @type {PaymentInformation}
 */
CreateOrderRequest.prototype.paymentInformation = undefined
