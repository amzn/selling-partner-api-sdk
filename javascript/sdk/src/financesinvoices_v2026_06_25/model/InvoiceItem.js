/**
 * The Selling Partner API for Invoices
 * The Selling Partner API for Invoices enables you to retrieve invoice data and streamline your bookkeeping processes.
 *
 * The version of the OpenAPI document: 2026-06-25
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import { ApiClient } from '../ApiClient.js'
import { Breakdown } from './Breakdown.js'
import { Currency } from './Currency.js'
import { LineItemContext } from './LineItemContext.js'
import { RelatedIdentifier } from './RelatedIdentifier.js'

/**
 * The InvoiceItem model module.
 * @module financesinvoices_v2026_06_25/model/InvoiceItem
 * @version 2026-06-25
 */
export class InvoiceItem {
  /**
   * Constructs a new <code>InvoiceItem</code>.
   * A line item within an invoice, including unit costs, quantities, tax information, and contextual details about the goods or services being invoiced.
   * @alias module:financesinvoices_v2026_06_25/model/InvoiceItem
   * @class
   * @param lineItemId {String} The unique identifier for the line item.
   * @param totalAmount {Currency}
   * @param lineItemContext {LineItemContext}
   */
  constructor (lineItemId, totalAmount, lineItemContext) {
    this.lineItemId = lineItemId
    this.totalAmount = totalAmount
    this.lineItemContext = lineItemContext
  }

  /**
   * Constructs a <code>InvoiceItem</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {InvoiceItem} obj Optional instance to populate.
   * @return {InvoiceItem} The populated <code>InvoiceItem</code> instance.
   */
  static constructFromObject (data, obj) {
    if (data) {
      switch (typeof data) {
        case 'string':
          obj = String(data)
          break
        case 'number':
          obj = Number(data)
          break
        case 'boolean':
          obj = Boolean(data)
          break
      }
      obj = obj || new InvoiceItem()
      if (data.hasOwnProperty('lineItemId')) { obj.lineItemId = ApiClient.convertToType(data.lineItemId, 'String') }
      if (data.hasOwnProperty('unitCost')) { obj.unitCost = Currency.constructFromObject(data.unitCost) }
      if (data.hasOwnProperty('quantity')) { obj.quantity = ApiClient.convertToType(data.quantity, 'Number') }
      if (data.hasOwnProperty('taxRateCodes')) { obj.taxRateCodes = ApiClient.convertToType(data.taxRateCodes, ['String']) }
      if (data.hasOwnProperty('totalAmount')) { obj.totalAmount = Currency.constructFromObject(data.totalAmount) }
      if (data.hasOwnProperty('totalAmountBreakdown')) { obj.totalAmountBreakdown = ApiClient.convertToType(data.totalAmountBreakdown, [Breakdown]) }
      if (data.hasOwnProperty('lineItemContext')) { obj.lineItemContext = LineItemContext.constructFromObject(data.lineItemContext) }
      if (data.hasOwnProperty('relatedItemIdentifiers')) { obj.relatedItemIdentifiers = ApiClient.convertToType(data.relatedItemIdentifiers, [RelatedIdentifier]) }
    }
    return obj
  }
}

/**
 * The unique identifier for the line item.
 * @member {String} lineItemId
 * @type {String}
 */
InvoiceItem.prototype.lineItemId = undefined

/**
 * @member {Currency} unitCost
 * @type {Currency}
 */
InvoiceItem.prototype.unitCost = undefined

/**
 * The number of units for the line item.
 * @member {Number} quantity
 * @type {Number}
 */
InvoiceItem.prototype.quantity = undefined

/**
 * A list of country code and tax rate combinations applied to the line item. Example: `DE 19`
 * @member {String[]} taxRateCodes
 * @type {String[]}
 */
InvoiceItem.prototype.taxRateCodes = undefined

/**
 * @member {Currency} totalAmount
 * @type {Currency}
 */
InvoiceItem.prototype.totalAmount = undefined

/**
 * The breakdown of the total amount into sub-components.  **Possible `breakdownType` values:**  * `ItemCostWithoutTax`: Line item cost excluding tax. * `ItemTaxAmount`: Tax amount on the line item. * `Other`: Other line item breakdown type.
 * @member {Breakdown[]} totalAmountBreakdown
 * @type {Breakdown[]}
 */
InvoiceItem.prototype.totalAmountBreakdown = undefined

/**
 * @member {LineItemContext} lineItemContext
 * @type {LineItemContext}
 */
InvoiceItem.prototype.lineItemContext = undefined

/**
 * List of related item identifiers for the line item.
 * @member {RelatedIdentifier[]} relatedItemIdentifiers
 * @type {RelatedIdentifier[]}
 */
InvoiceItem.prototype.relatedItemIdentifiers = undefined
