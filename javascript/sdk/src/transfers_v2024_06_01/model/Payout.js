/**
 * The Selling Partner API for Transfers.
 * The Selling Partner API for Transfers enables selling partners to retrieve payment methods and initiate payouts for their seller accounts. This API supports the following marketplaces: DE, FR, IT, ES, SE, NL, PL, and BE.
 *
 * The version of the OpenAPI document: 2024-06-01
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import { ApiClient } from '../ApiClient.js'
import { Currency } from './Currency.js'
import { PartnerMetadata } from './PartnerMetadata.js'
import { PaymentMethodName } from './PaymentMethodName.js'
import { PayoutStatusEntry } from './PayoutStatusEntry.js'
import { PayoutStatusName } from './PayoutStatusName.js'
import { PayoutTypeName } from './PayoutTypeName.js'
import { Period } from './Period.js'
import { RelatedIdentifier } from './RelatedIdentifier.js'

/**
 * The Payout model module.
 * @module transfers_v2024_06_01/model/Payout
 * @version 2024-06-01
 */
export class Payout {
  /**
   * Constructs a new <code>Payout</code>.
   * All the information related to a payout.
   * @alias module:transfers_v2024_06_01/model/Payout
   * @class
   * @param id {String} The unique identifier of the payout.
   * @param partnerMetadata {PartnerMetadata}
   * @param currentStatus {PayoutStatusName}
   * @param statusHistory {PayoutStatusEntry[]} A chronological list of payout status updates.
   */
  constructor (id, partnerMetadata, currentStatus, statusHistory) {
    this.id = id
    this.partnerMetadata = partnerMetadata
    this.currentStatus = currentStatus
    this.statusHistory = statusHistory
  }

  /**
   * Constructs a <code>Payout</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {Payout} obj Optional instance to populate.
   * @return {Payout} The populated <code>Payout</code> instance.
   */
  static constructFromObject (data, obj) {
    if (data) {
      switch (typeof data) {
        case 'string':
          obj = String(data)
          break
        case 'number':
          obj = Number(data)
          break
        case 'boolean':
          obj = Boolean(data)
          break
      }
      obj = obj || new Payout()
      if (data.hasOwnProperty('id')) { obj.id = ApiClient.convertToType(data.id, 'String') }
      if (data.hasOwnProperty('partnerMetadata')) { obj.partnerMetadata = PartnerMetadata.constructFromObject(data.partnerMetadata) }
      if (data.hasOwnProperty('currentStatus')) { obj.currentStatus = PayoutStatusName.constructFromObject(data.currentStatus) }
      if (data.hasOwnProperty('statusHistory')) { obj.statusHistory = ApiClient.convertToType(data.statusHistory, [PayoutStatusEntry]) }
      if (data.hasOwnProperty('type')) { obj.type = PayoutTypeName.constructFromObject(data.type) }
      if (data.hasOwnProperty('amount')) { obj.amount = Currency.constructFromObject(data.amount) }
      if (data.hasOwnProperty('accountTail')) { obj.accountTail = ApiClient.convertToType(data.accountTail, 'String') }
      if (data.hasOwnProperty('paymentMethod')) { obj.paymentMethod = PaymentMethodName.constructFromObject(data.paymentMethod) }
      if (data.hasOwnProperty('creationDate')) { obj.creationDate = ApiClient.convertToType(data.creationDate, 'Date') }
      if (data.hasOwnProperty('paymentRail')) { obj.paymentRail = ApiClient.convertToType(data.paymentRail, 'String') }
      if (data.hasOwnProperty('traceId')) { obj.traceId = ApiClient.convertToType(data.traceId, 'String') }
      if (data.hasOwnProperty('period')) { obj.period = Period.constructFromObject(data.period) }
      if (data.hasOwnProperty('relatedIdentifiers')) { obj.relatedIdentifiers = ApiClient.convertToType(data.relatedIdentifiers, [RelatedIdentifier]) }
    }
    return obj
  }
}

/**
 * The unique identifier of the payout.
 * @member {String} id
 * @type {String}
 */
Payout.prototype.id = undefined

/**
 * @member {PartnerMetadata} partnerMetadata
 * @type {PartnerMetadata}
 */
Payout.prototype.partnerMetadata = undefined

/**
 * @member {PayoutStatusName} currentStatus
 * @type {PayoutStatusName}
 */
Payout.prototype.currentStatus = undefined

/**
 * A chronological list of payout status updates.
 * @member {PayoutStatusEntry[]} statusHistory
 * @type {PayoutStatusEntry[]}
 */
Payout.prototype.statusHistory = undefined

/**
 * @member {PayoutTypeName} type
 * @type {PayoutTypeName}
 */
Payout.prototype.type = undefined

/**
 * @member {Currency} amount
 * @type {Currency}
 */
Payout.prototype.amount = undefined

/**
 * The last few digits of the payment instrument. May be omitted when the configured payment instrument is invalid.
 * @member {String} accountTail
 * @type {String}
 */
Payout.prototype.accountTail = undefined

/**
 * @member {PaymentMethodName} paymentMethod
 * @type {PaymentMethodName}
 */
Payout.prototype.paymentMethod = undefined

/**
 * A date in [ISO 8601](https://developer-docs.amazon.com/sp-api/docs/iso-8601) date-time format.
 * @member {Date} creationDate
 * @type {Date}
 */
Payout.prototype.creationDate = undefined

/**
 * The underlying payment network or clearing system used to transmit the payout.
 * @member {String} paymentRail
 * @type {String}
 */
Payout.prototype.paymentRail = undefined

/**
 * The banking network's unique identifier for a fund transfer request.
 * @member {String} traceId
 * @type {String}
 */
Payout.prototype.traceId = undefined

/**
 * @member {Period} period
 * @type {Period}
 */
Payout.prototype.period = undefined

/**
 * Related business identifiers of the payout.
 * @member {RelatedIdentifier[]} relatedIdentifiers
 * @type {RelatedIdentifier[]}
 */
Payout.prototype.relatedIdentifiers = undefined
