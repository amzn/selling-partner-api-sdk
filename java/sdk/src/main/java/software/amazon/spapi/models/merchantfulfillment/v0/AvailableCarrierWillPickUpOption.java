/*
 * Selling Partner API for Merchant Fulfillment
 * With the Selling Partner API for Merchant Fulfillment, you can build applications that sellers can use to purchase shipping for non-Prime and Prime orders using Amazon's Buy Shipping Services.
 *
 * The version of the OpenAPI document: v0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.merchantfulfillment.v0;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Indicates whether the carrier will pick up the package, and what fee is charged, if any. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class AvailableCarrierWillPickUpOption {
    public static final String SERIALIZED_NAME_CARRIER_WILL_PICK_UP_OPTION = "CarrierWillPickUpOption";

    @SerializedName(SERIALIZED_NAME_CARRIER_WILL_PICK_UP_OPTION)
    private CarrierWillPickUpOption carrierWillPickUpOption;

    public static final String SERIALIZED_NAME_CHARGE = "Charge";

    @SerializedName(SERIALIZED_NAME_CHARGE)
    private CurrencyAmount charge;

    public AvailableCarrierWillPickUpOption() {}

    public AvailableCarrierWillPickUpOption carrierWillPickUpOption(CarrierWillPickUpOption carrierWillPickUpOption) {
        this.carrierWillPickUpOption = carrierWillPickUpOption;
        return this;
    }

    /**
     * Get carrierWillPickUpOption
     *
     * @return carrierWillPickUpOption
     */
    @javax.annotation.Nonnull
    public CarrierWillPickUpOption getCarrierWillPickUpOption() {
        return carrierWillPickUpOption;
    }

    public void setCarrierWillPickUpOption(CarrierWillPickUpOption carrierWillPickUpOption) {
        this.carrierWillPickUpOption = carrierWillPickUpOption;
    }

    public AvailableCarrierWillPickUpOption charge(CurrencyAmount charge) {
        this.charge = charge;
        return this;
    }

    /**
     * Get charge
     *
     * @return charge
     */
    @javax.annotation.Nonnull
    public CurrencyAmount getCharge() {
        return charge;
    }

    public void setCharge(CurrencyAmount charge) {
        this.charge = charge;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AvailableCarrierWillPickUpOption availableCarrierWillPickUpOption = (AvailableCarrierWillPickUpOption) o;
        return Objects.equals(this.carrierWillPickUpOption, availableCarrierWillPickUpOption.carrierWillPickUpOption)
                && Objects.equals(this.charge, availableCarrierWillPickUpOption.charge);
    }

    @Override
    public int hashCode() {
        return Objects.hash(carrierWillPickUpOption, charge);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class AvailableCarrierWillPickUpOption {\n");
        sb.append("    carrierWillPickUpOption: ")
                .append(toIndentedString(carrierWillPickUpOption))
                .append("\n");
        sb.append("    charge: ").append(toIndentedString(charge)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("CarrierWillPickUpOption");
        openapiFields.add("Charge");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("CarrierWillPickUpOption");
        openapiRequiredFields.add("Charge");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to AvailableCarrierWillPickUpOption
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!AvailableCarrierWillPickUpOption.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in AvailableCarrierWillPickUpOption is not found in the empty JSON string",
                        AvailableCarrierWillPickUpOption.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!AvailableCarrierWillPickUpOption.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `AvailableCarrierWillPickUpOption` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : AvailableCarrierWillPickUpOption.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `CarrierWillPickUpOption`
        CarrierWillPickUpOption.validateJsonElement(jsonObj.get("CarrierWillPickUpOption"));
        // validate the required field `Charge`
        CurrencyAmount.validateJsonElement(jsonObj.get("Charge"));
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!AvailableCarrierWillPickUpOption.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'AvailableCarrierWillPickUpOption' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AvailableCarrierWillPickUpOption> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(AvailableCarrierWillPickUpOption.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<AvailableCarrierWillPickUpOption>() {
                        @Override
                        public void write(JsonWriter out, AvailableCarrierWillPickUpOption value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public AvailableCarrierWillPickUpOption read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of AvailableCarrierWillPickUpOption given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of AvailableCarrierWillPickUpOption
     * @throws IOException if the JSON string is invalid with respect to AvailableCarrierWillPickUpOption
     */
    public static AvailableCarrierWillPickUpOption fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, AvailableCarrierWillPickUpOption.class);
    }

    /**
     * Convert an instance of AvailableCarrierWillPickUpOption to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
