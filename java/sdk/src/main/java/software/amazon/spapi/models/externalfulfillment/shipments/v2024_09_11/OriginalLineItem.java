/*
 * The Selling Partner API for Amazon External Fulfillment Shipments Processing
 * You can use the External Fulfillment Shipments API to retrieve, manage, and track shipments processed through Amazon's external fulfillment network. Use this API to get shipment details, monitor status changes, and access fulfillment requirements.
 *
 * The version of the OpenAPI document: 2024-09-11
 * Contact: marketplaceapitest@amazon.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.externalfulfillment.shipments.v2024_09_11;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** A line item from the original shipment that corresponds to this shipment. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class OriginalLineItem {
    public static final String SERIALIZED_NAME_REPLACING_SKU = "replacingSku";

    @SerializedName(SERIALIZED_NAME_REPLACING_SKU)
    private String replacingSku;

    public static final String SERIALIZED_NAME_ORIGINAL_SKU = "originalSku";

    @SerializedName(SERIALIZED_NAME_ORIGINAL_SKU)
    private String originalSku;

    public static final String SERIALIZED_NAME_CHARGES = "charges";

    @SerializedName(SERIALIZED_NAME_CHARGES)
    private List<Charge> charges = new ArrayList<>();

    public OriginalLineItem() {}

    public OriginalLineItem replacingSku(String replacingSku) {
        this.replacingSku = replacingSku;
        return this;
    }

    /**
     * The SKU ID for this shipment
     *
     * @return replacingSku
     */
    @javax.annotation.Nonnull
    public String getReplacingSku() {
        return replacingSku;
    }

    public void setReplacingSku(String replacingSku) {
        this.replacingSku = replacingSku;
    }

    public OriginalLineItem originalSku(String originalSku) {
        this.originalSku = originalSku;
        return this;
    }

    /**
     * The SKU ID for the original shipment.
     *
     * @return originalSku
     */
    @javax.annotation.Nonnull
    public String getOriginalSku() {
        return originalSku;
    }

    public void setOriginalSku(String originalSku) {
        this.originalSku = originalSku;
    }

    public OriginalLineItem charges(List<Charge> charges) {
        this.charges = charges;
        return this;
    }

    public OriginalLineItem addChargesItem(Charge chargesItem) {
        if (this.charges == null) {
            this.charges = new ArrayList<>();
        }
        this.charges.add(chargesItem);
        return this;
    }

    /**
     * The charges associated with the shipment.
     *
     * @return charges
     */
    @javax.annotation.Nonnull
    public List<Charge> getCharges() {
        return charges;
    }

    public void setCharges(List<Charge> charges) {
        this.charges = charges;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OriginalLineItem originalLineItem = (OriginalLineItem) o;
        return Objects.equals(this.replacingSku, originalLineItem.replacingSku)
                && Objects.equals(this.originalSku, originalLineItem.originalSku)
                && Objects.equals(this.charges, originalLineItem.charges);
    }

    @Override
    public int hashCode() {
        return Objects.hash(replacingSku, originalSku, charges);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class OriginalLineItem {\n");
        sb.append("    replacingSku: ").append(toIndentedString(replacingSku)).append("\n");
        sb.append("    originalSku: ").append(toIndentedString(originalSku)).append("\n");
        sb.append("    charges: ").append(toIndentedString(charges)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("replacingSku");
        openapiFields.add("originalSku");
        openapiFields.add("charges");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("replacingSku");
        openapiRequiredFields.add("originalSku");
        openapiRequiredFields.add("charges");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to OriginalLineItem
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!OriginalLineItem.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in OriginalLineItem is not found in the empty JSON string",
                        OriginalLineItem.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!OriginalLineItem.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `OriginalLineItem` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : OriginalLineItem.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("replacingSku").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `replacingSku` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("replacingSku").toString()));
        }
        if (!jsonObj.get("originalSku").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `originalSku` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("originalSku").toString()));
        }
        // ensure the json data is an array
        if (!jsonObj.get("charges").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `charges` to be an array in the JSON string but got `%s`",
                    jsonObj.get("charges").toString()));
        }

        JsonArray jsonArraycharges = jsonObj.getAsJsonArray("charges");
        // validate the required field `charges` (array)
        for (int i = 0; i < jsonArraycharges.size(); i++) {
            Charge.validateJsonElement(jsonArraycharges.get(i));
        }
        ;
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!OriginalLineItem.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'OriginalLineItem' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<OriginalLineItem> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(OriginalLineItem.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<OriginalLineItem>() {
                        @Override
                        public void write(JsonWriter out, OriginalLineItem value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public OriginalLineItem read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of OriginalLineItem given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of OriginalLineItem
     * @throws IOException if the JSON string is invalid with respect to OriginalLineItem
     */
    public static OriginalLineItem fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, OriginalLineItem.class);
    }

    /**
     * Convert an instance of OriginalLineItem to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
