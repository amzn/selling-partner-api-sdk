/*
 * The Selling Partner API for Amazon External Fulfillment Return Item Processing
 * You can use the Amazon External Fulfillment Return Item Processing API to retrieve, track, and process return items through Amazon's External Fulfillment returns management system.
 *
 * The version of the OpenAPI document: 2024-09-11
 * Contact: marketplaceapitest@amazon.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.externalfulfillment.returns.v2024_09_11;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Provides details about the replanned returns. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class ReplanningDetails {
    public static final String SERIALIZED_NAME_PREVIOUS_TRACKING_INFO = "previousTrackingInfo";

    @SerializedName(SERIALIZED_NAME_PREVIOUS_TRACKING_INFO)
    private List<TrackingInfo> previousTrackingInfo = new ArrayList<>();

    public ReplanningDetails() {}

    public ReplanningDetails previousTrackingInfo(List<TrackingInfo> previousTrackingInfo) {
        this.previousTrackingInfo = previousTrackingInfo;
        return this;
    }

    public ReplanningDetails addPreviousTrackingInfoItem(TrackingInfo previousTrackingInfoItem) {
        if (this.previousTrackingInfo == null) {
            this.previousTrackingInfo = new ArrayList<>();
        }
        this.previousTrackingInfo.add(previousTrackingInfoItem);
        return this;
    }

    /**
     * A list of all the previous tracking information for the return.
     *
     * @return previousTrackingInfo
     */
    @javax.annotation.Nullable public List<TrackingInfo> getPreviousTrackingInfo() {
        return previousTrackingInfo;
    }

    public void setPreviousTrackingInfo(List<TrackingInfo> previousTrackingInfo) {
        this.previousTrackingInfo = previousTrackingInfo;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ReplanningDetails replanningDetails = (ReplanningDetails) o;
        return Objects.equals(this.previousTrackingInfo, replanningDetails.previousTrackingInfo);
    }

    @Override
    public int hashCode() {
        return Objects.hash(previousTrackingInfo);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ReplanningDetails {\n");
        sb.append("    previousTrackingInfo: ")
                .append(toIndentedString(previousTrackingInfo))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("previousTrackingInfo");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ReplanningDetails
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ReplanningDetails.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in ReplanningDetails is not found in the empty JSON string",
                        ReplanningDetails.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ReplanningDetails.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `ReplanningDetails` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("previousTrackingInfo") != null
                && !jsonObj.get("previousTrackingInfo").isJsonNull()) {
            JsonArray jsonArraypreviousTrackingInfo = jsonObj.getAsJsonArray("previousTrackingInfo");
            if (jsonArraypreviousTrackingInfo != null) {
                // ensure the json data is an array
                if (!jsonObj.get("previousTrackingInfo").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `previousTrackingInfo` to be an array in the JSON string but got `%s`",
                            jsonObj.get("previousTrackingInfo").toString()));
                }

                // validate the optional field `previousTrackingInfo` (array)
                for (int i = 0; i < jsonArraypreviousTrackingInfo.size(); i++) {
                    TrackingInfo.validateJsonElement(jsonArraypreviousTrackingInfo.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ReplanningDetails.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ReplanningDetails' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ReplanningDetails> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ReplanningDetails.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ReplanningDetails>() {
                        @Override
                        public void write(JsonWriter out, ReplanningDetails value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ReplanningDetails read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ReplanningDetails given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ReplanningDetails
     * @throws IOException if the JSON string is invalid with respect to ReplanningDetails
     */
    public static ReplanningDetails fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ReplanningDetails.class);
    }

    /**
     * Convert an instance of ReplanningDetails to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
