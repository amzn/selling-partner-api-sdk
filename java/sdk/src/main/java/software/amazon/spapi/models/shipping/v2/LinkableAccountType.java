/*
 * Amazon Shipping API
 * The Amazon Shipping API is designed to support outbound shipping use cases both for orders originating on Amazon-owned marketplaces as well as external channels/marketplaces. With these APIs, you can request shipping rates, create shipments, cancel shipments, and track shipments.
 *
 * The version of the OpenAPI document: v2
 * Contact: swa-api-core@amazon.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.shipping.v2;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Info About Linkable Account Type */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class LinkableAccountType {
    public static final String SERIALIZED_NAME_ACCOUNT_TYPE = "accountType";

    @SerializedName(SERIALIZED_NAME_ACCOUNT_TYPE)
    private AccountType accountType;

    public static final String SERIALIZED_NAME_CARRIER_ACCOUNT_INPUTS = "carrierAccountInputs";

    @SerializedName(SERIALIZED_NAME_CARRIER_ACCOUNT_INPUTS)
    private List<CarrierAccountInput> carrierAccountInputs = new ArrayList<>();

    public LinkableAccountType() {}

    public LinkableAccountType accountType(AccountType accountType) {
        this.accountType = accountType;
        return this;
    }

    /**
     * Get accountType
     *
     * @return accountType
     */
    @javax.annotation.Nullable public AccountType getAccountType() {
        return accountType;
    }

    public void setAccountType(AccountType accountType) {
        this.accountType = accountType;
    }

    public LinkableAccountType carrierAccountInputs(List<CarrierAccountInput> carrierAccountInputs) {
        this.carrierAccountInputs = carrierAccountInputs;
        return this;
    }

    public LinkableAccountType addCarrierAccountInputsItem(CarrierAccountInput carrierAccountInputsItem) {
        if (this.carrierAccountInputs == null) {
            this.carrierAccountInputs = new ArrayList<>();
        }
        this.carrierAccountInputs.add(carrierAccountInputsItem);
        return this;
    }

    /**
     * A list of CarrierAccountInput
     *
     * @return carrierAccountInputs
     */
    @javax.annotation.Nullable public List<CarrierAccountInput> getCarrierAccountInputs() {
        return carrierAccountInputs;
    }

    public void setCarrierAccountInputs(List<CarrierAccountInput> carrierAccountInputs) {
        this.carrierAccountInputs = carrierAccountInputs;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        LinkableAccountType linkableAccountType = (LinkableAccountType) o;
        return Objects.equals(this.accountType, linkableAccountType.accountType)
                && Objects.equals(this.carrierAccountInputs, linkableAccountType.carrierAccountInputs);
    }

    @Override
    public int hashCode() {
        return Objects.hash(accountType, carrierAccountInputs);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class LinkableAccountType {\n");
        sb.append("    accountType: ").append(toIndentedString(accountType)).append("\n");
        sb.append("    carrierAccountInputs: ")
                .append(toIndentedString(carrierAccountInputs))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("accountType");
        openapiFields.add("carrierAccountInputs");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to LinkableAccountType
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!LinkableAccountType.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in LinkableAccountType is not found in the empty JSON string",
                        LinkableAccountType.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!LinkableAccountType.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `LinkableAccountType` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the optional field `accountType`
        if (jsonObj.get("accountType") != null && !jsonObj.get("accountType").isJsonNull()) {
            AccountType.validateJsonElement(jsonObj.get("accountType"));
        }
        if (jsonObj.get("carrierAccountInputs") != null
                && !jsonObj.get("carrierAccountInputs").isJsonNull()) {
            JsonArray jsonArraycarrierAccountInputs = jsonObj.getAsJsonArray("carrierAccountInputs");
            if (jsonArraycarrierAccountInputs != null) {
                // ensure the json data is an array
                if (!jsonObj.get("carrierAccountInputs").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `carrierAccountInputs` to be an array in the JSON string but got `%s`",
                            jsonObj.get("carrierAccountInputs").toString()));
                }

                // validate the optional field `carrierAccountInputs` (array)
                for (int i = 0; i < jsonArraycarrierAccountInputs.size(); i++) {
                    CarrierAccountInput.validateJsonElement(jsonArraycarrierAccountInputs.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!LinkableAccountType.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'LinkableAccountType' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<LinkableAccountType> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(LinkableAccountType.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<LinkableAccountType>() {
                        @Override
                        public void write(JsonWriter out, LinkableAccountType value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public LinkableAccountType read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of LinkableAccountType given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of LinkableAccountType
     * @throws IOException if the JSON string is invalid with respect to LinkableAccountType
     */
    public static LinkableAccountType fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, LinkableAccountType.class);
    }

    /**
     * Convert an instance of LinkableAccountType to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
