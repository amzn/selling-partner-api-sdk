/*
 * The Selling Partner API for FBA inbound operations.
 * The Selling Partner API for Fulfillment By Amazon (FBA) Inbound. The FBA Inbound API enables building inbound workflows to create, manage, and send shipments into Amazon's fulfillment network. The API has interoperability with the Send-to-Amazon user interface.
 *
 * The version of the OpenAPI document: 2024-03-20
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.inbound.v2024_03_20;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The response to the &#x60;listPrepDetails&#x60; operation. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class ListPrepDetailsResponse {
    public static final String SERIALIZED_NAME_MSKU_PREP_DETAILS = "mskuPrepDetails";

    @SerializedName(SERIALIZED_NAME_MSKU_PREP_DETAILS)
    private List<MskuPrepDetail> mskuPrepDetails = new ArrayList<>();

    public ListPrepDetailsResponse() {}

    public ListPrepDetailsResponse mskuPrepDetails(List<MskuPrepDetail> mskuPrepDetails) {
        this.mskuPrepDetails = mskuPrepDetails;
        return this;
    }

    public ListPrepDetailsResponse addMskuPrepDetailsItem(MskuPrepDetail mskuPrepDetailsItem) {
        if (this.mskuPrepDetails == null) {
            this.mskuPrepDetails = new ArrayList<>();
        }
        this.mskuPrepDetails.add(mskuPrepDetailsItem);
        return this;
    }

    /**
     * A list of MSKUs and related prep details.
     *
     * @return mskuPrepDetails
     */
    @javax.annotation.Nonnull
    public List<MskuPrepDetail> getMskuPrepDetails() {
        return mskuPrepDetails;
    }

    public void setMskuPrepDetails(List<MskuPrepDetail> mskuPrepDetails) {
        this.mskuPrepDetails = mskuPrepDetails;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ListPrepDetailsResponse listPrepDetailsResponse = (ListPrepDetailsResponse) o;
        return Objects.equals(this.mskuPrepDetails, listPrepDetailsResponse.mskuPrepDetails);
    }

    @Override
    public int hashCode() {
        return Objects.hash(mskuPrepDetails);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ListPrepDetailsResponse {\n");
        sb.append("    mskuPrepDetails: ")
                .append(toIndentedString(mskuPrepDetails))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("mskuPrepDetails");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("mskuPrepDetails");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ListPrepDetailsResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ListPrepDetailsResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in ListPrepDetailsResponse is not found in the empty JSON string",
                        ListPrepDetailsResponse.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ListPrepDetailsResponse.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `ListPrepDetailsResponse` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ListPrepDetailsResponse.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the json data is an array
        if (!jsonObj.get("mskuPrepDetails").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `mskuPrepDetails` to be an array in the JSON string but got `%s`",
                    jsonObj.get("mskuPrepDetails").toString()));
        }

        JsonArray jsonArraymskuPrepDetails = jsonObj.getAsJsonArray("mskuPrepDetails");
        // validate the required field `mskuPrepDetails` (array)
        for (int i = 0; i < jsonArraymskuPrepDetails.size(); i++) {
            MskuPrepDetail.validateJsonElement(jsonArraymskuPrepDetails.get(i));
        }
        ;
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ListPrepDetailsResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ListPrepDetailsResponse' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ListPrepDetailsResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ListPrepDetailsResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ListPrepDetailsResponse>() {
                        @Override
                        public void write(JsonWriter out, ListPrepDetailsResponse value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ListPrepDetailsResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ListPrepDetailsResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ListPrepDetailsResponse
     * @throws IOException if the JSON string is invalid with respect to ListPrepDetailsResponse
     */
    public static ListPrepDetailsResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ListPrepDetailsResponse.class);
    }

    /**
     * Convert an instance of ListPrepDetailsResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
