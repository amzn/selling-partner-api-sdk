/*
 * The Selling Partner API for Fulfillment Outbound
 * The Selling Partner API for Fulfillment Outbound lets you create applications that help a seller fulfill Multi-Channel Fulfillment orders using their inventory in Amazon's fulfillment network. You can get information on both potential and existing fulfillment orders.
 *
 * The version of the OpenAPI document: 2026-07-04
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.outbound.v2026_07_04;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Specify packaging requirements. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class PackagingService {
    public static final String SERIALIZED_NAME_PACKAGING_OPTION = "packagingOption";

    @SerializedName(SERIALIZED_NAME_PACKAGING_OPTION)
    private String packagingOption;

    public static final String SERIALIZED_NAME_OVERBOX = "overbox";

    @SerializedName(SERIALIZED_NAME_OVERBOX)
    private String overbox;

    public static final String SERIALIZED_NAME_PACKING_SLIP = "packingSlip";

    @SerializedName(SERIALIZED_NAME_PACKING_SLIP)
    private OrderPackingSlip packingSlip;

    public PackagingService() {}

    public PackagingService packagingOption(String packagingOption) {
        this.packagingOption = packagingOption;
        return this;
    }

    /**
     * The packaging option to use for fulfillment. Possible values: &#x60;UNBRANDED&#x60;, &#x60;ANY&#x60;.
     *
     * @return packagingOption
     */
    @javax.annotation.Nullable public String getPackagingOption() {
        return packagingOption;
    }

    public void setPackagingOption(String packagingOption) {
        this.packagingOption = packagingOption;
    }

    public PackagingService overbox(String overbox) {
        this.overbox = overbox;
        return this;
    }

    /**
     * Whether to require an overbox for the shipment. Possible values: &#x60;REQUIRED&#x60;, &#x60;NOT_REQUIRED&#x60;.
     *
     * @return overbox
     */
    @javax.annotation.Nullable public String getOverbox() {
        return overbox;
    }

    public void setOverbox(String overbox) {
        this.overbox = overbox;
    }

    public PackagingService packingSlip(OrderPackingSlip packingSlip) {
        this.packingSlip = packingSlip;
        return this;
    }

    /**
     * Get packingSlip
     *
     * @return packingSlip
     */
    @javax.annotation.Nullable public OrderPackingSlip getPackingSlip() {
        return packingSlip;
    }

    public void setPackingSlip(OrderPackingSlip packingSlip) {
        this.packingSlip = packingSlip;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PackagingService packagingService = (PackagingService) o;
        return Objects.equals(this.packagingOption, packagingService.packagingOption)
                && Objects.equals(this.overbox, packagingService.overbox)
                && Objects.equals(this.packingSlip, packagingService.packingSlip);
    }

    @Override
    public int hashCode() {
        return Objects.hash(packagingOption, overbox, packingSlip);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class PackagingService {\n");
        sb.append("    packagingOption: ")
                .append(toIndentedString(packagingOption))
                .append("\n");
        sb.append("    overbox: ").append(toIndentedString(overbox)).append("\n");
        sb.append("    packingSlip: ").append(toIndentedString(packingSlip)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("packagingOption");
        openapiFields.add("overbox");
        openapiFields.add("packingSlip");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to PackagingService
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!PackagingService.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in PackagingService is not found in the empty JSON string",
                        PackagingService.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!PackagingService.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `PackagingService` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("packagingOption") != null
                        && !jsonObj.get("packagingOption").isJsonNull())
                && !jsonObj.get("packagingOption").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `packagingOption` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("packagingOption").toString()));
        }
        if ((jsonObj.get("overbox") != null && !jsonObj.get("overbox").isJsonNull())
                && !jsonObj.get("overbox").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `overbox` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("overbox").toString()));
        }
        // validate the optional field `packingSlip`
        if (jsonObj.get("packingSlip") != null && !jsonObj.get("packingSlip").isJsonNull()) {
            OrderPackingSlip.validateJsonElement(jsonObj.get("packingSlip"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!PackagingService.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'PackagingService' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<PackagingService> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(PackagingService.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<PackagingService>() {
                        @Override
                        public void write(JsonWriter out, PackagingService value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public PackagingService read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of PackagingService given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of PackagingService
     * @throws IOException if the JSON string is invalid with respect to PackagingService
     */
    public static PackagingService fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, PackagingService.class);
    }

    /**
     * Convert an instance of PackagingService to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
