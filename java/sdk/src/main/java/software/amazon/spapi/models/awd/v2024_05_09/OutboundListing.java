/*
 * The Selling Partner API for Amazon Warehousing and Distribution
 * The Selling Partner API for Amazon Warehousing and Distribution (AWD) provides programmatic access to information about AWD shipments and inventory.
 *
 * The version of the OpenAPI document: 2024-05-09
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.awd.v2024_05_09;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** A list of paginated outbound orders filtered by the attributes passed in the request. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class OutboundListing {
    public static final String SERIALIZED_NAME_NEXT_TOKEN = "nextToken";

    @SerializedName(SERIALIZED_NAME_NEXT_TOKEN)
    private String nextToken;

    public static final String SERIALIZED_NAME_OUTBOUND_ORDERS = "outboundOrders";

    @SerializedName(SERIALIZED_NAME_OUTBOUND_ORDERS)
    private List<OutboundOrder> outboundOrders = new ArrayList<>();

    public OutboundListing() {}

    public OutboundListing nextToken(String nextToken) {
        this.nextToken = nextToken;
        return this;
    }

    /**
     * TA token that is used to retrieve the next page of results. The response includes &#x60;nextToken&#x60; when the
     * number of results exceeds the specified &#x60;maxResults&#x60; value. To get the next page of results, call the
     * operation with this token and include the same arguments as the call that produced the token. To get a complete
     * list, call this operation until &#x60;nextToken&#x60; is null. Note that this operation can return empty pages.
     *
     * @return nextToken
     */
    @javax.annotation.Nullable public String getNextToken() {
        return nextToken;
    }

    public void setNextToken(String nextToken) {
        this.nextToken = nextToken;
    }

    public OutboundListing outboundOrders(List<OutboundOrder> outboundOrders) {
        this.outboundOrders = outboundOrders;
        return this;
    }

    public OutboundListing addOutboundOrdersItem(OutboundOrder outboundOrdersItem) {
        if (this.outboundOrders == null) {
            this.outboundOrders = new ArrayList<>();
        }
        this.outboundOrders.add(outboundOrdersItem);
        return this;
    }

    /**
     * List of outbound orders.
     *
     * @return outboundOrders
     */
    @javax.annotation.Nullable public List<OutboundOrder> getOutboundOrders() {
        return outboundOrders;
    }

    public void setOutboundOrders(List<OutboundOrder> outboundOrders) {
        this.outboundOrders = outboundOrders;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OutboundListing outboundListing = (OutboundListing) o;
        return Objects.equals(this.nextToken, outboundListing.nextToken)
                && Objects.equals(this.outboundOrders, outboundListing.outboundOrders);
    }

    @Override
    public int hashCode() {
        return Objects.hash(nextToken, outboundOrders);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class OutboundListing {\n");
        sb.append("    nextToken: ").append(toIndentedString(nextToken)).append("\n");
        sb.append("    outboundOrders: ")
                .append(toIndentedString(outboundOrders))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("nextToken");
        openapiFields.add("outboundOrders");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to OutboundListing
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!OutboundListing.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in OutboundListing is not found in the empty JSON string",
                        OutboundListing.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!OutboundListing.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `OutboundListing` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("nextToken") != null && !jsonObj.get("nextToken").isJsonNull())
                && !jsonObj.get("nextToken").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `nextToken` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("nextToken").toString()));
        }
        if (jsonObj.get("outboundOrders") != null
                && !jsonObj.get("outboundOrders").isJsonNull()) {
            JsonArray jsonArrayoutboundOrders = jsonObj.getAsJsonArray("outboundOrders");
            if (jsonArrayoutboundOrders != null) {
                // ensure the json data is an array
                if (!jsonObj.get("outboundOrders").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `outboundOrders` to be an array in the JSON string but got `%s`",
                            jsonObj.get("outboundOrders").toString()));
                }

                // validate the optional field `outboundOrders` (array)
                for (int i = 0; i < jsonArrayoutboundOrders.size(); i++) {
                    OutboundOrder.validateJsonElement(jsonArrayoutboundOrders.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!OutboundListing.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'OutboundListing' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<OutboundListing> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(OutboundListing.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<OutboundListing>() {
                        @Override
                        public void write(JsonWriter out, OutboundListing value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public OutboundListing read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of OutboundListing given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of OutboundListing
     * @throws IOException if the JSON string is invalid with respect to OutboundListing
     */
    public static OutboundListing fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, OutboundListing.class);
    }

    /**
     * Convert an instance of OutboundListing to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
