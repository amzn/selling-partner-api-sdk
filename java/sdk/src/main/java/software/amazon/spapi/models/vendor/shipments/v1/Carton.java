/*
 * Selling Partner API for Retail Procurement Shipments
 * The Selling Partner API for Retail Procurement Shipments provides programmatic access to retail shipping data for vendors.
 *
 * The version of the OpenAPI document: v1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.vendor.shipments.v1;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Details of the carton/package being shipped. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class Carton {
    public static final String SERIALIZED_NAME_CARTON_IDENTIFIERS = "cartonIdentifiers";

    @SerializedName(SERIALIZED_NAME_CARTON_IDENTIFIERS)
    private List<ContainerIdentification> cartonIdentifiers = new ArrayList<>();

    public static final String SERIALIZED_NAME_CARTON_SEQUENCE_NUMBER = "cartonSequenceNumber";

    @SerializedName(SERIALIZED_NAME_CARTON_SEQUENCE_NUMBER)
    private String cartonSequenceNumber;

    public static final String SERIALIZED_NAME_DIMENSIONS = "dimensions";

    @SerializedName(SERIALIZED_NAME_DIMENSIONS)
    private Dimensions dimensions;

    public static final String SERIALIZED_NAME_WEIGHT = "weight";

    @SerializedName(SERIALIZED_NAME_WEIGHT)
    private Weight weight;

    public static final String SERIALIZED_NAME_TRACKING_NUMBER = "trackingNumber";

    @SerializedName(SERIALIZED_NAME_TRACKING_NUMBER)
    private String trackingNumber;

    public static final String SERIALIZED_NAME_ITEMS = "items";

    @SerializedName(SERIALIZED_NAME_ITEMS)
    private List<ContainerItem> items = new ArrayList<>();

    public Carton() {}

    public Carton cartonIdentifiers(List<ContainerIdentification> cartonIdentifiers) {
        this.cartonIdentifiers = cartonIdentifiers;
        return this;
    }

    public Carton addCartonIdentifiersItem(ContainerIdentification cartonIdentifiersItem) {
        if (this.cartonIdentifiers == null) {
            this.cartonIdentifiers = new ArrayList<>();
        }
        this.cartonIdentifiers.add(cartonIdentifiersItem);
        return this;
    }

    /**
     * A list of carton identifiers.
     *
     * @return cartonIdentifiers
     */
    @javax.annotation.Nullable public List<ContainerIdentification> getCartonIdentifiers() {
        return cartonIdentifiers;
    }

    public void setCartonIdentifiers(List<ContainerIdentification> cartonIdentifiers) {
        this.cartonIdentifiers = cartonIdentifiers;
    }

    public Carton cartonSequenceNumber(String cartonSequenceNumber) {
        this.cartonSequenceNumber = cartonSequenceNumber;
        return this;
    }

    /**
     * Carton sequence number for the carton. The first carton will be 001, the second 002, and so on. This number is
     * used as a reference to refer to this carton from the pallet level.
     *
     * @return cartonSequenceNumber
     */
    @javax.annotation.Nonnull
    public String getCartonSequenceNumber() {
        return cartonSequenceNumber;
    }

    public void setCartonSequenceNumber(String cartonSequenceNumber) {
        this.cartonSequenceNumber = cartonSequenceNumber;
    }

    public Carton dimensions(Dimensions dimensions) {
        this.dimensions = dimensions;
        return this;
    }

    /**
     * Get dimensions
     *
     * @return dimensions
     */
    @javax.annotation.Nullable public Dimensions getDimensions() {
        return dimensions;
    }

    public void setDimensions(Dimensions dimensions) {
        this.dimensions = dimensions;
    }

    public Carton weight(Weight weight) {
        this.weight = weight;
        return this;
    }

    /**
     * Get weight
     *
     * @return weight
     */
    @javax.annotation.Nullable public Weight getWeight() {
        return weight;
    }

    public void setWeight(Weight weight) {
        this.weight = weight;
    }

    public Carton trackingNumber(String trackingNumber) {
        this.trackingNumber = trackingNumber;
        return this;
    }

    /**
     * This is required to be provided for every carton in the small parcel shipments.
     *
     * @return trackingNumber
     */
    @javax.annotation.Nullable public String getTrackingNumber() {
        return trackingNumber;
    }

    public void setTrackingNumber(String trackingNumber) {
        this.trackingNumber = trackingNumber;
    }

    public Carton items(List<ContainerItem> items) {
        this.items = items;
        return this;
    }

    public Carton addItemsItem(ContainerItem itemsItem) {
        if (this.items == null) {
            this.items = new ArrayList<>();
        }
        this.items.add(itemsItem);
        return this;
    }

    /**
     * A list of container item details.
     *
     * @return items
     */
    @javax.annotation.Nonnull
    public List<ContainerItem> getItems() {
        return items;
    }

    public void setItems(List<ContainerItem> items) {
        this.items = items;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        Carton carton = (Carton) o;
        return Objects.equals(this.cartonIdentifiers, carton.cartonIdentifiers)
                && Objects.equals(this.cartonSequenceNumber, carton.cartonSequenceNumber)
                && Objects.equals(this.dimensions, carton.dimensions)
                && Objects.equals(this.weight, carton.weight)
                && Objects.equals(this.trackingNumber, carton.trackingNumber)
                && Objects.equals(this.items, carton.items);
    }

    @Override
    public int hashCode() {
        return Objects.hash(cartonIdentifiers, cartonSequenceNumber, dimensions, weight, trackingNumber, items);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class Carton {\n");
        sb.append("    cartonIdentifiers: ")
                .append(toIndentedString(cartonIdentifiers))
                .append("\n");
        sb.append("    cartonSequenceNumber: ")
                .append(toIndentedString(cartonSequenceNumber))
                .append("\n");
        sb.append("    dimensions: ").append(toIndentedString(dimensions)).append("\n");
        sb.append("    weight: ").append(toIndentedString(weight)).append("\n");
        sb.append("    trackingNumber: ")
                .append(toIndentedString(trackingNumber))
                .append("\n");
        sb.append("    items: ").append(toIndentedString(items)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("cartonIdentifiers");
        openapiFields.add("cartonSequenceNumber");
        openapiFields.add("dimensions");
        openapiFields.add("weight");
        openapiFields.add("trackingNumber");
        openapiFields.add("items");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("cartonSequenceNumber");
        openapiRequiredFields.add("items");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to Carton
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!Carton.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in Carton is not found in the empty JSON string",
                        Carton.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!Carton.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `Carton` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : Carton.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("cartonIdentifiers") != null
                && !jsonObj.get("cartonIdentifiers").isJsonNull()) {
            JsonArray jsonArraycartonIdentifiers = jsonObj.getAsJsonArray("cartonIdentifiers");
            if (jsonArraycartonIdentifiers != null) {
                // ensure the json data is an array
                if (!jsonObj.get("cartonIdentifiers").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `cartonIdentifiers` to be an array in the JSON string but got `%s`",
                            jsonObj.get("cartonIdentifiers").toString()));
                }

                // validate the optional field `cartonIdentifiers` (array)
                for (int i = 0; i < jsonArraycartonIdentifiers.size(); i++) {
                    ContainerIdentification.validateJsonElement(jsonArraycartonIdentifiers.get(i));
                }
                ;
            }
        }
        if (!jsonObj.get("cartonSequenceNumber").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `cartonSequenceNumber` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("cartonSequenceNumber").toString()));
        }
        // validate the optional field `dimensions`
        if (jsonObj.get("dimensions") != null && !jsonObj.get("dimensions").isJsonNull()) {
            Dimensions.validateJsonElement(jsonObj.get("dimensions"));
        }
        // validate the optional field `weight`
        if (jsonObj.get("weight") != null && !jsonObj.get("weight").isJsonNull()) {
            Weight.validateJsonElement(jsonObj.get("weight"));
        }
        if ((jsonObj.get("trackingNumber") != null
                        && !jsonObj.get("trackingNumber").isJsonNull())
                && !jsonObj.get("trackingNumber").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `trackingNumber` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("trackingNumber").toString()));
        }
        // ensure the json data is an array
        if (!jsonObj.get("items").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `items` to be an array in the JSON string but got `%s`",
                    jsonObj.get("items").toString()));
        }

        JsonArray jsonArrayitems = jsonObj.getAsJsonArray("items");
        // validate the required field `items` (array)
        for (int i = 0; i < jsonArrayitems.size(); i++) {
            ContainerItem.validateJsonElement(jsonArrayitems.get(i));
        }
        ;
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!Carton.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'Carton' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<Carton> thisAdapter = gson.getDelegateAdapter(this, TypeToken.get(Carton.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<Carton>() {
                        @Override
                        public void write(JsonWriter out, Carton value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public Carton read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of Carton given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of Carton
     * @throws IOException if the JSON string is invalid with respect to Carton
     */
    public static Carton fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, Carton.class);
    }

    /**
     * Convert an instance of Carton to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
