/*
 * The Selling Partner API for Amazon Seller Wallet Open Banking API
 * The Selling Partner API for Seller Wallet (Seller Wallet API) provides financial information that is relevant to a seller's Seller Wallet account. You can obtain financial events, balances, and transfer schedules for Seller Wallet accounts. You can also schedule and initiate transactions.
 *
 * The version of the OpenAPI document: 2024-03-01
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.sellerwallet.v2024_03_01;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** A list of transfer schedules. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class TransferScheduleListing {
    public static final String SERIALIZED_NAME_NEXT_PAGE_TOKEN = "nextPageToken";

    @SerializedName(SERIALIZED_NAME_NEXT_PAGE_TOKEN)
    private String nextPageToken;

    public static final String SERIALIZED_NAME_TRANSFER_SCHEDULES = "transferSchedules";

    @SerializedName(SERIALIZED_NAME_TRANSFER_SCHEDULES)
    private List<TransferSchedule> transferSchedules = new ArrayList<>();

    public TransferScheduleListing() {}

    public TransferScheduleListing nextPageToken(String nextPageToken) {
        this.nextPageToken = nextPageToken;
        return this;
    }

    /**
     * A token that you use to retrieve the next page of results. The response includes &#x60;nextPageToken&#x60; when
     * the number of results exceeds 100. To get the next page of results, call the operation with this token and
     * include the same arguments as the call that produced the token. To get a complete list, call this operation until
     * &#x60;nextPageToken&#x60; is null. Note that this operation can return empty pages.
     *
     * @return nextPageToken
     */
    @javax.annotation.Nullable public String getNextPageToken() {
        return nextPageToken;
    }

    public void setNextPageToken(String nextPageToken) {
        this.nextPageToken = nextPageToken;
    }

    public TransferScheduleListing transferSchedules(List<TransferSchedule> transferSchedules) {
        this.transferSchedules = transferSchedules;
        return this;
    }

    public TransferScheduleListing addTransferSchedulesItem(TransferSchedule transferSchedulesItem) {
        if (this.transferSchedules == null) {
            this.transferSchedules = new ArrayList<>();
        }
        this.transferSchedules.add(transferSchedulesItem);
        return this;
    }

    /**
     * A list of transfer schedules.
     *
     * @return transferSchedules
     */
    @javax.annotation.Nonnull
    public List<TransferSchedule> getTransferSchedules() {
        return transferSchedules;
    }

    public void setTransferSchedules(List<TransferSchedule> transferSchedules) {
        this.transferSchedules = transferSchedules;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        TransferScheduleListing transferScheduleListing = (TransferScheduleListing) o;
        return Objects.equals(this.nextPageToken, transferScheduleListing.nextPageToken)
                && Objects.equals(this.transferSchedules, transferScheduleListing.transferSchedules);
    }

    @Override
    public int hashCode() {
        return Objects.hash(nextPageToken, transferSchedules);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class TransferScheduleListing {\n");
        sb.append("    nextPageToken: ").append(toIndentedString(nextPageToken)).append("\n");
        sb.append("    transferSchedules: ")
                .append(toIndentedString(transferSchedules))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("nextPageToken");
        openapiFields.add("transferSchedules");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("transferSchedules");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to TransferScheduleListing
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!TransferScheduleListing.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in TransferScheduleListing is not found in the empty JSON string",
                        TransferScheduleListing.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!TransferScheduleListing.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `TransferScheduleListing` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : TransferScheduleListing.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("nextPageToken") != null
                        && !jsonObj.get("nextPageToken").isJsonNull())
                && !jsonObj.get("nextPageToken").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `nextPageToken` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("nextPageToken").toString()));
        }
        // ensure the json data is an array
        if (!jsonObj.get("transferSchedules").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `transferSchedules` to be an array in the JSON string but got `%s`",
                    jsonObj.get("transferSchedules").toString()));
        }

        JsonArray jsonArraytransferSchedules = jsonObj.getAsJsonArray("transferSchedules");
        // validate the required field `transferSchedules` (array)
        for (int i = 0; i < jsonArraytransferSchedules.size(); i++) {
            TransferSchedule.validateJsonElement(jsonArraytransferSchedules.get(i));
        }
        ;
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!TransferScheduleListing.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'TransferScheduleListing' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<TransferScheduleListing> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(TransferScheduleListing.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<TransferScheduleListing>() {
                        @Override
                        public void write(JsonWriter out, TransferScheduleListing value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public TransferScheduleListing read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of TransferScheduleListing given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of TransferScheduleListing
     * @throws IOException if the JSON string is invalid with respect to TransferScheduleListing
     */
    public static TransferScheduleListing fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, TransferScheduleListing.class);
    }

    /**
     * Convert an instance of TransferScheduleListing to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
