/*
 * The Selling Partner API for FBA inbound operations.
 * The Selling Partner API for Fulfillment By Amazon (FBA) Inbound. The FBA Inbound API enables building inbound workflows to create, manage, and send shipments into Amazon's fulfillment network. The API has interoperability with the Send-to-Amazon user interface.
 *
 * The version of the OpenAPI document: 2024-03-20
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.inbound.v2024_03_20;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The &#x60;generateTransportationOptions&#x60; request. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class GenerateTransportationOptionsRequest {
    public static final String SERIALIZED_NAME_PLACEMENT_OPTION_ID = "placementOptionId";

    @SerializedName(SERIALIZED_NAME_PLACEMENT_OPTION_ID)
    private String placementOptionId;

    public static final String SERIALIZED_NAME_SHIPMENT_TRANSPORTATION_CONFIGURATIONS =
            "shipmentTransportationConfigurations";

    @SerializedName(SERIALIZED_NAME_SHIPMENT_TRANSPORTATION_CONFIGURATIONS)
    private List<ShipmentTransportationConfiguration> shipmentTransportationConfigurations = new ArrayList<>();

    public GenerateTransportationOptionsRequest() {}

    public GenerateTransportationOptionsRequest placementOptionId(String placementOptionId) {
        this.placementOptionId = placementOptionId;
        return this;
    }

    /**
     * The placement option to generate transportation options for.
     *
     * @return placementOptionId
     */
    @javax.annotation.Nonnull
    public String getPlacementOptionId() {
        return placementOptionId;
    }

    public void setPlacementOptionId(String placementOptionId) {
        this.placementOptionId = placementOptionId;
    }

    public GenerateTransportationOptionsRequest shipmentTransportationConfigurations(
            List<ShipmentTransportationConfiguration> shipmentTransportationConfigurations) {
        this.shipmentTransportationConfigurations = shipmentTransportationConfigurations;
        return this;
    }

    public GenerateTransportationOptionsRequest addShipmentTransportationConfigurationsItem(
            ShipmentTransportationConfiguration shipmentTransportationConfigurationsItem) {
        if (this.shipmentTransportationConfigurations == null) {
            this.shipmentTransportationConfigurations = new ArrayList<>();
        }
        this.shipmentTransportationConfigurations.add(shipmentTransportationConfigurationsItem);
        return this;
    }

    /**
     * List of shipment transportation configurations.
     *
     * @return shipmentTransportationConfigurations
     */
    @javax.annotation.Nonnull
    public List<ShipmentTransportationConfiguration> getShipmentTransportationConfigurations() {
        return shipmentTransportationConfigurations;
    }

    public void setShipmentTransportationConfigurations(
            List<ShipmentTransportationConfiguration> shipmentTransportationConfigurations) {
        this.shipmentTransportationConfigurations = shipmentTransportationConfigurations;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GenerateTransportationOptionsRequest generateTransportationOptionsRequest =
                (GenerateTransportationOptionsRequest) o;
        return Objects.equals(this.placementOptionId, generateTransportationOptionsRequest.placementOptionId)
                && Objects.equals(
                        this.shipmentTransportationConfigurations,
                        generateTransportationOptionsRequest.shipmentTransportationConfigurations);
    }

    @Override
    public int hashCode() {
        return Objects.hash(placementOptionId, shipmentTransportationConfigurations);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class GenerateTransportationOptionsRequest {\n");
        sb.append("    placementOptionId: ")
                .append(toIndentedString(placementOptionId))
                .append("\n");
        sb.append("    shipmentTransportationConfigurations: ")
                .append(toIndentedString(shipmentTransportationConfigurations))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("placementOptionId");
        openapiFields.add("shipmentTransportationConfigurations");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("placementOptionId");
        openapiRequiredFields.add("shipmentTransportationConfigurations");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GenerateTransportationOptionsRequest
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!GenerateTransportationOptionsRequest.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in GenerateTransportationOptionsRequest is not found in the empty JSON string",
                        GenerateTransportationOptionsRequest.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!GenerateTransportationOptionsRequest.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `GenerateTransportationOptionsRequest` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : GenerateTransportationOptionsRequest.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("placementOptionId").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `placementOptionId` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("placementOptionId").toString()));
        }
        // ensure the json data is an array
        if (!jsonObj.get("shipmentTransportationConfigurations").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `shipmentTransportationConfigurations` to be an array in the JSON string but got `%s`",
                    jsonObj.get("shipmentTransportationConfigurations").toString()));
        }

        JsonArray jsonArrayshipmentTransportationConfigurations =
                jsonObj.getAsJsonArray("shipmentTransportationConfigurations");
        // validate the required field `shipmentTransportationConfigurations` (array)
        for (int i = 0; i < jsonArrayshipmentTransportationConfigurations.size(); i++) {
            ShipmentTransportationConfiguration.validateJsonElement(
                    jsonArrayshipmentTransportationConfigurations.get(i));
        }
        ;
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GenerateTransportationOptionsRequest.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GenerateTransportationOptionsRequest' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GenerateTransportationOptionsRequest> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(GenerateTransportationOptionsRequest.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<GenerateTransportationOptionsRequest>() {
                        @Override
                        public void write(JsonWriter out, GenerateTransportationOptionsRequest value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public GenerateTransportationOptionsRequest read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of GenerateTransportationOptionsRequest given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GenerateTransportationOptionsRequest
     * @throws IOException if the JSON string is invalid with respect to GenerateTransportationOptionsRequest
     */
    public static GenerateTransportationOptionsRequest fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GenerateTransportationOptionsRequest.class);
    }

    /**
     * Convert an instance of GenerateTransportationOptionsRequest to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
