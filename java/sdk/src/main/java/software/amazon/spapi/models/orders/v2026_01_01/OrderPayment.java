/*
 * The Selling Partner API for Orders
 * The Selling Partner API for Orders returns orders information. This API supports the following types of orders:  - FBM (Fulfilled by Merchant) - FBA (Fulfilled by Amazon) - read only - Amazon Fresh  This API does not display order data that is more than two years old, except in the JP, AU, and SG marketplaces, for which data from 2016 and after is available.
 *
 * The version of the OpenAPI document: 2026-01-01
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.orders.v2026_01_01;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Payment information about the order. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class OrderPayment {
    public static final String SERIALIZED_NAME_PAYMENT_EXECUTIONS = "paymentExecutions";

    @SerializedName(SERIALIZED_NAME_PAYMENT_EXECUTIONS)
    private List<PaymentExecution> paymentExecutions = new ArrayList<>();

    public OrderPayment() {}

    public OrderPayment paymentExecutions(List<PaymentExecution> paymentExecutions) {
        this.paymentExecutions = paymentExecutions;
        return this;
    }

    public OrderPayment addPaymentExecutionsItem(PaymentExecution paymentExecutionsItem) {
        if (this.paymentExecutions == null) {
            this.paymentExecutions = new ArrayList<>();
        }
        this.paymentExecutions.add(paymentExecutionsItem);
        return this;
    }

    /**
     * A list of payment executions for the order.
     *
     * @return paymentExecutions
     */
    @javax.annotation.Nullable public List<PaymentExecution> getPaymentExecutions() {
        return paymentExecutions;
    }

    public void setPaymentExecutions(List<PaymentExecution> paymentExecutions) {
        this.paymentExecutions = paymentExecutions;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OrderPayment orderPayment = (OrderPayment) o;
        return Objects.equals(this.paymentExecutions, orderPayment.paymentExecutions);
    }

    @Override
    public int hashCode() {
        return Objects.hash(paymentExecutions);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class OrderPayment {\n");
        sb.append("    paymentExecutions: ")
                .append(toIndentedString(paymentExecutions))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("paymentExecutions");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to OrderPayment
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!OrderPayment.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in OrderPayment is not found in the empty JSON string",
                        OrderPayment.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!OrderPayment.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `OrderPayment` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("paymentExecutions") != null
                && !jsonObj.get("paymentExecutions").isJsonNull()) {
            JsonArray jsonArraypaymentExecutions = jsonObj.getAsJsonArray("paymentExecutions");
            if (jsonArraypaymentExecutions != null) {
                // ensure the json data is an array
                if (!jsonObj.get("paymentExecutions").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `paymentExecutions` to be an array in the JSON string but got `%s`",
                            jsonObj.get("paymentExecutions").toString()));
                }

                // validate the optional field `paymentExecutions` (array)
                for (int i = 0; i < jsonArraypaymentExecutions.size(); i++) {
                    PaymentExecution.validateJsonElement(jsonArraypaymentExecutions.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!OrderPayment.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'OrderPayment' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<OrderPayment> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(OrderPayment.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<OrderPayment>() {
                        @Override
                        public void write(JsonWriter out, OrderPayment value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public OrderPayment read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of OrderPayment given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of OrderPayment
     * @throws IOException if the JSON string is invalid with respect to OrderPayment
     */
    public static OrderPayment fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, OrderPayment.class);
    }

    /**
     * Convert an instance of OrderPayment to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
