/*
 * The Selling Partner API for Orders
 * The Selling Partner API for Orders returns orders information. This API supports the following types of orders:  - FBM (Fulfilled by Merchant) - FBA (Fulfilled by Amazon) - read only - Amazon Fresh  This API does not display order data that is more than two years old, except in the JP, AU, and SG marketplaces, for which data from 2016 and after is available.
 *
 * The version of the OpenAPI document: 2026-01-01
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.orders.v2026_01_01;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Customer-specified time preferences for when deliveries should be attempted at the destination address. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class PreferredDeliveryTime {
    public static final String SERIALIZED_NAME_BUSINESS_HOURS = "businessHours";

    @SerializedName(SERIALIZED_NAME_BUSINESS_HOURS)
    private List<BusinessHour> businessHours = new ArrayList<>();

    public static final String SERIALIZED_NAME_EXCEPTION_DATES = "exceptionDates";

    @SerializedName(SERIALIZED_NAME_EXCEPTION_DATES)
    private List<ExceptionDate> exceptionDates = new ArrayList<>();

    public PreferredDeliveryTime() {}

    public PreferredDeliveryTime businessHours(List<BusinessHour> businessHours) {
        this.businessHours = businessHours;
        return this;
    }

    public PreferredDeliveryTime addBusinessHoursItem(BusinessHour businessHoursItem) {
        if (this.businessHours == null) {
            this.businessHours = new ArrayList<>();
        }
        this.businessHours.add(businessHoursItem);
        return this;
    }

    /**
     * Business hours when the business is open for deliveries.
     *
     * @return businessHours
     */
    @javax.annotation.Nullable public List<BusinessHour> getBusinessHours() {
        return businessHours;
    }

    public void setBusinessHours(List<BusinessHour> businessHours) {
        this.businessHours = businessHours;
    }

    public PreferredDeliveryTime exceptionDates(List<ExceptionDate> exceptionDates) {
        this.exceptionDates = exceptionDates;
        return this;
    }

    public PreferredDeliveryTime addExceptionDatesItem(ExceptionDate exceptionDatesItem) {
        if (this.exceptionDates == null) {
            this.exceptionDates = new ArrayList<>();
        }
        this.exceptionDates.add(exceptionDatesItem);
        return this;
    }

    /**
     * Specific dates within the next 30 days when normal business hours do not apply.
     *
     * @return exceptionDates
     */
    @javax.annotation.Nullable public List<ExceptionDate> getExceptionDates() {
        return exceptionDates;
    }

    public void setExceptionDates(List<ExceptionDate> exceptionDates) {
        this.exceptionDates = exceptionDates;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PreferredDeliveryTime preferredDeliveryTime = (PreferredDeliveryTime) o;
        return Objects.equals(this.businessHours, preferredDeliveryTime.businessHours)
                && Objects.equals(this.exceptionDates, preferredDeliveryTime.exceptionDates);
    }

    @Override
    public int hashCode() {
        return Objects.hash(businessHours, exceptionDates);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class PreferredDeliveryTime {\n");
        sb.append("    businessHours: ").append(toIndentedString(businessHours)).append("\n");
        sb.append("    exceptionDates: ")
                .append(toIndentedString(exceptionDates))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("businessHours");
        openapiFields.add("exceptionDates");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to PreferredDeliveryTime
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!PreferredDeliveryTime.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in PreferredDeliveryTime is not found in the empty JSON string",
                        PreferredDeliveryTime.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!PreferredDeliveryTime.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `PreferredDeliveryTime` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("businessHours") != null
                && !jsonObj.get("businessHours").isJsonNull()) {
            JsonArray jsonArraybusinessHours = jsonObj.getAsJsonArray("businessHours");
            if (jsonArraybusinessHours != null) {
                // ensure the json data is an array
                if (!jsonObj.get("businessHours").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `businessHours` to be an array in the JSON string but got `%s`",
                            jsonObj.get("businessHours").toString()));
                }

                // validate the optional field `businessHours` (array)
                for (int i = 0; i < jsonArraybusinessHours.size(); i++) {
                    BusinessHour.validateJsonElement(jsonArraybusinessHours.get(i));
                }
                ;
            }
        }
        if (jsonObj.get("exceptionDates") != null
                && !jsonObj.get("exceptionDates").isJsonNull()) {
            JsonArray jsonArrayexceptionDates = jsonObj.getAsJsonArray("exceptionDates");
            if (jsonArrayexceptionDates != null) {
                // ensure the json data is an array
                if (!jsonObj.get("exceptionDates").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `exceptionDates` to be an array in the JSON string but got `%s`",
                            jsonObj.get("exceptionDates").toString()));
                }

                // validate the optional field `exceptionDates` (array)
                for (int i = 0; i < jsonArrayexceptionDates.size(); i++) {
                    ExceptionDate.validateJsonElement(jsonArrayexceptionDates.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!PreferredDeliveryTime.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'PreferredDeliveryTime' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<PreferredDeliveryTime> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(PreferredDeliveryTime.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<PreferredDeliveryTime>() {
                        @Override
                        public void write(JsonWriter out, PreferredDeliveryTime value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public PreferredDeliveryTime read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of PreferredDeliveryTime given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of PreferredDeliveryTime
     * @throws IOException if the JSON string is invalid with respect to PreferredDeliveryTime
     */
    public static PreferredDeliveryTime fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, PreferredDeliveryTime.class);
    }

    /**
     * Convert an instance of PreferredDeliveryTime to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
