/*
 * Selling Partner API for A+ Content Management
 * Use the A+ Content API to build applications that help selling partners add rich marketing content to their Amazon product detail pages. Selling partners can use A+ content to share their brand and product story, which helps buyers make informed purchasing decisions. Selling partners use content modules to add images and text.
 *
 * The version of the OpenAPI document: 2020-11-01
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.apluscontent.v2020_11_01;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** A standard background image with a floating text box. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class StandardImageTextOverlayModule {
    public static final String SERIALIZED_NAME_OVERLAY_COLOR_TYPE = "overlayColorType";

    @SerializedName(SERIALIZED_NAME_OVERLAY_COLOR_TYPE)
    private ColorType overlayColorType;

    public static final String SERIALIZED_NAME_BLOCK = "block";

    @SerializedName(SERIALIZED_NAME_BLOCK)
    private StandardImageTextBlock block;

    public StandardImageTextOverlayModule() {}

    public StandardImageTextOverlayModule overlayColorType(ColorType overlayColorType) {
        this.overlayColorType = overlayColorType;
        return this;
    }

    /**
     * Get overlayColorType
     *
     * @return overlayColorType
     */
    @javax.annotation.Nonnull
    public ColorType getOverlayColorType() {
        return overlayColorType;
    }

    public void setOverlayColorType(ColorType overlayColorType) {
        this.overlayColorType = overlayColorType;
    }

    public StandardImageTextOverlayModule block(StandardImageTextBlock block) {
        this.block = block;
        return this;
    }

    /**
     * Get block
     *
     * @return block
     */
    @javax.annotation.Nullable public StandardImageTextBlock getBlock() {
        return block;
    }

    public void setBlock(StandardImageTextBlock block) {
        this.block = block;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        StandardImageTextOverlayModule standardImageTextOverlayModule = (StandardImageTextOverlayModule) o;
        return Objects.equals(this.overlayColorType, standardImageTextOverlayModule.overlayColorType)
                && Objects.equals(this.block, standardImageTextOverlayModule.block);
    }

    @Override
    public int hashCode() {
        return Objects.hash(overlayColorType, block);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class StandardImageTextOverlayModule {\n");
        sb.append("    overlayColorType: ")
                .append(toIndentedString(overlayColorType))
                .append("\n");
        sb.append("    block: ").append(toIndentedString(block)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("overlayColorType");
        openapiFields.add("block");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("overlayColorType");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to StandardImageTextOverlayModule
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!StandardImageTextOverlayModule.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in StandardImageTextOverlayModule is not found in the empty JSON string",
                        StandardImageTextOverlayModule.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!StandardImageTextOverlayModule.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `StandardImageTextOverlayModule` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : StandardImageTextOverlayModule.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `overlayColorType`
        ColorType.validateJsonElement(jsonObj.get("overlayColorType"));
        // validate the optional field `block`
        if (jsonObj.get("block") != null && !jsonObj.get("block").isJsonNull()) {
            StandardImageTextBlock.validateJsonElement(jsonObj.get("block"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!StandardImageTextOverlayModule.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'StandardImageTextOverlayModule' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<StandardImageTextOverlayModule> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(StandardImageTextOverlayModule.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<StandardImageTextOverlayModule>() {
                        @Override
                        public void write(JsonWriter out, StandardImageTextOverlayModule value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public StandardImageTextOverlayModule read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of StandardImageTextOverlayModule given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of StandardImageTextOverlayModule
     * @throws IOException if the JSON string is invalid with respect to StandardImageTextOverlayModule
     */
    public static StandardImageTextOverlayModule fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, StandardImageTextOverlayModule.class);
    }

    /**
     * Convert an instance of StandardImageTextOverlayModule to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
