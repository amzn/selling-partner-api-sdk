/*
 * The Selling Partner API for CustomerFeedback
 * The Selling Partner API for Customer Feedback (Customer Feedback API) provides information about customer reviews and returns at both the item and browse node level.
 *
 * The version of the OpenAPI document: 2024-06-01
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.customerfeedback.v2024_06_01;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The item review subtopic metrics. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class ItemReviewSubtopicMetrics {
    public static final String SERIALIZED_NAME_NUMBER_OF_MENTIONS = "numberOfMentions";

    @SerializedName(SERIALIZED_NAME_NUMBER_OF_MENTIONS)
    private Integer numberOfMentions;

    public static final String SERIALIZED_NAME_OCCURRENCE_PERCENTAGE = "occurrencePercentage";

    @SerializedName(SERIALIZED_NAME_OCCURRENCE_PERCENTAGE)
    private Float occurrencePercentage;

    public ItemReviewSubtopicMetrics() {}

    public ItemReviewSubtopicMetrics numberOfMentions(Integer numberOfMentions) {
        this.numberOfMentions = numberOfMentions;
        return this;
    }

    /**
     * The number of times that reviews mention the subtopic.
     *
     * @return numberOfMentions
     */
    @javax.annotation.Nonnull
    public Integer getNumberOfMentions() {
        return numberOfMentions;
    }

    public void setNumberOfMentions(Integer numberOfMentions) {
        this.numberOfMentions = numberOfMentions;
    }

    public ItemReviewSubtopicMetrics occurrencePercentage(Float occurrencePercentage) {
        this.occurrencePercentage = occurrencePercentage;
        return this;
    }

    /**
     * The percentage of reviews that mention the subtopic.
     *
     * @return occurrencePercentage
     */
    @javax.annotation.Nonnull
    public Float getOccurrencePercentage() {
        return occurrencePercentage;
    }

    public void setOccurrencePercentage(Float occurrencePercentage) {
        this.occurrencePercentage = occurrencePercentage;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ItemReviewSubtopicMetrics itemReviewSubtopicMetrics = (ItemReviewSubtopicMetrics) o;
        return Objects.equals(this.numberOfMentions, itemReviewSubtopicMetrics.numberOfMentions)
                && Objects.equals(this.occurrencePercentage, itemReviewSubtopicMetrics.occurrencePercentage);
    }

    @Override
    public int hashCode() {
        return Objects.hash(numberOfMentions, occurrencePercentage);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ItemReviewSubtopicMetrics {\n");
        sb.append("    numberOfMentions: ")
                .append(toIndentedString(numberOfMentions))
                .append("\n");
        sb.append("    occurrencePercentage: ")
                .append(toIndentedString(occurrencePercentage))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("numberOfMentions");
        openapiFields.add("occurrencePercentage");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("numberOfMentions");
        openapiRequiredFields.add("occurrencePercentage");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ItemReviewSubtopicMetrics
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ItemReviewSubtopicMetrics.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in ItemReviewSubtopicMetrics is not found in the empty JSON string",
                        ItemReviewSubtopicMetrics.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ItemReviewSubtopicMetrics.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `ItemReviewSubtopicMetrics` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ItemReviewSubtopicMetrics.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ItemReviewSubtopicMetrics.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ItemReviewSubtopicMetrics' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ItemReviewSubtopicMetrics> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ItemReviewSubtopicMetrics.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ItemReviewSubtopicMetrics>() {
                        @Override
                        public void write(JsonWriter out, ItemReviewSubtopicMetrics value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ItemReviewSubtopicMetrics read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ItemReviewSubtopicMetrics given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ItemReviewSubtopicMetrics
     * @throws IOException if the JSON string is invalid with respect to ItemReviewSubtopicMetrics
     */
    public static ItemReviewSubtopicMetrics fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ItemReviewSubtopicMetrics.class);
    }

    /**
     * Convert an instance of ItemReviewSubtopicMetrics to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
