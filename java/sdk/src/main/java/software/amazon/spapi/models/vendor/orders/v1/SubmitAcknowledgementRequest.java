/*
 * Selling Partner API for Retail Procurement Orders
 * The Selling Partner API for Retail Procurement Orders provides programmatic access to vendor orders data.
 *
 * The version of the OpenAPI document: v1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.vendor.orders.v1;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The request schema for the submitAcknowledgment operation. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class SubmitAcknowledgementRequest {
    public static final String SERIALIZED_NAME_ACKNOWLEDGEMENTS = "acknowledgements";

    @SerializedName(SERIALIZED_NAME_ACKNOWLEDGEMENTS)
    private List<OrderAcknowledgement> acknowledgements = new ArrayList<>();

    public SubmitAcknowledgementRequest() {}

    public SubmitAcknowledgementRequest acknowledgements(List<OrderAcknowledgement> acknowledgements) {
        this.acknowledgements = acknowledgements;
        return this;
    }

    public SubmitAcknowledgementRequest addAcknowledgementsItem(OrderAcknowledgement acknowledgementsItem) {
        if (this.acknowledgements == null) {
            this.acknowledgements = new ArrayList<>();
        }
        this.acknowledgements.add(acknowledgementsItem);
        return this;
    }

    /**
     * An array of order acknowledgements to be submitted.
     *
     * @return acknowledgements
     */
    @javax.annotation.Nullable public List<OrderAcknowledgement> getAcknowledgements() {
        return acknowledgements;
    }

    public void setAcknowledgements(List<OrderAcknowledgement> acknowledgements) {
        this.acknowledgements = acknowledgements;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        SubmitAcknowledgementRequest submitAcknowledgementRequest = (SubmitAcknowledgementRequest) o;
        return Objects.equals(this.acknowledgements, submitAcknowledgementRequest.acknowledgements);
    }

    @Override
    public int hashCode() {
        return Objects.hash(acknowledgements);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class SubmitAcknowledgementRequest {\n");
        sb.append("    acknowledgements: ")
                .append(toIndentedString(acknowledgements))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("acknowledgements");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to SubmitAcknowledgementRequest
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!SubmitAcknowledgementRequest.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in SubmitAcknowledgementRequest is not found in the empty JSON string",
                        SubmitAcknowledgementRequest.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!SubmitAcknowledgementRequest.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `SubmitAcknowledgementRequest` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("acknowledgements") != null
                && !jsonObj.get("acknowledgements").isJsonNull()) {
            JsonArray jsonArrayacknowledgements = jsonObj.getAsJsonArray("acknowledgements");
            if (jsonArrayacknowledgements != null) {
                // ensure the json data is an array
                if (!jsonObj.get("acknowledgements").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `acknowledgements` to be an array in the JSON string but got `%s`",
                            jsonObj.get("acknowledgements").toString()));
                }

                // validate the optional field `acknowledgements` (array)
                for (int i = 0; i < jsonArrayacknowledgements.size(); i++) {
                    OrderAcknowledgement.validateJsonElement(jsonArrayacknowledgements.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SubmitAcknowledgementRequest.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SubmitAcknowledgementRequest' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SubmitAcknowledgementRequest> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(SubmitAcknowledgementRequest.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<SubmitAcknowledgementRequest>() {
                        @Override
                        public void write(JsonWriter out, SubmitAcknowledgementRequest value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public SubmitAcknowledgementRequest read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of SubmitAcknowledgementRequest given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of SubmitAcknowledgementRequest
     * @throws IOException if the JSON string is invalid with respect to SubmitAcknowledgementRequest
     */
    public static SubmitAcknowledgementRequest fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, SubmitAcknowledgementRequest.class);
    }

    /**
     * Convert an instance of SubmitAcknowledgementRequest to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
