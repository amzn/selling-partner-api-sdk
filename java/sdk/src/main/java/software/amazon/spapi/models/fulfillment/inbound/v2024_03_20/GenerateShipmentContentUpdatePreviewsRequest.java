/*
 * The Selling Partner API for FBA inbound operations.
 * The Selling Partner API for Fulfillment By Amazon (FBA) Inbound. The FBA Inbound API enables building inbound workflows to create, manage, and send shipments into Amazon's fulfillment network. The API has interoperability with the Send-to-Amazon user interface.
 *
 * The version of the OpenAPI document: 2024-03-20
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.inbound.v2024_03_20;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The &#x60;GenerateShipmentContentUpdatePreviews&#x60; request. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class GenerateShipmentContentUpdatePreviewsRequest {
    public static final String SERIALIZED_NAME_BOXES = "boxes";

    @SerializedName(SERIALIZED_NAME_BOXES)
    private List<BoxUpdateInput> boxes = new ArrayList<>();

    public static final String SERIALIZED_NAME_ITEMS = "items";

    @SerializedName(SERIALIZED_NAME_ITEMS)
    private List<ItemInput> items = new ArrayList<>();

    public GenerateShipmentContentUpdatePreviewsRequest() {}

    public GenerateShipmentContentUpdatePreviewsRequest boxes(List<BoxUpdateInput> boxes) {
        this.boxes = boxes;
        return this;
    }

    public GenerateShipmentContentUpdatePreviewsRequest addBoxesItem(BoxUpdateInput boxesItem) {
        if (this.boxes == null) {
            this.boxes = new ArrayList<>();
        }
        this.boxes.add(boxesItem);
        return this;
    }

    /**
     * A list of boxes that will be present in the shipment after the update.
     *
     * @return boxes
     */
    @javax.annotation.Nonnull
    public List<BoxUpdateInput> getBoxes() {
        return boxes;
    }

    public void setBoxes(List<BoxUpdateInput> boxes) {
        this.boxes = boxes;
    }

    public GenerateShipmentContentUpdatePreviewsRequest items(List<ItemInput> items) {
        this.items = items;
        return this;
    }

    public GenerateShipmentContentUpdatePreviewsRequest addItemsItem(ItemInput itemsItem) {
        if (this.items == null) {
            this.items = new ArrayList<>();
        }
        this.items.add(itemsItem);
        return this;
    }

    /**
     * A list of all items that will be present in the shipment after the update.
     *
     * @return items
     */
    @javax.annotation.Nonnull
    public List<ItemInput> getItems() {
        return items;
    }

    public void setItems(List<ItemInput> items) {
        this.items = items;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GenerateShipmentContentUpdatePreviewsRequest generateShipmentContentUpdatePreviewsRequest =
                (GenerateShipmentContentUpdatePreviewsRequest) o;
        return Objects.equals(this.boxes, generateShipmentContentUpdatePreviewsRequest.boxes)
                && Objects.equals(this.items, generateShipmentContentUpdatePreviewsRequest.items);
    }

    @Override
    public int hashCode() {
        return Objects.hash(boxes, items);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class GenerateShipmentContentUpdatePreviewsRequest {\n");
        sb.append("    boxes: ").append(toIndentedString(boxes)).append("\n");
        sb.append("    items: ").append(toIndentedString(items)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("boxes");
        openapiFields.add("items");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("boxes");
        openapiRequiredFields.add("items");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GenerateShipmentContentUpdatePreviewsRequest
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!GenerateShipmentContentUpdatePreviewsRequest.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in GenerateShipmentContentUpdatePreviewsRequest is not found in the empty JSON string",
                        GenerateShipmentContentUpdatePreviewsRequest.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!GenerateShipmentContentUpdatePreviewsRequest.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `GenerateShipmentContentUpdatePreviewsRequest` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : GenerateShipmentContentUpdatePreviewsRequest.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the json data is an array
        if (!jsonObj.get("boxes").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `boxes` to be an array in the JSON string but got `%s`",
                    jsonObj.get("boxes").toString()));
        }

        JsonArray jsonArrayboxes = jsonObj.getAsJsonArray("boxes");
        // validate the required field `boxes` (array)
        for (int i = 0; i < jsonArrayboxes.size(); i++) {
            BoxUpdateInput.validateJsonElement(jsonArrayboxes.get(i));
        }
        ;
        // ensure the json data is an array
        if (!jsonObj.get("items").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `items` to be an array in the JSON string but got `%s`",
                    jsonObj.get("items").toString()));
        }

        JsonArray jsonArrayitems = jsonObj.getAsJsonArray("items");
        // validate the required field `items` (array)
        for (int i = 0; i < jsonArrayitems.size(); i++) {
            ItemInput.validateJsonElement(jsonArrayitems.get(i));
        }
        ;
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GenerateShipmentContentUpdatePreviewsRequest.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GenerateShipmentContentUpdatePreviewsRequest' and its
                // subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GenerateShipmentContentUpdatePreviewsRequest> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(GenerateShipmentContentUpdatePreviewsRequest.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<GenerateShipmentContentUpdatePreviewsRequest>() {
                        @Override
                        public void write(JsonWriter out, GenerateShipmentContentUpdatePreviewsRequest value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public GenerateShipmentContentUpdatePreviewsRequest read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of GenerateShipmentContentUpdatePreviewsRequest given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GenerateShipmentContentUpdatePreviewsRequest
     * @throws IOException if the JSON string is invalid with respect to GenerateShipmentContentUpdatePreviewsRequest
     */
    public static GenerateShipmentContentUpdatePreviewsRequest fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GenerateShipmentContentUpdatePreviewsRequest.class);
    }

    /**
     * Convert an instance of GenerateShipmentContentUpdatePreviewsRequest to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
