/*
 * The Selling Partner API for Orders
 * The Selling Partner API for Orders returns orders information. This API supports the following types of orders:  - FBM (Fulfilled by Merchant) - FBA (Fulfilled by Amazon) - read only - Amazon Fresh  This API does not display order data that is more than two years old, except in the JP, AU, and SG marketplaces, for which data from 2016 and after is available.
 *
 * The version of the OpenAPI document: 2026-01-01
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.orders.v2026_01_01;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Tax information about the order. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class OrderTax {
    public static final String SERIALIZED_NAME_TAX_REGISTRATIONS = "taxRegistrations";

    @SerializedName(SERIALIZED_NAME_TAX_REGISTRATIONS)
    private List<OrderTaxRegistration> taxRegistrations = new ArrayList<>();

    public static final String SERIALIZED_NAME_TAX_INVOICING = "taxInvoicing";

    @SerializedName(SERIALIZED_NAME_TAX_INVOICING)
    private OrderTaxInvoicing taxInvoicing;

    public OrderTax() {}

    public OrderTax taxRegistrations(List<OrderTaxRegistration> taxRegistrations) {
        this.taxRegistrations = taxRegistrations;
        return this;
    }

    public OrderTax addTaxRegistrationsItem(OrderTaxRegistration taxRegistrationsItem) {
        if (this.taxRegistrations == null) {
            this.taxRegistrations = new ArrayList<>();
        }
        this.taxRegistrations.add(taxRegistrationsItem);
        return this;
    }

    /**
     * A list of tax registrations associated with the order.
     *
     * @return taxRegistrations
     */
    @javax.annotation.Nullable public List<OrderTaxRegistration> getTaxRegistrations() {
        return taxRegistrations;
    }

    public void setTaxRegistrations(List<OrderTaxRegistration> taxRegistrations) {
        this.taxRegistrations = taxRegistrations;
    }

    public OrderTax taxInvoicing(OrderTaxInvoicing taxInvoicing) {
        this.taxInvoicing = taxInvoicing;
        return this;
    }

    /**
     * Get taxInvoicing
     *
     * @return taxInvoicing
     */
    @javax.annotation.Nullable public OrderTaxInvoicing getTaxInvoicing() {
        return taxInvoicing;
    }

    public void setTaxInvoicing(OrderTaxInvoicing taxInvoicing) {
        this.taxInvoicing = taxInvoicing;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OrderTax orderTax = (OrderTax) o;
        return Objects.equals(this.taxRegistrations, orderTax.taxRegistrations)
                && Objects.equals(this.taxInvoicing, orderTax.taxInvoicing);
    }

    @Override
    public int hashCode() {
        return Objects.hash(taxRegistrations, taxInvoicing);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class OrderTax {\n");
        sb.append("    taxRegistrations: ")
                .append(toIndentedString(taxRegistrations))
                .append("\n");
        sb.append("    taxInvoicing: ").append(toIndentedString(taxInvoicing)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("taxRegistrations");
        openapiFields.add("taxInvoicing");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to OrderTax
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!OrderTax.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in OrderTax is not found in the empty JSON string",
                        OrderTax.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!OrderTax.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `OrderTax` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("taxRegistrations") != null
                && !jsonObj.get("taxRegistrations").isJsonNull()) {
            JsonArray jsonArraytaxRegistrations = jsonObj.getAsJsonArray("taxRegistrations");
            if (jsonArraytaxRegistrations != null) {
                // ensure the json data is an array
                if (!jsonObj.get("taxRegistrations").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `taxRegistrations` to be an array in the JSON string but got `%s`",
                            jsonObj.get("taxRegistrations").toString()));
                }

                // validate the optional field `taxRegistrations` (array)
                for (int i = 0; i < jsonArraytaxRegistrations.size(); i++) {
                    OrderTaxRegistration.validateJsonElement(jsonArraytaxRegistrations.get(i));
                }
                ;
            }
        }
        // validate the optional field `taxInvoicing`
        if (jsonObj.get("taxInvoicing") != null && !jsonObj.get("taxInvoicing").isJsonNull()) {
            OrderTaxInvoicing.validateJsonElement(jsonObj.get("taxInvoicing"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!OrderTax.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'OrderTax' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<OrderTax> thisAdapter = gson.getDelegateAdapter(this, TypeToken.get(OrderTax.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<OrderTax>() {
                        @Override
                        public void write(JsonWriter out, OrderTax value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public OrderTax read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of OrderTax given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of OrderTax
     * @throws IOException if the JSON string is invalid with respect to OrderTax
     */
    public static OrderTax fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, OrderTax.class);
    }

    /**
     * Convert an instance of OrderTax to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
