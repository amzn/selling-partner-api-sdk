/*
 * The Selling Partner API for Amazon External Fulfillment Shipments Processing
 * You can use the External Fulfillment Shipments API to retrieve, manage, and track shipments processed through Amazon's external fulfillment network. Use this API to get shipment details, monitor status changes, and access fulfillment requirements.
 *
 * The version of the OpenAPI document: 2024-09-11
 * Contact: marketplaceapitest@amazon.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.externalfulfillment.shipments.v2024_09_11;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/**
 * Details of the original shipment if &#x60;shipmentType&#x60; is either &#x60;REPLACEMENT&#x60; or
 * &#x60;EXCHANGE&#x60;.
 */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class ReplacedShipmentInfo {
    public static final String SERIALIZED_NAME_ID = "id";

    @SerializedName(SERIALIZED_NAME_ID)
    private String id;

    public static final String SERIALIZED_NAME_LOCATION_ID = "locationId";

    @SerializedName(SERIALIZED_NAME_LOCATION_ID)
    private String locationId;

    public static final String SERIALIZED_NAME_MARKETPLACE_ATTRIBUTES = "marketplaceAttributes";

    @SerializedName(SERIALIZED_NAME_MARKETPLACE_ATTRIBUTES)
    private MarketplaceAttributes marketplaceAttributes;

    public static final String SERIALIZED_NAME_ORIGINAL_LINE_ITEMS = "originalLineItems";

    @SerializedName(SERIALIZED_NAME_ORIGINAL_LINE_ITEMS)
    private List<OriginalLineItem> originalLineItems = new ArrayList<>();

    public ReplacedShipmentInfo() {}

    public ReplacedShipmentInfo id(String id) {
        this.id = id;
        return this;
    }

    /**
     * The original shipment&#39;s ID.
     *
     * @return id
     */
    @javax.annotation.Nonnull
    public String getId() {
        return id;
    }

    public void setId(String id) {
        this.id = id;
    }

    public ReplacedShipmentInfo locationId(String locationId) {
        this.locationId = locationId;
        return this;
    }

    /**
     * The original shipment&#39;s location ID.
     *
     * @return locationId
     */
    @javax.annotation.Nonnull
    public String getLocationId() {
        return locationId;
    }

    public void setLocationId(String locationId) {
        this.locationId = locationId;
    }

    public ReplacedShipmentInfo marketplaceAttributes(MarketplaceAttributes marketplaceAttributes) {
        this.marketplaceAttributes = marketplaceAttributes;
        return this;
    }

    /**
     * Get marketplaceAttributes
     *
     * @return marketplaceAttributes
     */
    @javax.annotation.Nonnull
    public MarketplaceAttributes getMarketplaceAttributes() {
        return marketplaceAttributes;
    }

    public void setMarketplaceAttributes(MarketplaceAttributes marketplaceAttributes) {
        this.marketplaceAttributes = marketplaceAttributes;
    }

    public ReplacedShipmentInfo originalLineItems(List<OriginalLineItem> originalLineItems) {
        this.originalLineItems = originalLineItems;
        return this;
    }

    public ReplacedShipmentInfo addOriginalLineItemsItem(OriginalLineItem originalLineItemsItem) {
        if (this.originalLineItems == null) {
            this.originalLineItems = new ArrayList<>();
        }
        this.originalLineItems.add(originalLineItemsItem);
        return this;
    }

    /**
     * The line items from the original shipment that corresponds to this shipment.
     *
     * @return originalLineItems
     */
    @javax.annotation.Nullable public List<OriginalLineItem> getOriginalLineItems() {
        return originalLineItems;
    }

    public void setOriginalLineItems(List<OriginalLineItem> originalLineItems) {
        this.originalLineItems = originalLineItems;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ReplacedShipmentInfo replacedShipmentInfo = (ReplacedShipmentInfo) o;
        return Objects.equals(this.id, replacedShipmentInfo.id)
                && Objects.equals(this.locationId, replacedShipmentInfo.locationId)
                && Objects.equals(this.marketplaceAttributes, replacedShipmentInfo.marketplaceAttributes)
                && Objects.equals(this.originalLineItems, replacedShipmentInfo.originalLineItems);
    }

    @Override
    public int hashCode() {
        return Objects.hash(id, locationId, marketplaceAttributes, originalLineItems);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ReplacedShipmentInfo {\n");
        sb.append("    id: ").append(toIndentedString(id)).append("\n");
        sb.append("    locationId: ").append(toIndentedString(locationId)).append("\n");
        sb.append("    marketplaceAttributes: ")
                .append(toIndentedString(marketplaceAttributes))
                .append("\n");
        sb.append("    originalLineItems: ")
                .append(toIndentedString(originalLineItems))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("id");
        openapiFields.add("locationId");
        openapiFields.add("marketplaceAttributes");
        openapiFields.add("originalLineItems");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("id");
        openapiRequiredFields.add("locationId");
        openapiRequiredFields.add("marketplaceAttributes");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ReplacedShipmentInfo
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ReplacedShipmentInfo.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in ReplacedShipmentInfo is not found in the empty JSON string",
                        ReplacedShipmentInfo.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ReplacedShipmentInfo.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `ReplacedShipmentInfo` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ReplacedShipmentInfo.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("id").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `id` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("id").toString()));
        }
        if (!jsonObj.get("locationId").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `locationId` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("locationId").toString()));
        }
        // validate the required field `marketplaceAttributes`
        MarketplaceAttributes.validateJsonElement(jsonObj.get("marketplaceAttributes"));
        if (jsonObj.get("originalLineItems") != null
                && !jsonObj.get("originalLineItems").isJsonNull()) {
            JsonArray jsonArrayoriginalLineItems = jsonObj.getAsJsonArray("originalLineItems");
            if (jsonArrayoriginalLineItems != null) {
                // ensure the json data is an array
                if (!jsonObj.get("originalLineItems").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `originalLineItems` to be an array in the JSON string but got `%s`",
                            jsonObj.get("originalLineItems").toString()));
                }

                // validate the optional field `originalLineItems` (array)
                for (int i = 0; i < jsonArrayoriginalLineItems.size(); i++) {
                    OriginalLineItem.validateJsonElement(jsonArrayoriginalLineItems.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ReplacedShipmentInfo.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ReplacedShipmentInfo' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ReplacedShipmentInfo> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ReplacedShipmentInfo.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ReplacedShipmentInfo>() {
                        @Override
                        public void write(JsonWriter out, ReplacedShipmentInfo value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ReplacedShipmentInfo read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ReplacedShipmentInfo given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ReplacedShipmentInfo
     * @throws IOException if the JSON string is invalid with respect to ReplacedShipmentInfo
     */
    public static ReplacedShipmentInfo fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ReplacedShipmentInfo.class);
    }

    /**
     * Convert an instance of ReplacedShipmentInfo to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
