/*
 * The Selling Partner API for Fulfillment Outbound
 * The Selling Partner API for Fulfillment Outbound lets you create applications that help a seller fulfill Multi-Channel Fulfillment orders using their inventory in Amazon's fulfillment network. You can get information on both potential and existing fulfillment orders.
 *
 * The version of the OpenAPI document: 2026-07-04
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.outbound.v2026_07_04;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/**
 * Service level configuration for data-plane requests (&#x60;getOffers&#x60;, &#x60;getOrderPreview&#x60;). Specifies
 * the tiers for which to get offers or previews.
 */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class PreviewServiceLevel {
    public static final String SERIALIZED_NAME_SERVICE_TIERS = "serviceTiers";

    @SerializedName(SERIALIZED_NAME_SERVICE_TIERS)
    private List<String> serviceTiers = new ArrayList<>();

    public PreviewServiceLevel() {}

    public PreviewServiceLevel serviceTiers(List<String> serviceTiers) {
        this.serviceTiers = serviceTiers;
        return this;
    }

    public PreviewServiceLevel addServiceTiersItem(String serviceTiersItem) {
        if (this.serviceTiers == null) {
            this.serviceTiers = new ArrayList<>();
        }
        this.serviceTiers.add(serviceTiersItem);
        return this;
    }

    /**
     * The service tiers to retrieve offers for. Available values: &#x60;STANDARD&#x60;, &#x60;EXPEDITED&#x60;,
     * &#x60;PRIORITY&#x60; (only available in Canada, India, and Mexico), &#x60;SCHEDULED&#x60; (only available in
     * Japan). Multiple values can be provided to receive offers across different tiers.
     *
     * @return serviceTiers
     */
    @javax.annotation.Nullable public List<String> getServiceTiers() {
        return serviceTiers;
    }

    public void setServiceTiers(List<String> serviceTiers) {
        this.serviceTiers = serviceTiers;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PreviewServiceLevel previewServiceLevel = (PreviewServiceLevel) o;
        return Objects.equals(this.serviceTiers, previewServiceLevel.serviceTiers);
    }

    @Override
    public int hashCode() {
        return Objects.hash(serviceTiers);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class PreviewServiceLevel {\n");
        sb.append("    serviceTiers: ").append(toIndentedString(serviceTiers)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("serviceTiers");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to PreviewServiceLevel
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!PreviewServiceLevel.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in PreviewServiceLevel is not found in the empty JSON string",
                        PreviewServiceLevel.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!PreviewServiceLevel.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `PreviewServiceLevel` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the optional json data is an array if present
        if (jsonObj.get("serviceTiers") != null
                && !jsonObj.get("serviceTiers").isJsonNull()
                && !jsonObj.get("serviceTiers").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `serviceTiers` to be an array in the JSON string but got `%s`",
                    jsonObj.get("serviceTiers").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!PreviewServiceLevel.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'PreviewServiceLevel' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<PreviewServiceLevel> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(PreviewServiceLevel.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<PreviewServiceLevel>() {
                        @Override
                        public void write(JsonWriter out, PreviewServiceLevel value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public PreviewServiceLevel read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of PreviewServiceLevel given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of PreviewServiceLevel
     * @throws IOException if the JSON string is invalid with respect to PreviewServiceLevel
     */
    public static PreviewServiceLevel fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, PreviewServiceLevel.class);
    }

    /**
     * Convert an instance of PreviewServiceLevel to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
