/*
 * The Selling Partner API for FBA inbound operations.
 * The Selling Partner API for Fulfillment By Amazon (FBA) Inbound. The FBA Inbound API enables building inbound workflows to create, manage, and send shipments into Amazon's fulfillment network. The API has interoperability with the Send-to-Amazon user interface.
 *
 * The version of the OpenAPI document: 2024-03-20
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.inbound.v2024_03_20;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The &#x60;getDeliveryChallanDocumentResponse&#x60; response. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class GetDeliveryChallanDocumentResponse {
    public static final String SERIALIZED_NAME_DOCUMENT_DOWNLOAD = "documentDownload";

    @SerializedName(SERIALIZED_NAME_DOCUMENT_DOWNLOAD)
    private DocumentDownload documentDownload;

    public GetDeliveryChallanDocumentResponse() {}

    public GetDeliveryChallanDocumentResponse documentDownload(DocumentDownload documentDownload) {
        this.documentDownload = documentDownload;
        return this;
    }

    /**
     * Get documentDownload
     *
     * @return documentDownload
     */
    @javax.annotation.Nonnull
    public DocumentDownload getDocumentDownload() {
        return documentDownload;
    }

    public void setDocumentDownload(DocumentDownload documentDownload) {
        this.documentDownload = documentDownload;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GetDeliveryChallanDocumentResponse getDeliveryChallanDocumentResponse = (GetDeliveryChallanDocumentResponse) o;
        return Objects.equals(this.documentDownload, getDeliveryChallanDocumentResponse.documentDownload);
    }

    @Override
    public int hashCode() {
        return Objects.hash(documentDownload);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class GetDeliveryChallanDocumentResponse {\n");
        sb.append("    documentDownload: ")
                .append(toIndentedString(documentDownload))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("documentDownload");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("documentDownload");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GetDeliveryChallanDocumentResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!GetDeliveryChallanDocumentResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in GetDeliveryChallanDocumentResponse is not found in the empty JSON string",
                        GetDeliveryChallanDocumentResponse.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!GetDeliveryChallanDocumentResponse.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `GetDeliveryChallanDocumentResponse` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : GetDeliveryChallanDocumentResponse.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `documentDownload`
        DocumentDownload.validateJsonElement(jsonObj.get("documentDownload"));
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GetDeliveryChallanDocumentResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GetDeliveryChallanDocumentResponse' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GetDeliveryChallanDocumentResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(GetDeliveryChallanDocumentResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<GetDeliveryChallanDocumentResponse>() {
                        @Override
                        public void write(JsonWriter out, GetDeliveryChallanDocumentResponse value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public GetDeliveryChallanDocumentResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of GetDeliveryChallanDocumentResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GetDeliveryChallanDocumentResponse
     * @throws IOException if the JSON string is invalid with respect to GetDeliveryChallanDocumentResponse
     */
    public static GetDeliveryChallanDocumentResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GetDeliveryChallanDocumentResponse.class);
    }

    /**
     * Convert an instance of GetDeliveryChallanDocumentResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
