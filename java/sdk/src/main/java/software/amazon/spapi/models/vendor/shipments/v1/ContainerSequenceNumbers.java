/*
 * Selling Partner API for Retail Procurement Shipments
 * The Selling Partner API for Retail Procurement Shipments provides programmatic access to retail shipping data for vendors.
 *
 * The version of the OpenAPI document: v1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.vendor.shipments.v1;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Container sequence numbers that are involved in this shipment. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class ContainerSequenceNumbers {
    public static final String SERIALIZED_NAME_CONTAINER_SEQUENCE_NUMBER = "containerSequenceNumber";

    @SerializedName(SERIALIZED_NAME_CONTAINER_SEQUENCE_NUMBER)
    private String containerSequenceNumber;

    public ContainerSequenceNumbers() {}

    public ContainerSequenceNumbers containerSequenceNumber(String containerSequenceNumber) {
        this.containerSequenceNumber = containerSequenceNumber;
        return this;
    }

    /**
     * A list of containers shipped
     *
     * @return containerSequenceNumber
     */
    @javax.annotation.Nullable public String getContainerSequenceNumber() {
        return containerSequenceNumber;
    }

    public void setContainerSequenceNumber(String containerSequenceNumber) {
        this.containerSequenceNumber = containerSequenceNumber;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ContainerSequenceNumbers containerSequenceNumbers = (ContainerSequenceNumbers) o;
        return Objects.equals(this.containerSequenceNumber, containerSequenceNumbers.containerSequenceNumber);
    }

    @Override
    public int hashCode() {
        return Objects.hash(containerSequenceNumber);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ContainerSequenceNumbers {\n");
        sb.append("    containerSequenceNumber: ")
                .append(toIndentedString(containerSequenceNumber))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("containerSequenceNumber");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ContainerSequenceNumbers
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ContainerSequenceNumbers.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in ContainerSequenceNumbers is not found in the empty JSON string",
                        ContainerSequenceNumbers.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ContainerSequenceNumbers.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `ContainerSequenceNumbers` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("containerSequenceNumber") != null
                        && !jsonObj.get("containerSequenceNumber").isJsonNull())
                && !jsonObj.get("containerSequenceNumber").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `containerSequenceNumber` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("containerSequenceNumber").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ContainerSequenceNumbers.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ContainerSequenceNumbers' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ContainerSequenceNumbers> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ContainerSequenceNumbers.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ContainerSequenceNumbers>() {
                        @Override
                        public void write(JsonWriter out, ContainerSequenceNumbers value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ContainerSequenceNumbers read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ContainerSequenceNumbers given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ContainerSequenceNumbers
     * @throws IOException if the JSON string is invalid with respect to ContainerSequenceNumbers
     */
    public static ContainerSequenceNumbers fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ContainerSequenceNumbers.class);
    }

    /**
     * Convert an instance of ContainerSequenceNumbers to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
