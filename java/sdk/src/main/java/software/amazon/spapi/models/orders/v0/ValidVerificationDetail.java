/*
 * Selling Partner API for Orders
 * Use the Orders Selling Partner API to programmatically retrieve order information. With this API, you can develop fast, flexible, and custom applications to manage order synchronization, perform order research, and create demand-based decision support tools.   _Note:_ For the JP, AU, and SG marketplaces, the Orders API supports orders from 2016 onward. For all other marketplaces, the Orders API supports orders for the last two years (orders older than this don't show up in the response).
 *
 * The version of the OpenAPI document: v0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.orders.v0;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/**
 * The types of verification details that may be provided for the order and the criteria required for when the type of
 * verification detail can be provided. The types of verification details allowed depend on the type of regulated
 * product and will not change order to order.
 */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class ValidVerificationDetail {
    public static final String SERIALIZED_NAME_VERIFICATION_DETAIL_TYPE = "VerificationDetailType";

    @SerializedName(SERIALIZED_NAME_VERIFICATION_DETAIL_TYPE)
    private String verificationDetailType;

    public static final String SERIALIZED_NAME_VALID_VERIFICATION_STATUSES = "ValidVerificationStatuses";

    @SerializedName(SERIALIZED_NAME_VALID_VERIFICATION_STATUSES)
    private List<VerificationStatus> validVerificationStatuses = new ArrayList<>();

    public ValidVerificationDetail() {}

    public ValidVerificationDetail verificationDetailType(String verificationDetailType) {
        this.verificationDetailType = verificationDetailType;
        return this;
    }

    /**
     * A supported type of verification detail. The type indicates which verification detail could be shared while
     * updating the regulated order. Valid values: &#x60;prescriptionDetail&#x60;,
     * &#x60;approvedAlternativeDetails&#x60;, &#x60;interimStatusDetail&#x60;.
     *
     * @return verificationDetailType
     */
    @javax.annotation.Nonnull
    public String getVerificationDetailType() {
        return verificationDetailType;
    }

    public void setVerificationDetailType(String verificationDetailType) {
        this.verificationDetailType = verificationDetailType;
    }

    public ValidVerificationDetail validVerificationStatuses(List<VerificationStatus> validVerificationStatuses) {
        this.validVerificationStatuses = validVerificationStatuses;
        return this;
    }

    public ValidVerificationDetail addValidVerificationStatusesItem(VerificationStatus validVerificationStatusesItem) {
        if (this.validVerificationStatuses == null) {
            this.validVerificationStatuses = new ArrayList<>();
        }
        this.validVerificationStatuses.add(validVerificationStatusesItem);
        return this;
    }

    /**
     * A list of valid verification statuses where the associated verification detail type may be provided. For example,
     * if the value of this field is [\&quot;Approved\&quot;], calls to provide the associated verification detail will
     * fail for orders with a &#x60;VerificationStatus&#x60; of &#x60;Pending&#x60;, &#x60;Rejected&#x60;,
     * &#x60;Expired&#x60;, or &#x60;Cancelled&#x60;.
     *
     * @return validVerificationStatuses
     */
    @javax.annotation.Nonnull
    public List<VerificationStatus> getValidVerificationStatuses() {
        return validVerificationStatuses;
    }

    public void setValidVerificationStatuses(List<VerificationStatus> validVerificationStatuses) {
        this.validVerificationStatuses = validVerificationStatuses;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ValidVerificationDetail validVerificationDetail = (ValidVerificationDetail) o;
        return Objects.equals(this.verificationDetailType, validVerificationDetail.verificationDetailType)
                && Objects.equals(this.validVerificationStatuses, validVerificationDetail.validVerificationStatuses);
    }

    @Override
    public int hashCode() {
        return Objects.hash(verificationDetailType, validVerificationStatuses);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ValidVerificationDetail {\n");
        sb.append("    verificationDetailType: ")
                .append(toIndentedString(verificationDetailType))
                .append("\n");
        sb.append("    validVerificationStatuses: ")
                .append(toIndentedString(validVerificationStatuses))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("VerificationDetailType");
        openapiFields.add("ValidVerificationStatuses");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("VerificationDetailType");
        openapiRequiredFields.add("ValidVerificationStatuses");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ValidVerificationDetail
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ValidVerificationDetail.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in ValidVerificationDetail is not found in the empty JSON string",
                        ValidVerificationDetail.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ValidVerificationDetail.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `ValidVerificationDetail` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ValidVerificationDetail.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("VerificationDetailType").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `VerificationDetailType` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("VerificationDetailType").toString()));
        }
        // ensure the required json array is present
        if (jsonObj.get("ValidVerificationStatuses") == null) {
            throw new IllegalArgumentException(
                    "Expected the field `linkedContent` to be an array in the JSON string but got `null`");
        } else if (!jsonObj.get("ValidVerificationStatuses").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `ValidVerificationStatuses` to be an array in the JSON string but got `%s`",
                    jsonObj.get("ValidVerificationStatuses").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ValidVerificationDetail.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ValidVerificationDetail' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ValidVerificationDetail> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ValidVerificationDetail.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ValidVerificationDetail>() {
                        @Override
                        public void write(JsonWriter out, ValidVerificationDetail value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ValidVerificationDetail read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ValidVerificationDetail given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ValidVerificationDetail
     * @throws IOException if the JSON string is invalid with respect to ValidVerificationDetail
     */
    public static ValidVerificationDetail fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ValidVerificationDetail.class);
    }

    /**
     * Convert an instance of ValidVerificationDetail to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
