/*
 * The Selling Partner API for Fulfillment Outbound
 * The Selling Partner API for Fulfillment Outbound lets you create applications that help a seller fulfill Multi-Channel Fulfillment orders using their inventory in Amazon's fulfillment network. You can get information on both potential and existing fulfillment orders.
 *
 * The version of the OpenAPI document: 2026-07-04
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.outbound.v2026_07_04;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The response schema for &#x60;getOrderPreview&#x60;. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class GetOrderPreviewResponse {
    public static final String SERIALIZED_NAME_PLANNED_SHIPMENTS = "plannedShipments";

    @SerializedName(SERIALIZED_NAME_PLANNED_SHIPMENTS)
    private List<PreviewPlannedShipment> plannedShipments = new ArrayList<>();

    public static final String SERIALIZED_NAME_CONSTRAINTS = "constraints";

    @SerializedName(SERIALIZED_NAME_CONSTRAINTS)
    private List<Constraint> constraints = new ArrayList<>();

    public GetOrderPreviewResponse() {}

    public GetOrderPreviewResponse plannedShipments(List<PreviewPlannedShipment> plannedShipments) {
        this.plannedShipments = plannedShipments;
        return this;
    }

    public GetOrderPreviewResponse addPlannedShipmentsItem(PreviewPlannedShipment plannedShipmentsItem) {
        if (this.plannedShipments == null) {
            this.plannedShipments = new ArrayList<>();
        }
        this.plannedShipments.add(plannedShipmentsItem);
        return this;
    }

    /**
     * An array of planned shipment information.
     *
     * @return plannedShipments
     */
    @javax.annotation.Nonnull
    public List<PreviewPlannedShipment> getPlannedShipments() {
        return plannedShipments;
    }

    public void setPlannedShipments(List<PreviewPlannedShipment> plannedShipments) {
        this.plannedShipments = plannedShipments;
    }

    public GetOrderPreviewResponse constraints(List<Constraint> constraints) {
        this.constraints = constraints;
        return this;
    }

    public GetOrderPreviewResponse addConstraintsItem(Constraint constraintsItem) {
        if (this.constraints == null) {
            this.constraints = new ArrayList<>();
        }
        this.constraints.add(constraintsItem);
        return this;
    }

    /**
     * An array of constraints that may affect the fulfillment order.
     *
     * @return constraints
     */
    @javax.annotation.Nullable public List<Constraint> getConstraints() {
        return constraints;
    }

    public void setConstraints(List<Constraint> constraints) {
        this.constraints = constraints;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GetOrderPreviewResponse getOrderPreviewResponse = (GetOrderPreviewResponse) o;
        return Objects.equals(this.plannedShipments, getOrderPreviewResponse.plannedShipments)
                && Objects.equals(this.constraints, getOrderPreviewResponse.constraints);
    }

    @Override
    public int hashCode() {
        return Objects.hash(plannedShipments, constraints);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class GetOrderPreviewResponse {\n");
        sb.append("    plannedShipments: ")
                .append(toIndentedString(plannedShipments))
                .append("\n");
        sb.append("    constraints: ").append(toIndentedString(constraints)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("plannedShipments");
        openapiFields.add("constraints");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("plannedShipments");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GetOrderPreviewResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!GetOrderPreviewResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in GetOrderPreviewResponse is not found in the empty JSON string",
                        GetOrderPreviewResponse.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!GetOrderPreviewResponse.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `GetOrderPreviewResponse` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : GetOrderPreviewResponse.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the json data is an array
        if (!jsonObj.get("plannedShipments").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `plannedShipments` to be an array in the JSON string but got `%s`",
                    jsonObj.get("plannedShipments").toString()));
        }

        JsonArray jsonArrayplannedShipments = jsonObj.getAsJsonArray("plannedShipments");
        // validate the required field `plannedShipments` (array)
        for (int i = 0; i < jsonArrayplannedShipments.size(); i++) {
            PreviewPlannedShipment.validateJsonElement(jsonArrayplannedShipments.get(i));
        }
        ;
        if (jsonObj.get("constraints") != null && !jsonObj.get("constraints").isJsonNull()) {
            JsonArray jsonArrayconstraints = jsonObj.getAsJsonArray("constraints");
            if (jsonArrayconstraints != null) {
                // ensure the json data is an array
                if (!jsonObj.get("constraints").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `constraints` to be an array in the JSON string but got `%s`",
                            jsonObj.get("constraints").toString()));
                }

                // validate the optional field `constraints` (array)
                for (int i = 0; i < jsonArrayconstraints.size(); i++) {
                    Constraint.validateJsonElement(jsonArrayconstraints.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GetOrderPreviewResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GetOrderPreviewResponse' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GetOrderPreviewResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(GetOrderPreviewResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<GetOrderPreviewResponse>() {
                        @Override
                        public void write(JsonWriter out, GetOrderPreviewResponse value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public GetOrderPreviewResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of GetOrderPreviewResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GetOrderPreviewResponse
     * @throws IOException if the JSON string is invalid with respect to GetOrderPreviewResponse
     */
    public static GetOrderPreviewResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GetOrderPreviewResponse.class);
    }

    /**
     * Convert an instance of GetOrderPreviewResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
