/*
 * The Selling Partner API for FBA inbound operations.
 * The Selling Partner API for Fulfillment By Amazon (FBA) Inbound. The FBA Inbound API enables building inbound workflows to create, manage, and send shipments into Amazon's fulfillment network. The API has interoperability with the Send-to-Amazon user interface.
 *
 * The version of the OpenAPI document: 2024-03-20
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.inbound.v2024_03_20;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The &#x60;confirmTransportationOptions&#x60; request. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class ConfirmTransportationOptionsRequest {
    public static final String SERIALIZED_NAME_TRANSPORTATION_SELECTIONS = "transportationSelections";

    @SerializedName(SERIALIZED_NAME_TRANSPORTATION_SELECTIONS)
    private List<TransportationSelection> transportationSelections = new ArrayList<>();

    public ConfirmTransportationOptionsRequest() {}

    public ConfirmTransportationOptionsRequest transportationSelections(
            List<TransportationSelection> transportationSelections) {
        this.transportationSelections = transportationSelections;
        return this;
    }

    public ConfirmTransportationOptionsRequest addTransportationSelectionsItem(
            TransportationSelection transportationSelectionsItem) {
        if (this.transportationSelections == null) {
            this.transportationSelections = new ArrayList<>();
        }
        this.transportationSelections.add(transportationSelectionsItem);
        return this;
    }

    /**
     * Information needed to confirm one of the available transportation options.
     *
     * @return transportationSelections
     */
    @javax.annotation.Nonnull
    public List<TransportationSelection> getTransportationSelections() {
        return transportationSelections;
    }

    public void setTransportationSelections(List<TransportationSelection> transportationSelections) {
        this.transportationSelections = transportationSelections;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ConfirmTransportationOptionsRequest confirmTransportationOptionsRequest =
                (ConfirmTransportationOptionsRequest) o;
        return Objects.equals(
                this.transportationSelections, confirmTransportationOptionsRequest.transportationSelections);
    }

    @Override
    public int hashCode() {
        return Objects.hash(transportationSelections);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ConfirmTransportationOptionsRequest {\n");
        sb.append("    transportationSelections: ")
                .append(toIndentedString(transportationSelections))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("transportationSelections");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("transportationSelections");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ConfirmTransportationOptionsRequest
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ConfirmTransportationOptionsRequest.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in ConfirmTransportationOptionsRequest is not found in the empty JSON string",
                        ConfirmTransportationOptionsRequest.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ConfirmTransportationOptionsRequest.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `ConfirmTransportationOptionsRequest` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ConfirmTransportationOptionsRequest.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the json data is an array
        if (!jsonObj.get("transportationSelections").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `transportationSelections` to be an array in the JSON string but got `%s`",
                    jsonObj.get("transportationSelections").toString()));
        }

        JsonArray jsonArraytransportationSelections = jsonObj.getAsJsonArray("transportationSelections");
        // validate the required field `transportationSelections` (array)
        for (int i = 0; i < jsonArraytransportationSelections.size(); i++) {
            TransportationSelection.validateJsonElement(jsonArraytransportationSelections.get(i));
        }
        ;
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ConfirmTransportationOptionsRequest.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ConfirmTransportationOptionsRequest' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ConfirmTransportationOptionsRequest> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ConfirmTransportationOptionsRequest.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ConfirmTransportationOptionsRequest>() {
                        @Override
                        public void write(JsonWriter out, ConfirmTransportationOptionsRequest value)
                                throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ConfirmTransportationOptionsRequest read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ConfirmTransportationOptionsRequest given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ConfirmTransportationOptionsRequest
     * @throws IOException if the JSON string is invalid with respect to ConfirmTransportationOptionsRequest
     */
    public static ConfirmTransportationOptionsRequest fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ConfirmTransportationOptionsRequest.class);
    }

    /**
     * Convert an instance of ConfirmTransportationOptionsRequest to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
