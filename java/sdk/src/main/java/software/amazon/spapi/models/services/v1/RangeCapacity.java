/*
 * Selling Partner API for Services
 * With the Services API, you can build applications that help service providers get and modify their service orders and manage their resources.
 *
 * The version of the OpenAPI document: v1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.services.v1;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Range capacity entity where each entry has a capacity type and corresponding slots. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class RangeCapacity {
    public static final String SERIALIZED_NAME_CAPACITY_TYPE = "capacityType";

    @SerializedName(SERIALIZED_NAME_CAPACITY_TYPE)
    private CapacityType capacityType;

    public static final String SERIALIZED_NAME_SLOTS = "slots";

    @SerializedName(SERIALIZED_NAME_SLOTS)
    private List<RangeSlot> slots = new ArrayList<>();

    public RangeCapacity() {}

    public RangeCapacity capacityType(CapacityType capacityType) {
        this.capacityType = capacityType;
        return this;
    }

    /**
     * Get capacityType
     *
     * @return capacityType
     */
    @javax.annotation.Nullable public CapacityType getCapacityType() {
        return capacityType;
    }

    public void setCapacityType(CapacityType capacityType) {
        this.capacityType = capacityType;
    }

    public RangeCapacity slots(List<RangeSlot> slots) {
        this.slots = slots;
        return this;
    }

    public RangeCapacity addSlotsItem(RangeSlot slotsItem) {
        if (this.slots == null) {
            this.slots = new ArrayList<>();
        }
        this.slots.add(slotsItem);
        return this;
    }

    /**
     * Array of capacity slots in range slot format.
     *
     * @return slots
     */
    @javax.annotation.Nullable public List<RangeSlot> getSlots() {
        return slots;
    }

    public void setSlots(List<RangeSlot> slots) {
        this.slots = slots;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        RangeCapacity rangeCapacity = (RangeCapacity) o;
        return Objects.equals(this.capacityType, rangeCapacity.capacityType)
                && Objects.equals(this.slots, rangeCapacity.slots);
    }

    @Override
    public int hashCode() {
        return Objects.hash(capacityType, slots);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class RangeCapacity {\n");
        sb.append("    capacityType: ").append(toIndentedString(capacityType)).append("\n");
        sb.append("    slots: ").append(toIndentedString(slots)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("capacityType");
        openapiFields.add("slots");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to RangeCapacity
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!RangeCapacity.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in RangeCapacity is not found in the empty JSON string",
                        RangeCapacity.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!RangeCapacity.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `RangeCapacity` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the optional field `capacityType`
        if (jsonObj.get("capacityType") != null && !jsonObj.get("capacityType").isJsonNull()) {
            CapacityType.validateJsonElement(jsonObj.get("capacityType"));
        }
        if (jsonObj.get("slots") != null && !jsonObj.get("slots").isJsonNull()) {
            JsonArray jsonArrayslots = jsonObj.getAsJsonArray("slots");
            if (jsonArrayslots != null) {
                // ensure the json data is an array
                if (!jsonObj.get("slots").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `slots` to be an array in the JSON string but got `%s`",
                            jsonObj.get("slots").toString()));
                }

                // validate the optional field `slots` (array)
                for (int i = 0; i < jsonArrayslots.size(); i++) {
                    RangeSlot.validateJsonElement(jsonArrayslots.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!RangeCapacity.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'RangeCapacity' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<RangeCapacity> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(RangeCapacity.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<RangeCapacity>() {
                        @Override
                        public void write(JsonWriter out, RangeCapacity value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public RangeCapacity read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of RangeCapacity given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of RangeCapacity
     * @throws IOException if the JSON string is invalid with respect to RangeCapacity
     */
    public static RangeCapacity fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, RangeCapacity.class);
    }

    /**
     * Convert an instance of RangeCapacity to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
