/*
 * Selling Partner API for Pricing
 * The Selling Partner API for Pricing helps you programmatically retrieve product pricing and offer pricing information for Amazon store products.  For more information, refer to the [Product Pricing v2022-05-01 Use Case Guide](https://developer-docs.amazon.com/sp-api/docs/product-pricing-api-v2022-05-01-use-case-guide).
 *
 * The version of the OpenAPI document: 2022-05-01
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.pricing.v2022_05_01;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Describes the segment in which the offer is featured. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class FeaturedOfferSegment {
    /** The customer membership type that makes up this segment */
    @JsonAdapter(CustomerMembershipEnum.Adapter.class)
    public enum CustomerMembershipEnum {
        PRIME("PRIME"),

        NON_PRIME("NON_PRIME"),

        DEFAULT("DEFAULT");

        private String value;

        CustomerMembershipEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static CustomerMembershipEnum fromValue(String value) {
            for (CustomerMembershipEnum b : CustomerMembershipEnum.values()) {
                if (b.value.equals(value)) {
                    return b;
                }
            }
            throw new IllegalArgumentException("Unexpected value '" + value + "'");
        }

        public static class Adapter extends TypeAdapter<CustomerMembershipEnum> {
            @Override
            public void write(final JsonWriter jsonWriter, final CustomerMembershipEnum enumeration)
                    throws IOException {
                jsonWriter.value(enumeration.getValue());
            }

            @Override
            public CustomerMembershipEnum read(final JsonReader jsonReader) throws IOException {
                String value = jsonReader.nextString();
                return CustomerMembershipEnum.fromValue(value);
            }
        }

        public static void validateJsonElement(JsonElement jsonElement) throws IOException {
            String value = jsonElement.getAsString();
            CustomerMembershipEnum.fromValue(value);
        }
    }

    public static final String SERIALIZED_NAME_CUSTOMER_MEMBERSHIP = "customerMembership";

    @SerializedName(SERIALIZED_NAME_CUSTOMER_MEMBERSHIP)
    private CustomerMembershipEnum customerMembership;

    public static final String SERIALIZED_NAME_SEGMENT_DETAILS = "segmentDetails";

    @SerializedName(SERIALIZED_NAME_SEGMENT_DETAILS)
    private SegmentDetails segmentDetails;

    public FeaturedOfferSegment() {}

    public FeaturedOfferSegment customerMembership(CustomerMembershipEnum customerMembership) {
        this.customerMembership = customerMembership;
        return this;
    }

    /**
     * The customer membership type that makes up this segment
     *
     * @return customerMembership
     */
    @javax.annotation.Nonnull
    public CustomerMembershipEnum getCustomerMembership() {
        return customerMembership;
    }

    public void setCustomerMembership(CustomerMembershipEnum customerMembership) {
        this.customerMembership = customerMembership;
    }

    public FeaturedOfferSegment segmentDetails(SegmentDetails segmentDetails) {
        this.segmentDetails = segmentDetails;
        return this;
    }

    /**
     * Get segmentDetails
     *
     * @return segmentDetails
     */
    @javax.annotation.Nonnull
    public SegmentDetails getSegmentDetails() {
        return segmentDetails;
    }

    public void setSegmentDetails(SegmentDetails segmentDetails) {
        this.segmentDetails = segmentDetails;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        FeaturedOfferSegment featuredOfferSegment = (FeaturedOfferSegment) o;
        return Objects.equals(this.customerMembership, featuredOfferSegment.customerMembership)
                && Objects.equals(this.segmentDetails, featuredOfferSegment.segmentDetails);
    }

    @Override
    public int hashCode() {
        return Objects.hash(customerMembership, segmentDetails);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class FeaturedOfferSegment {\n");
        sb.append("    customerMembership: ")
                .append(toIndentedString(customerMembership))
                .append("\n");
        sb.append("    segmentDetails: ")
                .append(toIndentedString(segmentDetails))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("customerMembership");
        openapiFields.add("segmentDetails");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("customerMembership");
        openapiRequiredFields.add("segmentDetails");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to FeaturedOfferSegment
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!FeaturedOfferSegment.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in FeaturedOfferSegment is not found in the empty JSON string",
                        FeaturedOfferSegment.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!FeaturedOfferSegment.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `FeaturedOfferSegment` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : FeaturedOfferSegment.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (!jsonObj.get("customerMembership").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `customerMembership` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("customerMembership").toString()));
        }
        // validate the required field `customerMembership`
        CustomerMembershipEnum.validateJsonElement(jsonObj.get("customerMembership"));
        // validate the required field `segmentDetails`
        SegmentDetails.validateJsonElement(jsonObj.get("segmentDetails"));
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!FeaturedOfferSegment.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'FeaturedOfferSegment' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<FeaturedOfferSegment> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(FeaturedOfferSegment.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<FeaturedOfferSegment>() {
                        @Override
                        public void write(JsonWriter out, FeaturedOfferSegment value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public FeaturedOfferSegment read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of FeaturedOfferSegment given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of FeaturedOfferSegment
     * @throws IOException if the JSON string is invalid with respect to FeaturedOfferSegment
     */
    public static FeaturedOfferSegment fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, FeaturedOfferSegment.class);
    }

    /**
     * Convert an instance of FeaturedOfferSegment to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
