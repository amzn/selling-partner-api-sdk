/*
 * Selling Partner API for Pricing
 * The Selling Partner API for Pricing helps you programmatically retrieve product pricing and offer information for Amazon Marketplace products.
 *
 * The version of the OpenAPI document: v0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.pricing.v0;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Specifies the identifiers used to uniquely identify an item. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class IdentifierType {
    public static final String SERIALIZED_NAME_MARKETPLACE_A_S_I_N = "MarketplaceASIN";

    @SerializedName(SERIALIZED_NAME_MARKETPLACE_A_S_I_N)
    private ASINIdentifier marketplaceASIN;

    public static final String SERIALIZED_NAME_SK_U_IDENTIFIER = "SKUIdentifier";

    @SerializedName(SERIALIZED_NAME_SK_U_IDENTIFIER)
    private SellerSKUIdentifier skUIdentifier;

    public IdentifierType() {}

    public IdentifierType marketplaceASIN(ASINIdentifier marketplaceASIN) {
        this.marketplaceASIN = marketplaceASIN;
        return this;
    }

    /**
     * Get marketplaceASIN
     *
     * @return marketplaceASIN
     */
    @javax.annotation.Nonnull
    public ASINIdentifier getMarketplaceASIN() {
        return marketplaceASIN;
    }

    public void setMarketplaceASIN(ASINIdentifier marketplaceASIN) {
        this.marketplaceASIN = marketplaceASIN;
    }

    public IdentifierType skUIdentifier(SellerSKUIdentifier skUIdentifier) {
        this.skUIdentifier = skUIdentifier;
        return this;
    }

    /**
     * Get skUIdentifier
     *
     * @return skUIdentifier
     */
    @javax.annotation.Nullable public SellerSKUIdentifier getSkUIdentifier() {
        return skUIdentifier;
    }

    public void setSkUIdentifier(SellerSKUIdentifier skUIdentifier) {
        this.skUIdentifier = skUIdentifier;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        IdentifierType identifierType = (IdentifierType) o;
        return Objects.equals(this.marketplaceASIN, identifierType.marketplaceASIN)
                && Objects.equals(this.skUIdentifier, identifierType.skUIdentifier);
    }

    @Override
    public int hashCode() {
        return Objects.hash(marketplaceASIN, skUIdentifier);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class IdentifierType {\n");
        sb.append("    marketplaceASIN: ")
                .append(toIndentedString(marketplaceASIN))
                .append("\n");
        sb.append("    skUIdentifier: ").append(toIndentedString(skUIdentifier)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("MarketplaceASIN");
        openapiFields.add("SKUIdentifier");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("MarketplaceASIN");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to IdentifierType
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!IdentifierType.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in IdentifierType is not found in the empty JSON string",
                        IdentifierType.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!IdentifierType.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `IdentifierType` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : IdentifierType.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `MarketplaceASIN`
        ASINIdentifier.validateJsonElement(jsonObj.get("MarketplaceASIN"));
        // validate the optional field `SKUIdentifier`
        if (jsonObj.get("SKUIdentifier") != null
                && !jsonObj.get("SKUIdentifier").isJsonNull()) {
            SellerSKUIdentifier.validateJsonElement(jsonObj.get("SKUIdentifier"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!IdentifierType.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'IdentifierType' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<IdentifierType> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(IdentifierType.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<IdentifierType>() {
                        @Override
                        public void write(JsonWriter out, IdentifierType value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public IdentifierType read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of IdentifierType given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of IdentifierType
     * @throws IOException if the JSON string is invalid with respect to IdentifierType
     */
    public static IdentifierType fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, IdentifierType.class);
    }

    /**
     * Convert an instance of IdentifierType to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
