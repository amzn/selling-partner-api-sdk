/*
 * Selling Partner APIs for Fulfillment Outbound
 * The Selling Partner API for Fulfillment Outbound lets you create applications that help a seller fulfill Multi-Channel Fulfillment orders using their inventory in Amazon's fulfillment network. You can get information on both potential and existing fulfillment orders.
 *
 * The version of the OpenAPI document: 2020-07-01
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.outbound.v2020_07_01;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/**
 * A list of fulfillment order previews, including estimated shipping weights, estimated shipping fees, and estimated
 * ship dates and arrival dates.
 */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class GetFulfillmentPreviewResult {
    public static final String SERIALIZED_NAME_FULFILLMENT_PREVIEWS = "fulfillmentPreviews";

    @SerializedName(SERIALIZED_NAME_FULFILLMENT_PREVIEWS)
    private List<FulfillmentPreview> fulfillmentPreviews = new ArrayList<>();

    public GetFulfillmentPreviewResult() {}

    public GetFulfillmentPreviewResult fulfillmentPreviews(List<FulfillmentPreview> fulfillmentPreviews) {
        this.fulfillmentPreviews = fulfillmentPreviews;
        return this;
    }

    public GetFulfillmentPreviewResult addFulfillmentPreviewsItem(FulfillmentPreview fulfillmentPreviewsItem) {
        if (this.fulfillmentPreviews == null) {
            this.fulfillmentPreviews = new ArrayList<>();
        }
        this.fulfillmentPreviews.add(fulfillmentPreviewsItem);
        return this;
    }

    /**
     * An array of fulfillment preview information.
     *
     * @return fulfillmentPreviews
     */
    @javax.annotation.Nullable public List<FulfillmentPreview> getFulfillmentPreviews() {
        return fulfillmentPreviews;
    }

    public void setFulfillmentPreviews(List<FulfillmentPreview> fulfillmentPreviews) {
        this.fulfillmentPreviews = fulfillmentPreviews;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GetFulfillmentPreviewResult getFulfillmentPreviewResult = (GetFulfillmentPreviewResult) o;
        return Objects.equals(this.fulfillmentPreviews, getFulfillmentPreviewResult.fulfillmentPreviews);
    }

    @Override
    public int hashCode() {
        return Objects.hash(fulfillmentPreviews);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class GetFulfillmentPreviewResult {\n");
        sb.append("    fulfillmentPreviews: ")
                .append(toIndentedString(fulfillmentPreviews))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("fulfillmentPreviews");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GetFulfillmentPreviewResult
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!GetFulfillmentPreviewResult.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in GetFulfillmentPreviewResult is not found in the empty JSON string",
                        GetFulfillmentPreviewResult.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!GetFulfillmentPreviewResult.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `GetFulfillmentPreviewResult` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("fulfillmentPreviews") != null
                && !jsonObj.get("fulfillmentPreviews").isJsonNull()) {
            JsonArray jsonArrayfulfillmentPreviews = jsonObj.getAsJsonArray("fulfillmentPreviews");
            if (jsonArrayfulfillmentPreviews != null) {
                // ensure the json data is an array
                if (!jsonObj.get("fulfillmentPreviews").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `fulfillmentPreviews` to be an array in the JSON string but got `%s`",
                            jsonObj.get("fulfillmentPreviews").toString()));
                }

                // validate the optional field `fulfillmentPreviews` (array)
                for (int i = 0; i < jsonArrayfulfillmentPreviews.size(); i++) {
                    FulfillmentPreview.validateJsonElement(jsonArrayfulfillmentPreviews.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GetFulfillmentPreviewResult.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GetFulfillmentPreviewResult' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GetFulfillmentPreviewResult> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(GetFulfillmentPreviewResult.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<GetFulfillmentPreviewResult>() {
                        @Override
                        public void write(JsonWriter out, GetFulfillmentPreviewResult value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public GetFulfillmentPreviewResult read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of GetFulfillmentPreviewResult given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GetFulfillmentPreviewResult
     * @throws IOException if the JSON string is invalid with respect to GetFulfillmentPreviewResult
     */
    public static GetFulfillmentPreviewResult fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GetFulfillmentPreviewResult.class);
    }

    /**
     * Convert an instance of GetFulfillmentPreviewResult to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
