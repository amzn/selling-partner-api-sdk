/*
 * The Selling Partner API for FBA inbound operations.
 * The Selling Partner API for Fulfillment By Amazon (FBA) Inbound. The FBA Inbound API enables building inbound workflows to create, manage, and send shipments into Amazon's fulfillment network. The API has interoperability with the Send-to-Amazon user interface.
 *
 * The version of the OpenAPI document: 2024-03-20
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.inbound.v2024_03_20;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The &#x60;listDeliveryWindowOptions&#x60; response. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class ListDeliveryWindowOptionsResponse {
    public static final String SERIALIZED_NAME_DELIVERY_WINDOW_OPTIONS = "deliveryWindowOptions";

    @SerializedName(SERIALIZED_NAME_DELIVERY_WINDOW_OPTIONS)
    private List<DeliveryWindowOption> deliveryWindowOptions = new ArrayList<>();

    public static final String SERIALIZED_NAME_PAGINATION = "pagination";

    @SerializedName(SERIALIZED_NAME_PAGINATION)
    private Pagination pagination;

    public ListDeliveryWindowOptionsResponse() {}

    public ListDeliveryWindowOptionsResponse deliveryWindowOptions(List<DeliveryWindowOption> deliveryWindowOptions) {
        this.deliveryWindowOptions = deliveryWindowOptions;
        return this;
    }

    public ListDeliveryWindowOptionsResponse addDeliveryWindowOptionsItem(
            DeliveryWindowOption deliveryWindowOptionsItem) {
        if (this.deliveryWindowOptions == null) {
            this.deliveryWindowOptions = new ArrayList<>();
        }
        this.deliveryWindowOptions.add(deliveryWindowOptionsItem);
        return this;
    }

    /**
     * Delivery window options generated for the placement option.
     *
     * @return deliveryWindowOptions
     */
    @javax.annotation.Nonnull
    public List<DeliveryWindowOption> getDeliveryWindowOptions() {
        return deliveryWindowOptions;
    }

    public void setDeliveryWindowOptions(List<DeliveryWindowOption> deliveryWindowOptions) {
        this.deliveryWindowOptions = deliveryWindowOptions;
    }

    public ListDeliveryWindowOptionsResponse pagination(Pagination pagination) {
        this.pagination = pagination;
        return this;
    }

    /**
     * Get pagination
     *
     * @return pagination
     */
    @javax.annotation.Nullable public Pagination getPagination() {
        return pagination;
    }

    public void setPagination(Pagination pagination) {
        this.pagination = pagination;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ListDeliveryWindowOptionsResponse listDeliveryWindowOptionsResponse = (ListDeliveryWindowOptionsResponse) o;
        return Objects.equals(this.deliveryWindowOptions, listDeliveryWindowOptionsResponse.deliveryWindowOptions)
                && Objects.equals(this.pagination, listDeliveryWindowOptionsResponse.pagination);
    }

    @Override
    public int hashCode() {
        return Objects.hash(deliveryWindowOptions, pagination);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ListDeliveryWindowOptionsResponse {\n");
        sb.append("    deliveryWindowOptions: ")
                .append(toIndentedString(deliveryWindowOptions))
                .append("\n");
        sb.append("    pagination: ").append(toIndentedString(pagination)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("deliveryWindowOptions");
        openapiFields.add("pagination");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("deliveryWindowOptions");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ListDeliveryWindowOptionsResponse
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ListDeliveryWindowOptionsResponse.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in ListDeliveryWindowOptionsResponse is not found in the empty JSON string",
                        ListDeliveryWindowOptionsResponse.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ListDeliveryWindowOptionsResponse.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `ListDeliveryWindowOptionsResponse` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : ListDeliveryWindowOptionsResponse.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // ensure the json data is an array
        if (!jsonObj.get("deliveryWindowOptions").isJsonArray()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `deliveryWindowOptions` to be an array in the JSON string but got `%s`",
                    jsonObj.get("deliveryWindowOptions").toString()));
        }

        JsonArray jsonArraydeliveryWindowOptions = jsonObj.getAsJsonArray("deliveryWindowOptions");
        // validate the required field `deliveryWindowOptions` (array)
        for (int i = 0; i < jsonArraydeliveryWindowOptions.size(); i++) {
            DeliveryWindowOption.validateJsonElement(jsonArraydeliveryWindowOptions.get(i));
        }
        ;
        // validate the optional field `pagination`
        if (jsonObj.get("pagination") != null && !jsonObj.get("pagination").isJsonNull()) {
            Pagination.validateJsonElement(jsonObj.get("pagination"));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ListDeliveryWindowOptionsResponse.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ListDeliveryWindowOptionsResponse' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ListDeliveryWindowOptionsResponse> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ListDeliveryWindowOptionsResponse.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ListDeliveryWindowOptionsResponse>() {
                        @Override
                        public void write(JsonWriter out, ListDeliveryWindowOptionsResponse value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ListDeliveryWindowOptionsResponse read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ListDeliveryWindowOptionsResponse given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ListDeliveryWindowOptionsResponse
     * @throws IOException if the JSON string is invalid with respect to ListDeliveryWindowOptionsResponse
     */
    public static ListDeliveryWindowOptionsResponse fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ListDeliveryWindowOptionsResponse.class);
    }

    /**
     * Convert an instance of ListDeliveryWindowOptionsResponse to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
