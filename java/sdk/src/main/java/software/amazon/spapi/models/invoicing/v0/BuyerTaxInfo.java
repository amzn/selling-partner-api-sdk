/*
 * Selling Partner API for Shipment Invoicing
 * The Selling Partner API for Shipment Invoicing helps you programmatically retrieve shipment invoice information in the Brazil marketplace for a selling partner’s Fulfillment by Amazon (FBA) orders.
 *
 * The version of the OpenAPI document: v0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.invoicing.v0;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Tax information about the buyer. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class BuyerTaxInfo {
    public static final String SERIALIZED_NAME_COMPANY_LEGAL_NAME = "CompanyLegalName";

    @SerializedName(SERIALIZED_NAME_COMPANY_LEGAL_NAME)
    private String companyLegalName;

    public static final String SERIALIZED_NAME_TAXING_REGION = "TaxingRegion";

    @SerializedName(SERIALIZED_NAME_TAXING_REGION)
    private String taxingRegion;

    public static final String SERIALIZED_NAME_TAX_CLASSIFICATIONS = "TaxClassifications";

    @SerializedName(SERIALIZED_NAME_TAX_CLASSIFICATIONS)
    private List<TaxClassification> taxClassifications = new ArrayList<>();

    public BuyerTaxInfo() {}

    public BuyerTaxInfo companyLegalName(String companyLegalName) {
        this.companyLegalName = companyLegalName;
        return this;
    }

    /**
     * The legal name of the company.
     *
     * @return companyLegalName
     */
    @javax.annotation.Nullable public String getCompanyLegalName() {
        return companyLegalName;
    }

    public void setCompanyLegalName(String companyLegalName) {
        this.companyLegalName = companyLegalName;
    }

    public BuyerTaxInfo taxingRegion(String taxingRegion) {
        this.taxingRegion = taxingRegion;
        return this;
    }

    /**
     * The country or region imposing the tax.
     *
     * @return taxingRegion
     */
    @javax.annotation.Nullable public String getTaxingRegion() {
        return taxingRegion;
    }

    public void setTaxingRegion(String taxingRegion) {
        this.taxingRegion = taxingRegion;
    }

    public BuyerTaxInfo taxClassifications(List<TaxClassification> taxClassifications) {
        this.taxClassifications = taxClassifications;
        return this;
    }

    public BuyerTaxInfo addTaxClassificationsItem(TaxClassification taxClassificationsItem) {
        if (this.taxClassifications == null) {
            this.taxClassifications = new ArrayList<>();
        }
        this.taxClassifications.add(taxClassificationsItem);
        return this;
    }

    /**
     * The list of tax classifications.
     *
     * @return taxClassifications
     */
    @javax.annotation.Nullable public List<TaxClassification> getTaxClassifications() {
        return taxClassifications;
    }

    public void setTaxClassifications(List<TaxClassification> taxClassifications) {
        this.taxClassifications = taxClassifications;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        BuyerTaxInfo buyerTaxInfo = (BuyerTaxInfo) o;
        return Objects.equals(this.companyLegalName, buyerTaxInfo.companyLegalName)
                && Objects.equals(this.taxingRegion, buyerTaxInfo.taxingRegion)
                && Objects.equals(this.taxClassifications, buyerTaxInfo.taxClassifications);
    }

    @Override
    public int hashCode() {
        return Objects.hash(companyLegalName, taxingRegion, taxClassifications);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class BuyerTaxInfo {\n");
        sb.append("    companyLegalName: ")
                .append(toIndentedString(companyLegalName))
                .append("\n");
        sb.append("    taxingRegion: ").append(toIndentedString(taxingRegion)).append("\n");
        sb.append("    taxClassifications: ")
                .append(toIndentedString(taxClassifications))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("CompanyLegalName");
        openapiFields.add("TaxingRegion");
        openapiFields.add("TaxClassifications");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to BuyerTaxInfo
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!BuyerTaxInfo.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in BuyerTaxInfo is not found in the empty JSON string",
                        BuyerTaxInfo.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!BuyerTaxInfo.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `BuyerTaxInfo` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("CompanyLegalName") != null
                        && !jsonObj.get("CompanyLegalName").isJsonNull())
                && !jsonObj.get("CompanyLegalName").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `CompanyLegalName` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("CompanyLegalName").toString()));
        }
        if ((jsonObj.get("TaxingRegion") != null && !jsonObj.get("TaxingRegion").isJsonNull())
                && !jsonObj.get("TaxingRegion").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `TaxingRegion` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("TaxingRegion").toString()));
        }
        if (jsonObj.get("TaxClassifications") != null
                && !jsonObj.get("TaxClassifications").isJsonNull()) {
            JsonArray jsonArraytaxClassifications = jsonObj.getAsJsonArray("TaxClassifications");
            if (jsonArraytaxClassifications != null) {
                // ensure the json data is an array
                if (!jsonObj.get("TaxClassifications").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `TaxClassifications` to be an array in the JSON string but got `%s`",
                            jsonObj.get("TaxClassifications").toString()));
                }

                // validate the optional field `TaxClassifications` (array)
                for (int i = 0; i < jsonArraytaxClassifications.size(); i++) {
                    TaxClassification.validateJsonElement(jsonArraytaxClassifications.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!BuyerTaxInfo.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'BuyerTaxInfo' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<BuyerTaxInfo> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(BuyerTaxInfo.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<BuyerTaxInfo>() {
                        @Override
                        public void write(JsonWriter out, BuyerTaxInfo value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public BuyerTaxInfo read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of BuyerTaxInfo given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of BuyerTaxInfo
     * @throws IOException if the JSON string is invalid with respect to BuyerTaxInfo
     */
    public static BuyerTaxInfo fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, BuyerTaxInfo.class);
    }

    /**
     * Convert an instance of BuyerTaxInfo to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
