/*
 * Selling Partner API for Retail Procurement Shipments
 * The Selling Partner API for Retail Procurement Shipments provides programmatic access to retail shipping data for vendors.
 *
 * The version of the OpenAPI document: v1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.vendor.shipments.v1;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The request schema for the SubmitShipmentConfirmations operation. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class SubmitShipmentConfirmationsRequest {
    public static final String SERIALIZED_NAME_SHIPMENT_CONFIRMATIONS = "shipmentConfirmations";

    @SerializedName(SERIALIZED_NAME_SHIPMENT_CONFIRMATIONS)
    private List<ShipmentConfirmation> shipmentConfirmations = new ArrayList<>();

    public SubmitShipmentConfirmationsRequest() {}

    public SubmitShipmentConfirmationsRequest shipmentConfirmations(List<ShipmentConfirmation> shipmentConfirmations) {
        this.shipmentConfirmations = shipmentConfirmations;
        return this;
    }

    public SubmitShipmentConfirmationsRequest addShipmentConfirmationsItem(
            ShipmentConfirmation shipmentConfirmationsItem) {
        if (this.shipmentConfirmations == null) {
            this.shipmentConfirmations = new ArrayList<>();
        }
        this.shipmentConfirmations.add(shipmentConfirmationsItem);
        return this;
    }

    /**
     * A list of one or more shipment confirmations.
     *
     * @return shipmentConfirmations
     */
    @javax.annotation.Nullable public List<ShipmentConfirmation> getShipmentConfirmations() {
        return shipmentConfirmations;
    }

    public void setShipmentConfirmations(List<ShipmentConfirmation> shipmentConfirmations) {
        this.shipmentConfirmations = shipmentConfirmations;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        SubmitShipmentConfirmationsRequest submitShipmentConfirmationsRequest = (SubmitShipmentConfirmationsRequest) o;
        return Objects.equals(this.shipmentConfirmations, submitShipmentConfirmationsRequest.shipmentConfirmations);
    }

    @Override
    public int hashCode() {
        return Objects.hash(shipmentConfirmations);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class SubmitShipmentConfirmationsRequest {\n");
        sb.append("    shipmentConfirmations: ")
                .append(toIndentedString(shipmentConfirmations))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("shipmentConfirmations");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to SubmitShipmentConfirmationsRequest
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!SubmitShipmentConfirmationsRequest.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in SubmitShipmentConfirmationsRequest is not found in the empty JSON string",
                        SubmitShipmentConfirmationsRequest.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!SubmitShipmentConfirmationsRequest.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `SubmitShipmentConfirmationsRequest` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("shipmentConfirmations") != null
                && !jsonObj.get("shipmentConfirmations").isJsonNull()) {
            JsonArray jsonArrayshipmentConfirmations = jsonObj.getAsJsonArray("shipmentConfirmations");
            if (jsonArrayshipmentConfirmations != null) {
                // ensure the json data is an array
                if (!jsonObj.get("shipmentConfirmations").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `shipmentConfirmations` to be an array in the JSON string but got `%s`",
                            jsonObj.get("shipmentConfirmations").toString()));
                }

                // validate the optional field `shipmentConfirmations` (array)
                for (int i = 0; i < jsonArrayshipmentConfirmations.size(); i++) {
                    ShipmentConfirmation.validateJsonElement(jsonArrayshipmentConfirmations.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SubmitShipmentConfirmationsRequest.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SubmitShipmentConfirmationsRequest' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SubmitShipmentConfirmationsRequest> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(SubmitShipmentConfirmationsRequest.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<SubmitShipmentConfirmationsRequest>() {
                        @Override
                        public void write(JsonWriter out, SubmitShipmentConfirmationsRequest value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public SubmitShipmentConfirmationsRequest read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of SubmitShipmentConfirmationsRequest given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of SubmitShipmentConfirmationsRequest
     * @throws IOException if the JSON string is invalid with respect to SubmitShipmentConfirmationsRequest
     */
    public static SubmitShipmentConfirmationsRequest fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, SubmitShipmentConfirmationsRequest.class);
    }

    /**
     * Convert an instance of SubmitShipmentConfirmationsRequest to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
