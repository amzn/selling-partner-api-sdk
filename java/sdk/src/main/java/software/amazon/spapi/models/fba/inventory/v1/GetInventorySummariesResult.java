/*
 * Selling Partner API for FBA Inventory
 * The Selling Partner API for FBA Inventory lets you programmatically retrieve information about inventory in Amazon's fulfillment network.
 *
 * The version of the OpenAPI document: v1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fba.inventory.v1;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The payload schema for the getInventorySummaries operation. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class GetInventorySummariesResult {
    public static final String SERIALIZED_NAME_GRANULARITY = "granularity";

    @SerializedName(SERIALIZED_NAME_GRANULARITY)
    private Granularity granularity;

    public static final String SERIALIZED_NAME_INVENTORY_SUMMARIES = "inventorySummaries";

    @SerializedName(SERIALIZED_NAME_INVENTORY_SUMMARIES)
    private InventorySummaries inventorySummaries = new ArrayList<>();

    public GetInventorySummariesResult() {}

    public GetInventorySummariesResult granularity(Granularity granularity) {
        this.granularity = granularity;
        return this;
    }

    /**
     * Get granularity
     *
     * @return granularity
     */
    @javax.annotation.Nonnull
    public Granularity getGranularity() {
        return granularity;
    }

    public void setGranularity(Granularity granularity) {
        this.granularity = granularity;
    }

    public GetInventorySummariesResult inventorySummaries(InventorySummaries inventorySummaries) {
        this.inventorySummaries = inventorySummaries;
        return this;
    }

    /**
     * Get inventorySummaries
     *
     * @return inventorySummaries
     */
    @javax.annotation.Nonnull
    public InventorySummaries getInventorySummaries() {
        return inventorySummaries;
    }

    public void setInventorySummaries(InventorySummaries inventorySummaries) {
        this.inventorySummaries = inventorySummaries;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        GetInventorySummariesResult getInventorySummariesResult = (GetInventorySummariesResult) o;
        return Objects.equals(this.granularity, getInventorySummariesResult.granularity)
                && Objects.equals(this.inventorySummaries, getInventorySummariesResult.inventorySummaries);
    }

    @Override
    public int hashCode() {
        return Objects.hash(granularity, inventorySummaries);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class GetInventorySummariesResult {\n");
        sb.append("    granularity: ").append(toIndentedString(granularity)).append("\n");
        sb.append("    inventorySummaries: ")
                .append(toIndentedString(inventorySummaries))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("granularity");
        openapiFields.add("inventorySummaries");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("granularity");
        openapiRequiredFields.add("inventorySummaries");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to GetInventorySummariesResult
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!GetInventorySummariesResult.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in GetInventorySummariesResult is not found in the empty JSON string",
                        GetInventorySummariesResult.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!GetInventorySummariesResult.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `GetInventorySummariesResult` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : GetInventorySummariesResult.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `granularity`
        Granularity.validateJsonElement(jsonObj.get("granularity"));
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!GetInventorySummariesResult.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'GetInventorySummariesResult' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<GetInventorySummariesResult> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(GetInventorySummariesResult.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<GetInventorySummariesResult>() {
                        @Override
                        public void write(JsonWriter out, GetInventorySummariesResult value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public GetInventorySummariesResult read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of GetInventorySummariesResult given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of GetInventorySummariesResult
     * @throws IOException if the JSON string is invalid with respect to GetInventorySummariesResult
     */
    public static GetInventorySummariesResult fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, GetInventorySummariesResult.class);
    }

    /**
     * Convert an instance of GetInventorySummariesResult to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
