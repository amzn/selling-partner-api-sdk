/*
 * The Selling Partner API for FBA inbound operations.
 * The Selling Partner API for Fulfillment By Amazon (FBA) Inbound. The FBA Inbound API enables building inbound workflows to create, manage, and send shipments into Amazon's fulfillment network. The API has interoperability with the Send-to-Amazon user interface.
 *
 * The version of the OpenAPI document: 2024-03-20
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.fulfillment.inbound.v2024_03_20;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.math.BigDecimal;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Appointment details for carrier pickup or fulfillment center appointments. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class SelfShipAppointmentDetails {
    public static final String SERIALIZED_NAME_APPOINTMENT_ID = "appointmentId";

    @SerializedName(SERIALIZED_NAME_APPOINTMENT_ID)
    private BigDecimal appointmentId;

    public static final String SERIALIZED_NAME_APPOINTMENT_SLOT_TIME = "appointmentSlotTime";

    @SerializedName(SERIALIZED_NAME_APPOINTMENT_SLOT_TIME)
    private AppointmentSlotTime appointmentSlotTime;

    public static final String SERIALIZED_NAME_APPOINTMENT_STATUS = "appointmentStatus";

    @SerializedName(SERIALIZED_NAME_APPOINTMENT_STATUS)
    private String appointmentStatus;

    public SelfShipAppointmentDetails() {}

    public SelfShipAppointmentDetails appointmentId(BigDecimal appointmentId) {
        this.appointmentId = appointmentId;
        return this;
    }

    /**
     * Identifier for appointment.
     *
     * @return appointmentId
     */
    @javax.annotation.Nullable public BigDecimal getAppointmentId() {
        return appointmentId;
    }

    public void setAppointmentId(BigDecimal appointmentId) {
        this.appointmentId = appointmentId;
    }

    public SelfShipAppointmentDetails appointmentSlotTime(AppointmentSlotTime appointmentSlotTime) {
        this.appointmentSlotTime = appointmentSlotTime;
        return this;
    }

    /**
     * Get appointmentSlotTime
     *
     * @return appointmentSlotTime
     */
    @javax.annotation.Nullable public AppointmentSlotTime getAppointmentSlotTime() {
        return appointmentSlotTime;
    }

    public void setAppointmentSlotTime(AppointmentSlotTime appointmentSlotTime) {
        this.appointmentSlotTime = appointmentSlotTime;
    }

    public SelfShipAppointmentDetails appointmentStatus(String appointmentStatus) {
        this.appointmentStatus = appointmentStatus;
        return this;
    }

    /**
     * Status of the appointment.
     *
     * @return appointmentStatus
     */
    @javax.annotation.Nullable public String getAppointmentStatus() {
        return appointmentStatus;
    }

    public void setAppointmentStatus(String appointmentStatus) {
        this.appointmentStatus = appointmentStatus;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        SelfShipAppointmentDetails selfShipAppointmentDetails = (SelfShipAppointmentDetails) o;
        return Objects.equals(this.appointmentId, selfShipAppointmentDetails.appointmentId)
                && Objects.equals(this.appointmentSlotTime, selfShipAppointmentDetails.appointmentSlotTime)
                && Objects.equals(this.appointmentStatus, selfShipAppointmentDetails.appointmentStatus);
    }

    @Override
    public int hashCode() {
        return Objects.hash(appointmentId, appointmentSlotTime, appointmentStatus);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class SelfShipAppointmentDetails {\n");
        sb.append("    appointmentId: ").append(toIndentedString(appointmentId)).append("\n");
        sb.append("    appointmentSlotTime: ")
                .append(toIndentedString(appointmentSlotTime))
                .append("\n");
        sb.append("    appointmentStatus: ")
                .append(toIndentedString(appointmentStatus))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("appointmentId");
        openapiFields.add("appointmentSlotTime");
        openapiFields.add("appointmentStatus");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to SelfShipAppointmentDetails
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!SelfShipAppointmentDetails.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in SelfShipAppointmentDetails is not found in the empty JSON string",
                        SelfShipAppointmentDetails.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!SelfShipAppointmentDetails.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `SelfShipAppointmentDetails` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the optional field `appointmentSlotTime`
        if (jsonObj.get("appointmentSlotTime") != null
                && !jsonObj.get("appointmentSlotTime").isJsonNull()) {
            AppointmentSlotTime.validateJsonElement(jsonObj.get("appointmentSlotTime"));
        }
        if ((jsonObj.get("appointmentStatus") != null
                        && !jsonObj.get("appointmentStatus").isJsonNull())
                && !jsonObj.get("appointmentStatus").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `appointmentStatus` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("appointmentStatus").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SelfShipAppointmentDetails.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SelfShipAppointmentDetails' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SelfShipAppointmentDetails> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(SelfShipAppointmentDetails.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<SelfShipAppointmentDetails>() {
                        @Override
                        public void write(JsonWriter out, SelfShipAppointmentDetails value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public SelfShipAppointmentDetails read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of SelfShipAppointmentDetails given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of SelfShipAppointmentDetails
     * @throws IOException if the JSON string is invalid with respect to SelfShipAppointmentDetails
     */
    public static SelfShipAppointmentDetails fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, SelfShipAppointmentDetails.class);
    }

    /**
     * Convert an instance of SelfShipAppointmentDetails to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
