/*
 * Selling Partner API for Services
 * With the Services API, you can build applications that help service providers get and modify their service orders and manage their resources.
 *
 * The version of the OpenAPI document: v1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.services.v1;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Information about the service job provider. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class ServiceJobProvider {
    public static final String SERIALIZED_NAME_SERVICE_JOB_PROVIDER_ID = "serviceJobProviderId";

    @SerializedName(SERIALIZED_NAME_SERVICE_JOB_PROVIDER_ID)
    private String serviceJobProviderId;

    public ServiceJobProvider() {}

    public ServiceJobProvider serviceJobProviderId(String serviceJobProviderId) {
        this.serviceJobProviderId = serviceJobProviderId;
        return this;
    }

    /**
     * The identifier of the service job provider.
     *
     * @return serviceJobProviderId
     */
    @javax.annotation.Nullable public String getServiceJobProviderId() {
        return serviceJobProviderId;
    }

    public void setServiceJobProviderId(String serviceJobProviderId) {
        this.serviceJobProviderId = serviceJobProviderId;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        ServiceJobProvider serviceJobProvider = (ServiceJobProvider) o;
        return Objects.equals(this.serviceJobProviderId, serviceJobProvider.serviceJobProviderId);
    }

    @Override
    public int hashCode() {
        return Objects.hash(serviceJobProviderId);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class ServiceJobProvider {\n");
        sb.append("    serviceJobProviderId: ")
                .append(toIndentedString(serviceJobProviderId))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("serviceJobProviderId");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ServiceJobProvider
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!ServiceJobProvider.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in ServiceJobProvider is not found in the empty JSON string",
                        ServiceJobProvider.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!ServiceJobProvider.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `ServiceJobProvider` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if ((jsonObj.get("serviceJobProviderId") != null
                        && !jsonObj.get("serviceJobProviderId").isJsonNull())
                && !jsonObj.get("serviceJobProviderId").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `serviceJobProviderId` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("serviceJobProviderId").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ServiceJobProvider.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ServiceJobProvider' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ServiceJobProvider> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(ServiceJobProvider.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<ServiceJobProvider>() {
                        @Override
                        public void write(JsonWriter out, ServiceJobProvider value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public ServiceJobProvider read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of ServiceJobProvider given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ServiceJobProvider
     * @throws IOException if the JSON string is invalid with respect to ServiceJobProvider
     */
    public static ServiceJobProvider fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ServiceJobProvider.class);
    }

    /**
     * Convert an instance of ServiceJobProvider to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
