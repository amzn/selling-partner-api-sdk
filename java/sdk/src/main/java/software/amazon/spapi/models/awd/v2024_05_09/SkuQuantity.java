/*
 * The Selling Partner API for Amazon Warehousing and Distribution
 * The Selling Partner API for Amazon Warehousing and Distribution (AWD) provides programmatic access to information about AWD shipments and inventory.
 *
 * The version of the OpenAPI document: 2024-05-09
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.awd.v2024_05_09;

import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Quantity details for a SKU as part of a shipment */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class SkuQuantity {
    public static final String SERIALIZED_NAME_EXPECTED_QUANTITY = "expectedQuantity";

    @SerializedName(SERIALIZED_NAME_EXPECTED_QUANTITY)
    private InventoryQuantity expectedQuantity;

    public static final String SERIALIZED_NAME_RECEIVED_QUANTITY = "receivedQuantity";

    @SerializedName(SERIALIZED_NAME_RECEIVED_QUANTITY)
    private InventoryQuantity receivedQuantity;

    public static final String SERIALIZED_NAME_SKU = "sku";

    @SerializedName(SERIALIZED_NAME_SKU)
    private String sku;

    public SkuQuantity() {}

    public SkuQuantity expectedQuantity(InventoryQuantity expectedQuantity) {
        this.expectedQuantity = expectedQuantity;
        return this;
    }

    /**
     * Get expectedQuantity
     *
     * @return expectedQuantity
     */
    @javax.annotation.Nonnull
    public InventoryQuantity getExpectedQuantity() {
        return expectedQuantity;
    }

    public void setExpectedQuantity(InventoryQuantity expectedQuantity) {
        this.expectedQuantity = expectedQuantity;
    }

    public SkuQuantity receivedQuantity(InventoryQuantity receivedQuantity) {
        this.receivedQuantity = receivedQuantity;
        return this;
    }

    /**
     * Get receivedQuantity
     *
     * @return receivedQuantity
     */
    @javax.annotation.Nullable public InventoryQuantity getReceivedQuantity() {
        return receivedQuantity;
    }

    public void setReceivedQuantity(InventoryQuantity receivedQuantity) {
        this.receivedQuantity = receivedQuantity;
    }

    public SkuQuantity sku(String sku) {
        this.sku = sku;
        return this;
    }

    /**
     * The merchant stock keeping unit
     *
     * @return sku
     */
    @javax.annotation.Nonnull
    public String getSku() {
        return sku;
    }

    public void setSku(String sku) {
        this.sku = sku;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        SkuQuantity skuQuantity = (SkuQuantity) o;
        return Objects.equals(this.expectedQuantity, skuQuantity.expectedQuantity)
                && Objects.equals(this.receivedQuantity, skuQuantity.receivedQuantity)
                && Objects.equals(this.sku, skuQuantity.sku);
    }

    @Override
    public int hashCode() {
        return Objects.hash(expectedQuantity, receivedQuantity, sku);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class SkuQuantity {\n");
        sb.append("    expectedQuantity: ")
                .append(toIndentedString(expectedQuantity))
                .append("\n");
        sb.append("    receivedQuantity: ")
                .append(toIndentedString(receivedQuantity))
                .append("\n");
        sb.append("    sku: ").append(toIndentedString(sku)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("expectedQuantity");
        openapiFields.add("receivedQuantity");
        openapiFields.add("sku");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
        openapiRequiredFields.add("expectedQuantity");
        openapiRequiredFields.add("sku");
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to SkuQuantity
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!SkuQuantity.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in SkuQuantity is not found in the empty JSON string",
                        SkuQuantity.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!SkuQuantity.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `SkuQuantity` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }

        // check to make sure all required properties/fields are present in the JSON string
        for (String requiredField : SkuQuantity.openapiRequiredFields) {
            if (jsonElement.getAsJsonObject().get(requiredField) == null) {
                throw new IllegalArgumentException(String.format(
                        "The required field `%s` is not found in the JSON string: %s",
                        requiredField, jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the required field `expectedQuantity`
        InventoryQuantity.validateJsonElement(jsonObj.get("expectedQuantity"));
        // validate the optional field `receivedQuantity`
        if (jsonObj.get("receivedQuantity") != null
                && !jsonObj.get("receivedQuantity").isJsonNull()) {
            InventoryQuantity.validateJsonElement(jsonObj.get("receivedQuantity"));
        }
        if (!jsonObj.get("sku").isJsonPrimitive()) {
            throw new IllegalArgumentException(String.format(
                    "Expected the field `sku` to be a primitive type in the JSON string but got `%s`",
                    jsonObj.get("sku").toString()));
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SkuQuantity.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SkuQuantity' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SkuQuantity> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(SkuQuantity.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<SkuQuantity>() {
                        @Override
                        public void write(JsonWriter out, SkuQuantity value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public SkuQuantity read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of SkuQuantity given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of SkuQuantity
     * @throws IOException if the JSON string is invalid with respect to SkuQuantity
     */
    public static SkuQuantity fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, SkuQuantity.class);
    }

    /**
     * Convert an instance of SkuQuantity to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
