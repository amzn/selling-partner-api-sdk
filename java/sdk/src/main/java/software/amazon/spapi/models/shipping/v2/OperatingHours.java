/*
 * Amazon Shipping API
 * The Amazon Shipping API is designed to support outbound shipping use cases both for orders originating on Amazon-owned marketplaces as well as external channels/marketplaces. With these APIs, you can request shipping rates, create shipments, cancel shipments, and track shipments.
 *
 * The version of the OpenAPI document: v2
 * Contact: swa-api-core@amazon.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.shipping.v2;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** The hours in which the access point shall remain operational */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class OperatingHours {
    public static final String SERIALIZED_NAME_CLOSING_TIME = "closingTime";

    @SerializedName(SERIALIZED_NAME_CLOSING_TIME)
    private TimeOfDay closingTime;

    public static final String SERIALIZED_NAME_OPENING_TIME = "openingTime";

    @SerializedName(SERIALIZED_NAME_OPENING_TIME)
    private TimeOfDay openingTime;

    public static final String SERIALIZED_NAME_MID_DAY_CLOSURES = "midDayClosures";

    @SerializedName(SERIALIZED_NAME_MID_DAY_CLOSURES)
    private List<TimeOfDay> midDayClosures = new ArrayList<>();

    public OperatingHours() {}

    public OperatingHours closingTime(TimeOfDay closingTime) {
        this.closingTime = closingTime;
        return this;
    }

    /**
     * Get closingTime
     *
     * @return closingTime
     */
    @javax.annotation.Nullable public TimeOfDay getClosingTime() {
        return closingTime;
    }

    public void setClosingTime(TimeOfDay closingTime) {
        this.closingTime = closingTime;
    }

    public OperatingHours openingTime(TimeOfDay openingTime) {
        this.openingTime = openingTime;
        return this;
    }

    /**
     * Get openingTime
     *
     * @return openingTime
     */
    @javax.annotation.Nullable public TimeOfDay getOpeningTime() {
        return openingTime;
    }

    public void setOpeningTime(TimeOfDay openingTime) {
        this.openingTime = openingTime;
    }

    public OperatingHours midDayClosures(List<TimeOfDay> midDayClosures) {
        this.midDayClosures = midDayClosures;
        return this;
    }

    public OperatingHours addMidDayClosuresItem(TimeOfDay midDayClosuresItem) {
        if (this.midDayClosures == null) {
            this.midDayClosures = new ArrayList<>();
        }
        this.midDayClosures.add(midDayClosuresItem);
        return this;
    }

    /**
     * midDayClosures operating hours array
     *
     * @return midDayClosures
     */
    @javax.annotation.Nullable public List<TimeOfDay> getMidDayClosures() {
        return midDayClosures;
    }

    public void setMidDayClosures(List<TimeOfDay> midDayClosures) {
        this.midDayClosures = midDayClosures;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OperatingHours operatingHours = (OperatingHours) o;
        return Objects.equals(this.closingTime, operatingHours.closingTime)
                && Objects.equals(this.openingTime, operatingHours.openingTime)
                && Objects.equals(this.midDayClosures, operatingHours.midDayClosures);
    }

    @Override
    public int hashCode() {
        return Objects.hash(closingTime, openingTime, midDayClosures);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class OperatingHours {\n");
        sb.append("    closingTime: ").append(toIndentedString(closingTime)).append("\n");
        sb.append("    openingTime: ").append(toIndentedString(openingTime)).append("\n");
        sb.append("    midDayClosures: ")
                .append(toIndentedString(midDayClosures))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("closingTime");
        openapiFields.add("openingTime");
        openapiFields.add("midDayClosures");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to OperatingHours
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!OperatingHours.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in OperatingHours is not found in the empty JSON string",
                        OperatingHours.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!OperatingHours.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `OperatingHours` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the optional field `closingTime`
        if (jsonObj.get("closingTime") != null && !jsonObj.get("closingTime").isJsonNull()) {
            TimeOfDay.validateJsonElement(jsonObj.get("closingTime"));
        }
        // validate the optional field `openingTime`
        if (jsonObj.get("openingTime") != null && !jsonObj.get("openingTime").isJsonNull()) {
            TimeOfDay.validateJsonElement(jsonObj.get("openingTime"));
        }
        if (jsonObj.get("midDayClosures") != null
                && !jsonObj.get("midDayClosures").isJsonNull()) {
            JsonArray jsonArraymidDayClosures = jsonObj.getAsJsonArray("midDayClosures");
            if (jsonArraymidDayClosures != null) {
                // ensure the json data is an array
                if (!jsonObj.get("midDayClosures").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `midDayClosures` to be an array in the JSON string but got `%s`",
                            jsonObj.get("midDayClosures").toString()));
                }

                // validate the optional field `midDayClosures` (array)
                for (int i = 0; i < jsonArraymidDayClosures.size(); i++) {
                    TimeOfDay.validateJsonElement(jsonArraymidDayClosures.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!OperatingHours.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'OperatingHours' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<OperatingHours> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(OperatingHours.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<OperatingHours>() {
                        @Override
                        public void write(JsonWriter out, OperatingHours value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public OperatingHours read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of OperatingHours given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of OperatingHours
     * @throws IOException if the JSON string is invalid with respect to OperatingHours
     */
    public static OperatingHours fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, OperatingHours.class);
    }

    /**
     * Convert an instance of OperatingHours to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
