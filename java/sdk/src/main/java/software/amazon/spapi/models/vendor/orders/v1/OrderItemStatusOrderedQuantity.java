/*
 * Selling Partner API for Retail Procurement Orders
 * The Selling Partner API for Retail Procurement Orders provides programmatic access to vendor orders data.
 *
 * The version of the OpenAPI document: v1
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.vendor.orders.v1;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Ordered quantity information. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class OrderItemStatusOrderedQuantity {
    public static final String SERIALIZED_NAME_ORDERED_QUANTITY = "orderedQuantity";

    @SerializedName(SERIALIZED_NAME_ORDERED_QUANTITY)
    private ItemQuantity orderedQuantity;

    public static final String SERIALIZED_NAME_ORDERED_QUANTITY_DETAILS = "orderedQuantityDetails";

    @SerializedName(SERIALIZED_NAME_ORDERED_QUANTITY_DETAILS)
    private List<OrderedQuantityDetails> orderedQuantityDetails = new ArrayList<>();

    public OrderItemStatusOrderedQuantity() {}

    public OrderItemStatusOrderedQuantity orderedQuantity(ItemQuantity orderedQuantity) {
        this.orderedQuantity = orderedQuantity;
        return this;
    }

    /**
     * Get orderedQuantity
     *
     * @return orderedQuantity
     */
    @javax.annotation.Nullable public ItemQuantity getOrderedQuantity() {
        return orderedQuantity;
    }

    public void setOrderedQuantity(ItemQuantity orderedQuantity) {
        this.orderedQuantity = orderedQuantity;
    }

    public OrderItemStatusOrderedQuantity orderedQuantityDetails(List<OrderedQuantityDetails> orderedQuantityDetails) {
        this.orderedQuantityDetails = orderedQuantityDetails;
        return this;
    }

    public OrderItemStatusOrderedQuantity addOrderedQuantityDetailsItem(
            OrderedQuantityDetails orderedQuantityDetailsItem) {
        if (this.orderedQuantityDetails == null) {
            this.orderedQuantityDetails = new ArrayList<>();
        }
        this.orderedQuantityDetails.add(orderedQuantityDetailsItem);
        return this;
    }

    /**
     * Details of item quantity ordered.
     *
     * @return orderedQuantityDetails
     */
    @javax.annotation.Nullable public List<OrderedQuantityDetails> getOrderedQuantityDetails() {
        return orderedQuantityDetails;
    }

    public void setOrderedQuantityDetails(List<OrderedQuantityDetails> orderedQuantityDetails) {
        this.orderedQuantityDetails = orderedQuantityDetails;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        OrderItemStatusOrderedQuantity orderItemStatusOrderedQuantity = (OrderItemStatusOrderedQuantity) o;
        return Objects.equals(this.orderedQuantity, orderItemStatusOrderedQuantity.orderedQuantity)
                && Objects.equals(this.orderedQuantityDetails, orderItemStatusOrderedQuantity.orderedQuantityDetails);
    }

    @Override
    public int hashCode() {
        return Objects.hash(orderedQuantity, orderedQuantityDetails);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class OrderItemStatusOrderedQuantity {\n");
        sb.append("    orderedQuantity: ")
                .append(toIndentedString(orderedQuantity))
                .append("\n");
        sb.append("    orderedQuantityDetails: ")
                .append(toIndentedString(orderedQuantityDetails))
                .append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("orderedQuantity");
        openapiFields.add("orderedQuantityDetails");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to OrderItemStatusOrderedQuantity
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!OrderItemStatusOrderedQuantity.openapiRequiredFields
                    .isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in OrderItemStatusOrderedQuantity is not found in the empty JSON string",
                        OrderItemStatusOrderedQuantity.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!OrderItemStatusOrderedQuantity.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `OrderItemStatusOrderedQuantity` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        // validate the optional field `orderedQuantity`
        if (jsonObj.get("orderedQuantity") != null
                && !jsonObj.get("orderedQuantity").isJsonNull()) {
            ItemQuantity.validateJsonElement(jsonObj.get("orderedQuantity"));
        }
        if (jsonObj.get("orderedQuantityDetails") != null
                && !jsonObj.get("orderedQuantityDetails").isJsonNull()) {
            JsonArray jsonArrayorderedQuantityDetails = jsonObj.getAsJsonArray("orderedQuantityDetails");
            if (jsonArrayorderedQuantityDetails != null) {
                // ensure the json data is an array
                if (!jsonObj.get("orderedQuantityDetails").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `orderedQuantityDetails` to be an array in the JSON string but got `%s`",
                            jsonObj.get("orderedQuantityDetails").toString()));
                }

                // validate the optional field `orderedQuantityDetails` (array)
                for (int i = 0; i < jsonArrayorderedQuantityDetails.size(); i++) {
                    OrderedQuantityDetails.validateJsonElement(jsonArrayorderedQuantityDetails.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!OrderItemStatusOrderedQuantity.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'OrderItemStatusOrderedQuantity' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<OrderItemStatusOrderedQuantity> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(OrderItemStatusOrderedQuantity.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<OrderItemStatusOrderedQuantity>() {
                        @Override
                        public void write(JsonWriter out, OrderItemStatusOrderedQuantity value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public OrderItemStatusOrderedQuantity read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of OrderItemStatusOrderedQuantity given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of OrderItemStatusOrderedQuantity
     * @throws IOException if the JSON string is invalid with respect to OrderItemStatusOrderedQuantity
     */
    public static OrderItemStatusOrderedQuantity fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, OrderItemStatusOrderedQuantity.class);
    }

    /**
     * Convert an instance of OrderItemStatusOrderedQuantity to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
