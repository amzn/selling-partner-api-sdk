/*
 * Selling Partner API for Direct Fulfillment Orders
 * The Selling Partner API for Direct Fulfillment Orders provides programmatic access to a direct fulfillment vendor's order data.
 *
 * The version of the OpenAPI document: 2021-12-28
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.models.vendor.df.orders.v2021_12_28;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Objects;
import java.util.Set;
import software.amazon.spapi.JSON;

/** Total tax details for the line item. */
@javax.annotation.Generated(
        value = "org.openapitools.codegen.languages.JavaClientCodegen",
        comments = "Generator version: 7.9.0")
public class TaxItemDetails {
    public static final String SERIALIZED_NAME_TAX_LINE_ITEM = "taxLineItem";

    @SerializedName(SERIALIZED_NAME_TAX_LINE_ITEM)
    private List<TaxDetails> taxLineItem = new ArrayList<>();

    public TaxItemDetails() {}

    public TaxItemDetails taxLineItem(List<TaxDetails> taxLineItem) {
        this.taxLineItem = taxLineItem;
        return this;
    }

    public TaxItemDetails addTaxLineItemItem(TaxDetails taxLineItemItem) {
        if (this.taxLineItem == null) {
            this.taxLineItem = new ArrayList<>();
        }
        this.taxLineItem.add(taxLineItemItem);
        return this;
    }

    /**
     * A list of tax line items.
     *
     * @return taxLineItem
     */
    @javax.annotation.Nullable public List<TaxDetails> getTaxLineItem() {
        return taxLineItem;
    }

    public void setTaxLineItem(List<TaxDetails> taxLineItem) {
        this.taxLineItem = taxLineItem;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        TaxItemDetails taxItemDetails = (TaxItemDetails) o;
        return Objects.equals(this.taxLineItem, taxItemDetails.taxLineItem);
    }

    @Override
    public int hashCode() {
        return Objects.hash(taxLineItem);
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class TaxItemDetails {\n");
        sb.append("    taxLineItem: ").append(toIndentedString(taxLineItem)).append("\n");
        sb.append("}");
        return sb.toString();
    }

    /** Convert the given object to string with each line indented by 4 spaces (except the first line). */
    private String toIndentedString(Object o) {
        if (o == null) {
            return "null";
        }
        return o.toString().replace("\n", "\n    ");
    }

    public static HashSet<String> openapiFields;
    public static HashSet<String> openapiRequiredFields;

    static {
        // a set of all properties/fields (JSON key names)
        openapiFields = new HashSet<String>();
        openapiFields.add("taxLineItem");

        // a set of required properties/fields (JSON key names)
        openapiRequiredFields = new HashSet<String>();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to TaxItemDetails
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        if (jsonElement == null) {
            if (!TaxItemDetails.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
                throw new IllegalArgumentException(String.format(
                        "The required field(s) %s in TaxItemDetails is not found in the empty JSON string",
                        TaxItemDetails.openapiRequiredFields.toString()));
            }
        }

        Set<Map.Entry<String, JsonElement>> entries =
                jsonElement.getAsJsonObject().entrySet();
        // check to see if the JSON string contains additional fields
        for (Map.Entry<String, JsonElement> entry : entries) {
            if (!TaxItemDetails.openapiFields.contains(entry.getKey())) {
                throw new IllegalArgumentException(String.format(
                        "The field `%s` in the JSON string is not defined in the `TaxItemDetails` properties. JSON: %s",
                        entry.getKey(), jsonElement.toString()));
            }
        }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
        if (jsonObj.get("taxLineItem") != null && !jsonObj.get("taxLineItem").isJsonNull()) {
            JsonArray jsonArraytaxLineItem = jsonObj.getAsJsonArray("taxLineItem");
            if (jsonArraytaxLineItem != null) {
                // ensure the json data is an array
                if (!jsonObj.get("taxLineItem").isJsonArray()) {
                    throw new IllegalArgumentException(String.format(
                            "Expected the field `taxLineItem` to be an array in the JSON string but got `%s`",
                            jsonObj.get("taxLineItem").toString()));
                }

                // validate the optional field `taxLineItem` (array)
                for (int i = 0; i < jsonArraytaxLineItem.size(); i++) {
                    TaxDetails.validateJsonElement(jsonArraytaxLineItem.get(i));
                }
                ;
            }
        }
    }

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!TaxItemDetails.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'TaxItemDetails' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<TaxItemDetails> thisAdapter =
                    gson.getDelegateAdapter(this, TypeToken.get(TaxItemDetails.class));

            return (TypeAdapter<T>)
                    new TypeAdapter<TaxItemDetails>() {
                        @Override
                        public void write(JsonWriter out, TaxItemDetails value) throws IOException {
                            JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
                            elementAdapter.write(out, obj);
                        }

                        @Override
                        public TaxItemDetails read(JsonReader in) throws IOException {
                            JsonElement jsonElement = elementAdapter.read(in);
                            validateJsonElement(jsonElement);
                            return thisAdapter.fromJsonTree(jsonElement);
                        }
                    }.nullSafe();
        }
    }

    /**
     * Create an instance of TaxItemDetails given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of TaxItemDetails
     * @throws IOException if the JSON string is invalid with respect to TaxItemDetails
     */
    public static TaxItemDetails fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, TaxItemDetails.class);
    }

    /**
     * Convert an instance of TaxItemDetails to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}
