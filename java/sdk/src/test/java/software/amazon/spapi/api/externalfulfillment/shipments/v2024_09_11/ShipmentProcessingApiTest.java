/*
 * The Selling Partner API for Amazon External Fulfillment Shipments Processing
 * You can use the External Fulfillment Shipments API to retrieve, manage, and track shipments processed through Amazon's external fulfillment network. Use this API to get shipment details, monitor status changes, and access fulfillment requirements.
 *
 * The version of the OpenAPI document: 2024-09-11
 * Contact: marketplaceapitest@amazon.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package software.amazon.spapi.api.externalfulfillment.shipments.v2024_09_11;

import static org.junit.jupiter.api.Assertions.*;

import com.amazon.SellingPartnerAPIAA.LWAAuthorizationCredentials;
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse.BodyHandlers;
import java.util.*;
import org.jeasy.random.EasyRandom;
import org.jeasy.random.EasyRandomParameters;
import org.junit.jupiter.api.Test;
import software.amazon.spapi.ApiResponse;
import software.amazon.spapi.models.externalfulfillment.shipments.v2024_09_11.InvoiceResponse;
import software.amazon.spapi.models.externalfulfillment.shipments.v2024_09_11.ModelPackage;
import software.amazon.spapi.models.externalfulfillment.shipments.v2024_09_11.Packages;
import software.amazon.spapi.models.externalfulfillment.shipments.v2024_09_11.ShipLabelsResponse;
import software.amazon.spapi.models.externalfulfillment.shipments.v2024_09_11.ShippingOptionsResponse;
import software.amazon.spapi.models.productfees.v0.GetMyFeesEstimatesRequest;

public class ShipmentProcessingApiTest {

    private static final String endpoint = "http://localhost:3000";
    private static final String authEndpoint = "http://localhost:3000/auth/o2/token";
    private static final LWAAuthorizationCredentials credentials = LWAAuthorizationCredentials.builder()
            .clientId("clientId")
            .clientSecret("clientSecret")
            .refreshToken("refreshToken")
            .endpoint(authEndpoint)
            .build();

    private final ShipmentProcessingApi api = new ShipmentProcessingApi.Builder()
            .lwaAuthorizationCredentials(credentials)
            .endpoint(endpoint)
            .build();

    private final EasyRandom easyRandom = new EasyRandom(new EasyRandomParameters()
            .collectionSizeRange(1, 2)
            .randomizationDepth(10)
            .randomize(GetMyFeesEstimatesRequest.class, GetMyFeesEstimatesRequest::new));

    @Test
    public void createPackagesTest() throws Exception {
        instructBackendMock("shipmentProcessing", "createPackages", "204");

        String shipmentId = easyRandom.nextObject(String.class);

        Packages body = easyRandom.nextObject(Packages.class);

        api.createPackagesWithHttpInfo(shipmentId, body);
    }

    @Test
    public void generateInvoiceTest() throws Exception {
        instructBackendMock("shipmentProcessing", "generateInvoice", "200");

        String shipmentId = easyRandom.nextObject(String.class);

        ApiResponse<InvoiceResponse> response = api.generateInvoiceWithHttpInfo(shipmentId);

        assertEquals(200, response.getStatusCode());
        assertValidResponsePayload(200, response.getData());
    }

    @Test
    public void generateShipLabelsTest() throws Exception {
        instructBackendMock("shipmentProcessing", "generateShipLabels", "200");

        String shipmentId = easyRandom.nextObject(String.class);

        String operation = easyRandom.nextObject(String.class);

        ApiResponse<ShipLabelsResponse> response =
                api.generateShipLabelsWithHttpInfo(shipmentId, operation, null, null);

        assertEquals(200, response.getStatusCode());
        assertValidResponsePayload(200, response.getData());
    }

    @Test
    public void processShipmentTest() throws Exception {
        instructBackendMock("shipmentProcessing", "processShipment", "204");

        String shipmentId = easyRandom.nextObject(String.class);

        String operation = easyRandom.nextObject(String.class);

        api.processShipmentWithHttpInfo(shipmentId, operation, null);
    }

    @Test
    public void retrieveInvoiceTest() throws Exception {
        instructBackendMock("shipmentProcessing", "retrieveInvoice", "200");

        String shipmentId = easyRandom.nextObject(String.class);

        ApiResponse<InvoiceResponse> response = api.retrieveInvoiceWithHttpInfo(shipmentId);

        assertEquals(200, response.getStatusCode());
        assertValidResponsePayload(200, response.getData());
    }

    @Test
    public void retrieveShippingOptionsTest() throws Exception {
        instructBackendMock("shipmentProcessing", "retrieveShippingOptions", "200");

        String shipmentId = easyRandom.nextObject(String.class);

        String packageId = easyRandom.nextObject(String.class);

        ApiResponse<ShippingOptionsResponse> response = api.retrieveShippingOptionsWithHttpInfo(shipmentId, packageId);

        assertEquals(200, response.getStatusCode());
        assertValidResponsePayload(200, response.getData());
    }

    @Test
    public void updatePackageTest() throws Exception {
        instructBackendMock("shipmentProcessing", "updatePackage", "204");

        String shipmentId = easyRandom.nextObject(String.class);

        String packageId = easyRandom.nextObject(String.class);

        ModelPackage body = easyRandom.nextObject(ModelPackage.class);

        api.updatePackageWithHttpInfo(shipmentId, packageId, body);
    }

    @Test
    public void updatePackageStatusTest() throws Exception {
        instructBackendMock("shipmentProcessing", "updatePackageStatus", "204");

        String shipmentId = easyRandom.nextObject(String.class);

        String packageId = easyRandom.nextObject(String.class);

        api.updatePackageStatusWithHttpInfo(shipmentId, packageId, null, null);
    }

    private void instructBackendMock(String basename, String operationId, String code) throws Exception {
        String lowerCaseCompressedBasename = basename.replaceAll("/\"W| ", "").toLowerCase();
        String cleanOperationId = operationId.replaceAll("_\\d+$", "");
        HttpRequest request = HttpRequest.newBuilder()
                .uri(new URI(endpoint + "/response/" + lowerCaseCompressedBasename + "-" + cleanOperationId + "/code/"
                        + code))
                .POST(HttpRequest.BodyPublishers.noBody())
                .build();

        HttpClient.newHttpClient().send(request, BodyHandlers.discarding());
    }

    private static void assertValidResponsePayload(int statusCode, Object body) {
        if (statusCode != 204) assertNotNull(body);
    }
}
